// A tiny set of M1 tokens for the dev surface. NOTE: this is NOT the Design-System Catalog component
// library — M1 builds no component library (AGENTS.md). The canonical tokens + the F-06 type scale
// (21/15/11/9) + the component map land in M2 (a re-sync is owed first — OQ-111).
export const colors = {
  bg: '#0e0e14',
  surface: '#1a1a24',
  accent: '#e7c84b',
  text: '#f2f2f7',
  dim: '#8a8a99',
  border: '#2a2a38',
} as const;
