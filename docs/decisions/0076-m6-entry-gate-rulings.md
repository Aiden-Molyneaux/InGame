# 0076 — M6-entry gate rulings (the §0 sitting)

**Date:** 2026-07-16 · **Status:** LOCKED (owner: "I'll take your recommendations" — all twelve
ruled as recommended off the pre-filed skeleton) · **Owner:** Aiden
**Companion:** [0077](0077-m6-ach-starter-content.md) (the §0.4 starter-content picks) · the gate
receipt is [`m6-entry-decision-log.md`](../planning/m6-entry-decision-log.md); the brief is
[`m6-build-task.md`](../planning/m6-build-task.md).

| §0 | Ruling |
|---|---|
| **0.1** | **The friend-read class RATIFIED (gate-3):** the fourth SYS-01 read class — `// SYS-01-FRIEND-READ` marker + `friendScoped(...)` repo helper, lint-valid **only** with an explicit accepted-friendship predicate; every cross-user payload serializes through **allowlist shapes** (friend/full vs non-friend/limited per api-contract `/users/:id`) with PROF-03 privacy gates applied server-side; **blocked / suspended / deleted collapse to ONE indistinguishable "unavailable"** (MOD-09 — a standing test asserts the three are byte-identical, both directions). `rule-02-scoping` extended **statement-scoped** (the M5 neighbor-launder fix pattern). **G-D re-fires on this class** at P1. → product-spec 0.60 SYS-01. |
| **0.2** | **OQ-146 RESOLVED — publish-time label denormalization:** an additive migration snapshots the CARD-22 display-label set beside `premium_component_ids` at publish (+ backfill of existing published cards from their compositions). Unlocks the full equipped readout on SOC-11 / compare / gallery with **no cross-user composition read, ever**. → api-contract 0.69. |
| **0.3** | **OQ-145 posture RULED (implementation stays M8/G-N):** deletion **flips status, never DELETEs the row** (adopters-keep — the G-N P-HARD findings) · `authorShapeFor` **must be wired** into the four attribution read paths · **PII kept-set = keep financial rows (ledger/receipts), anonymize the user row** (accounting-grade retention). The beta ships **without in-app deletion** (App Store 5.1.1(v) binds at store submission, not TestFlight); the beta welcome note promises a support-channel manual path. |
| **0.4** | **ACH pull-forward RATIFIED (M7 → M6; OQ-004 pulled forward):** engine = full ACH-01/02/03/04/09 on the ACH-08 spine; content = the **SYS-04-style seed** (picks in **0077**: 12 milestones + 6 eggs) so eggs ship without an app release — the beta egg-recommendation loop is the design goal; **ECON-05's prestige/milestone half rides this engine now** (adoption-count milestones land early); ACH-06 celebration **in-app only at M6** (push half → M7, accepted gap). |
| **0.5** | **Report slice (E8b) — capture-only:** `reports` table + `POST /reports` (MOD-01 reason enum · details-required-where-specific · rate-bucketed) + the ReportSheet UX (board 4.16) on card/user/game with the **Block cross-link**. MOD-02 threshold auto-hide · MOD-03 queue · MOD-04 console → **M7**. With block, this completes the Guideline-1.2 pair for the beta. |
| **0.6** | **SOC-10 invites + OQ-096 (invite half) RESOLVED:** the pre-named **`AUTH-LOOKUP`** bearer read-class (0073 §0.1) implements `GET /invites/:token` — enumerated-repo, token-scoped, never session-authed. Token mechanics: **signed opaque · TTL 7 days · ≤5 active per user · multi-redemption within TTL** (a party QR survives multiple scans); create-new-invalidates-oldest. **Beta-window fallback:** universal links stay parked (§10) — in-app QR (client-rendered, OQ-073) + a tokenized URL on the project domain's static landing (→ TestFlight while in beta; store-listing routing lands M8). Share-link TTL/signature half of OQ-096 → M8. → api-contract 0.69. |
| **0.7** | **409 refusal-code family + buckets** (LOOK_CAP/0073 precedent; F-17 land-as-built; G-K async): `ALREADY_FRIENDS` · `REQUEST_PENDING` · `REQUEST_COOLDOWN {cooldownUntil}` · `SELF_TARGET` · `NOT_FRIENDS` · `LIST_FULL` (Top-10 cap-10 + the **queue cap 50** — closes OQ-097's WTP half) · `INVITE_INVALID` · `INVITE_EXPIRED`. Buckets (SYS-05-tunable): `friends:request` **10/hr + 30/day** · `invites:create` **5/day** · `reports:create` **10/day** (the reporter-cap half of OQ-093 lands early; dedupe → M7) · `users:search` **30/min** · `recommendations:create` **20/day**. Re-request cooldown default **7 days** (SYS-04). → api-contract 0.69. |
| **0.8** | **Discover M6 slice:** screen shell + **UP NEXT room in full** (WTP-01/02/03 + SOC-05 recs) · **DISCOVER room browse-only** (trending-cards — live since M5 P3 — + upcoming rows); notify-me toggles → M7 (inert without push) · DISC-02/03 → M7. **CAT-12 FRIENDS-ARE-PLAYING rail lands on Add Game** (`/catalog/friends-active`, drawn 0.23; slotted M6 by 0062). |
| **0.9** | **AUTH-01 breach check (the 0073 §0.9 re-slot, now due):** HIBP **range API / k-anonymity** at register + password change · **fail-open on provider outage** (availability beats the check; telemetry + a `breachCheckSkipped` signal) · no new runtime dep (plain fetch) · SYS-04 kill-switch. → product-spec 0.60 AUTH-01. |
| **0.10** | **Settings M6 slice:** the lean-list shell + the **BLOCKED page** (list via `GET /me/blocks` · unblock w/ `ConfirmSheet` — the MOD-09 lone-exception affordance) + sign-out homed in. Notifications page → M7 (rides push) · feedback pages → M7 · admin-console row → M7 · account-delete row → M8 (0.3). |
| **0.11** | **Beta mechanics (0071 made concrete):** cohort **5–20, majority iOS, 2–4 Android**. **Alpha = beta's first step** — the close-friends wave (~3–5) onto TestFlight **internal** as soon as the first EAS build exists (they alpha the M5 product; social lands under them). External TestFlight group for the full cohort (**Beta App Review submitted early** — days of lead); Android riders via **Play internal** when Google clears. Feedback + nudges = the **group chat** (push → M7, accepted). The **12-tester Play quota recruits beyond the cohort, starting now** (provisioning #9). Safety-rail acceptance recorded at the exit sitting (report ✓ in-scope early · block ✓ · delete trails M8). |
| **0.12** | **Carryover stances:** OQ-136 pick-a-card → **onboarding-era** (wants new-user context). OQ-140 composition presets → **⟨stretch⟩** only if the window has room. **OQ-142 RESOLVED** — the shipped **~6-stickers-per-zone cap ratified** (SYS-04-tunable) → product-spec 0.60 DEV-03. **Worn-premium-shell gate → GATE at M6**: the write gate exists (M5 F-2b `PREMIUM_UNRECONCILED`); the residue is pre-economy worn rows — a one-time data fix resets un-entitled worn premium shells/themes to the free defaults (rides P1's migration window). Store INDEX aisle-count honesty → the P10 lane. OQ-143 → a §1.1 design-spec footnote, rides the next design-spec touch. |

**Exit state:** product-spec **0.60** · api-contract **0.69** · OQs updated (096-invite-half /
097-WTP-half / 142 / 145-posture / 146 / 136 / 140 / 004-annotated) · m6-build-task §0 closed ·
`/health` 🟢 → **§3 build cleared** (the §1 friend-fabric spike first; P6c seed cleared by 0077;
P14 waits on the Google email; P15/P16 ride the owner's provisioning sittings).
