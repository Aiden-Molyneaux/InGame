# SCREEN STATUS — the page dashboard

> **What this is:** the always-current map of every app surface — design state, queue position,
> mockup version, **the implements-from file** (what the frontend build will compose from), whether
> its states board exists, whether **design-spec has formalized that board**, and whether the
> **api-contract covers the functionality shown on that page**. Updated **at the end of every draft pass** (both design
> sessions); the owner reads UP NEXT first. Row order = the IA in
> [`ui-design-requirements.md`](ui-design-requirements.md) Part 2. Mockup files live in
> **per-page subfolders** under [`mockups/`](mockups/) (file map: its README); the catalog
> stays at the mockups root.

**Last updated:** 2026-06-25 (**Contributor profile (§4.9 · CAT-07) — IN PASS:** the next un-started screen by road-to-market build-order (the only M5 surface). **3 distinct organizing models drafted** off Profile's `MY CONTRIBUTIONS` gateway → `mockups/contributor-profile/` — **A The Record** (by type: totals → games-added+reach · cards-designed+adoptions · field-edits · badges) · **B The Trophy Wall** (by prestige: badge case + most-adopted card hero, lists demoted) · **C The Impact Dashboard** (by reach: footprint heroes + items ranked by impact). Each renders **P1–P5** (self · empty contributor-hook · friend-view · privacy-limited PROF-03 `lock-well` · Skeleton); Offline/LoadError deferred to converge. **Burt PASS ×3** (0 blocker/major) — F-02 held (gold only on the P2 acquisitive hooks + Store key + C's `RankChip`/first value-marker), **badges drawn non-gold** (owner-ratify vs the Friends `.achv` gold glyph). Backing **`GET /users/:id/contributions` is prose-only** → API page-audit + OQ owed at converge. Brief + README under the track folder. **Next: owner gate → converge `contributor-states.html`.** · **Prior (2026-06-24) — Collection peek-flip — COL-12 added (spec-owner):** decision 0026 reverses OQ-059/0025 — the Collection gains an in-place **card peek-flip** (shelf+grid; **tap → CARD-01 stats back · tap-back returns · VIEW GAME → 4.2 · long-press shortcut**; friend-view privacy-gated). **product-spec 0.24 · api 0.24 · design-spec 0.25** (§2.1 + COL-12); **no on-face indicator** (owner) — discoverability via a first-run coachmark. **Board:** the COL-12 stage (grid · shelf · friend) was folded into the canonical **`collection-states.html`** (scratch variant retired, 2026-06-24); on-back VIEW GAME reworked to the real `KeycapButton/secondary`. · **Prior (same day): Catalog v0.5 — the flat in-screen switch family cataloged (owner-requested):** the [`InGame Design System Catalog`](mockups/InGame%20Design%20System%20Catalog.dc.html) Buttons card gains **SEGMENTBUTTON** — the flat **SegmentedKeycap/pair** (Discover UP&nbsp;NEXT↔DISCOVER toggle) + **SectionSwitch** (game-page dossier PLAY·CARDS·ABOUT + the device-editor section rail), **active = accent border + accent icon** (F-09 — a border, not a keycap; **no pip**), with the `+CountTag` badge; extracted verbatim from [`discover-states.html`](mockups/discover/discover-states.html) `.seg-pair` + the converged [`game-page-states.html`](mockups/game-page/game-page-states.html) `.tabsw`. **Closes the catalog's "segmented control (Discover)" gap line.** design-spec §1.5 already names `SegmentedKeycap` (0.15) — but its prose voices active as *"pressed + StateMark"*; the **flat** treatment the converged boards render is the same direction as the **flat-button ripple** (owner picked Inset Recess 2026-06-17) — so the catalog now shows the converged flat form, and the §1.5 prose reconcile + app-wide `SectionSwitch` unification stay tracked at **OQ-063** / the flat-button ripple. No converged-board state/API change. · **Prior (same day):** **Game page (4.2) CONVERGED → `game-page-states.html`:** candidate B (the dossier merge) folded into the canonical states board — the **8 core artboards** (M1 PLAY dual-face · M2 EDIT form · M3 CARDS switcher · M4 gallery+adopt · M5 ABOUT · M6 neutral · M7 friend+compare · M8 upcoming/be-first) **+ the deferred lifecycle states** (L1 loading `Skeleton` · L2 `LoadError` · L3 `Offline` writes-gated + error→`Toast`, SYS-10 · L4 soft-hidden/reported, **MOD-02/08/09**), the lifecycle grammar reused verbatim from the sibling boards; the owner-picked **flat Inset-Recess keycaps** carried; the **`.btn.sec`→`.btn.cream`** KeycapButton/SectionHeader split applied (Bugfix #4). Reconciliations flagged to the inbox: **OQ-069** (CARD-01 card-back adoption-count) · **OQ-070** (WISHLIST in the owned editor); OQ-058/061 kept open. **Design-spec formalization + API page-audit now owed (spec-owner).** The `dual-dossier`(+`-flat-buttons`/`-typescale`) · `trophy-*` · `draft-*` · `mix-takes` lineage retired, kept for history. · **Prior: Design-system — flat / 2D button exploration (owner-requested, drafted on the Collection Shelf):** [`collection-flat-buttons-drafts.html`](mockups/collection/collection-flat-buttons-drafts.html) renders the Shelf main page **4-up — the current 3D original (baseline) + 3 columns with the in-screen keycaps flattened** — `shadow.key` (the `0 3px 0` raised edge / clipped drop-shadow) **and** the press-down translate removed — each with a distinct flat pressed-affordance: **A** tonal inversion (fill↔ink) · **B** inset recess (inner shadow, no motion) · **C** accent ring (inset, hugs the C5 notch). Scope = screen `KeycapButton`/`ToolKeycap` only (LOG HOURS · sort/filter/view · ADD); the shell `NavBand` keys (`0 4px 0`) stay physical. **Owner PICKED B · Inset Recess (2026-06-17)** — first applied to a new game-page variant ([`game-page-dual-dossier-flat-buttons.html`](mockups/game-page/game-page-dual-dossier-flat-buttons.html)), verified flat across all 8 artboards (idle + pressed-recess, no over-reach; CSS-coverage audit clean — adversarial workflow). **Round 2 (owner asked for 3 more options):** [`collection-flat-buttons-drafts-2.html`](mockups/collection/collection-flat-buttons-drafts-2.html) drafts **D notch-ignite (lit C5 step-corners) · E snap-pip (leading pixel marker) · F scanline-energize (CRT texture)** beside B — generated/selected by an ideation panel, all verified flat / no-motion / clip-safe on the notched ADD; final affordance pick under review. **Still owed (once the affordance is locked): design-spec F-03 `shadow.key` ripple + apply across all remaining live boards (cf. the marker audit). No converged-board state/API change yet.** · **Prior:** **Game-page track (4.2) — dual-face hero cards sized to the catalog `/grid` (collection 2-up)** ([`game-page-dual-dossier.html`](mockups/game-page/game-page-dual-dossier.html)): owner clarified the `/grid` size targets the **dual-face hero (THE FACE + YOUR STATS)**, not the switcher/gallery — so the two hero cards now render **2-up at 161×225**, filling the PLAY row like the collection grid (base art 7px + plate 10px), **superseding their 2026-06-16 `/hero` (138×193) sizing**; the catalog `/grid` specimen was corrected from an idealized 110px to the **real collection 2-up size (161×225)**. An earlier same-day pass had taken the **CARDS switcher (M3) + community gallery (M4)** 2-up — **reverted** per owner; they stay **3-up `cell`**. *(Catalog correction reflects the converged §3.1 collection — no spec/API ripple; game page still in-pass.)* **· Prior (2026-06-16): dual-face hero cards sized to the catalog `/hero`** ([`game-page-dual-dossier.html`](mockups/game-page/game-page-dual-dossier.html)): the **front (face) + back (stats)** dual-face cards were a local `face2` (146×204) → now the **catalog GameCard `/hero` (138×193, F-01)**; the stale local `.hero` (168×235) was corrected to the catalog token and the now-dead `.face2` rule removed (pure size alignment — no spec/API ripple; brings the mockup into compliance with the already-formalized token). **· Prior (2026-06-15): owner PICKED the dossier merge (B) + refined** ([`game-page-dual-dossier.html`](mockups/game-page/game-page-dual-dossier.html)): owner *"the dossier take looks better"* → **B chosen over `trophy-dualface` (A)**; 3 refinements — **(1)** the bottom **`SectionSwitch` is one-line** (icon inline w/ label; **pink pip dropped** → matches the device-editor rail + the marker audit; active = orange border + orange icon) · **(2)** **CARDS tap-select** (thin **orange ring + gap** on the selected card) · **(3)** that card's **inline options** (its `EquipReadout` + **SET AS MAIN · EDIT IN STYLER · DELETE**). **Next: converge B → `game-page-states.html`.** · **Prior (same day): the merge drafted** — the dossier's section switcher (PLAY·CARDS·ABOUT) moves to the **bottom of the screen** in the `device-switcher` **SectionSwitch** style; the **dual-face hero is kept**; **stats edited via a dossier form** (not on the card back); all functionality across **8 artboards** (M1 PLAY · M2 form-edit · M3 switcher · M4 gallery+adopt · M5 ABOUT · M6 neutral · M7 friend dual-face+compare · M8 upcoming/be-first); buttons corrected (orange `/primary`, gold = card-creating). Sits beside `trophy-dualface` as candidate **B** for the owner's pick. · **Prior — On-screen state-marker audit (design-system)** — the pink `ChipPip`/`PipLight` leaks onto flat, square screen chrome in 3 roles (tool keycaps · F-09 selection · segments); **3 replacement drafts** ([`mockups/design-system/onscreen-marker-drafts.html`](mockups/design-system/onscreen-marker-drafts.html)) swap it for an `scr.accent` **orange · flat · square** marker (F-05/F-07/F-09-as-written); shell LEDs stay pink. Audit [`audit/2026-06-14/onscreen-pip-marker-audit.md`](audit/2026-06-14/onscreen-pip-marker-audit.md). Owner picks → spec-owner ripple. · **Prior (2026-06-13): Game-page track (4.2) — A WON the gate, rev 4 applied**: owner ruled "moving forward with A" ("card-led scroll"); B/C retired. Three owner changes now in `game-page-draft-a-cardled` (rev 4): a dedicated **owned-landing** panel (P2 — the just-opened resting state; the stats panel becomes P2b) · the **⋮ overflow is now a pull-up drawer** · **COMPARE now opens a drawer** (P6) — every heavy surface shares the one bottom-sheet grammar. Still carries rev-3 (catalog buttons · rating PENDING OQ-058 · card-back no "made in InGame"). Next: any further A tweaks → converge → `game-page-states.html` (+ deferred lifecycle states). Ruling appended to the brief. · **Collection track — §3.1 friend-view browse parity (COL-11 / decision 0021)**: `collection-states.html` gains the friend-view **read-only browse bar** on both artboards + 2 new artboards (**sorted-by-hours list** · **read-only sort/filter drawer**, no Arrange); board ✅, API ✅ (widened 0.20), **Design-spec ⬜** (§2.1 friend-tools formalization owed). · **spec-owner — §4.2 Game page prepped (decision 0020)**: friend card-tap → the Game page **friend-view state** · their card + **equipped readout** (CARD-22) + per-game ctx · **opt-in compare** · **atomic adopt** (SOC-11); spec 0.20 / api 0.19 / design-req 0.15. · **Discover track — §3.2 Discover CONVERGED** (`discover-states.html`): owner picked **A "Two rooms"** + edits (bottom/smaller `SegmentedKeycap` · standard header · real games · Browse-By cut → OQ-057 · simplified Up Next + Discover); board ✅, **Design-spec ✅** (formalized → 0.15: §1.5 Discover set — `SegmentedKeycap`/`QueueRow`/`ReleaseRow`/`RecRow`/`AdoptCount`/`NotifyToggle` + §2.7), **API ✅** (page-audited → 0.21: queue shape · trending-cards shape · upcoming notify-me · friend-recs feed · DISC-02 parked; OQ-053/054/055/057 resolved). Drafts a/b/c kept for history. Brief `plans/2026-06-13-discover-drafts-brief.md`. Prior: report track — **§4.16 Report converged + design-spec formalized** (`report-states.html`, A `/drawer`; 3 targets × full matrix; MOD-01/02 · SOC-09 · SYS-10): board ✅, API ✅ (page-audited), **Design-spec ✅ (formalized → 0.14: §1.5 lifecycle + `ReportConfirm` + §2.6)**. Prior: canvas track — Canvas converged (print shop · `canvas-states.html`), the 0014 editor arc complete, OQ-007 + OQ-040 resolved; spec-owner triage 0019.)

## Up next (the queue)
- **▶ B PICKED + reworked (design — Contributor profile 4.9, 2026-06-26): owner confirm → converge.** §4.9 "My Contributions" (CAT-07), the M5 surface. Owner picked **B The Trophy Wall** (by prestige) and ruled three changes, applied to `contributor-draft-b-trophy.html`: **(1)** removed the achievement badges; **(2)** added **contributor percentile tags on the stat tiles** — small gold `PctPill`s, **the Profile PROF-07 pattern** (GAMES · TOP 25%, ADOPTIONS · TOP 10%), on the P1/P3 stat row + P4's public totals; **(3)** dropped **field-edits**. *(First take was a dedicated standing bar; owner redirected 2026-06-26 to the Profile-style tags-on-numbers treatment.)* **Burt clean.** The change **resolves the badge-gold flag** (percentile tags are catalog-gold PctPills) but **diverges from CAT-07** → **OQ-079** (spec-owner ripples product-spec CAT-07 + defines the percentile data model). `GET /users/:id/contributions` still prose-only → API page-audit owed at converge. Drafts A/C kept for history. **Next: converge B → `contributor-states.html`** + design-spec + API page-audit (OQ-079). Brief + gate ruling `mockups/contributor-profile/contributor-profile-brief.md`.
- **✅ DONE (design — Collection, 2026-06-24): COL-12 peek-flip folded into the canonical board.** The peek-flip (product-spec **COL-12** · api **0.24** · design-spec **§2.1 / 0.25**, decision 0026 reversing OQ-059/0025) now lives in the canonical **[`collection-states.html`](mockups/collection/collection-states.html)** as the **States · card peek-flip (COL-12)** stage — 3 artboards (grid · shelf · friend, privacy-gated); the scratch variant was retired. On-back **VIEW GAME** reworked from a bespoke chip to the real `KeycapButton/secondary` (cream · flat · Scanline · `mini`+`block`). **Burt PASS ✅ (0 blocker/major).** No on-face indicator (owner); discoverability = a first-run coachmark. Owner review of the flip UI still welcome.
- **▶ AUDITED ✅ (design-system, 2026-06-23): Burt 13-board conformance audit — all PASS, 0 blocker / 0 major.** Every `*-states` board verified vs **F-01..F-09 + §1.8 lifecycle + standalone hygiene**; each finding adversarially re-checked (1 false positive killed). **F-02 gold clarified → _acquisitive_** (card-creating · PIXELS economy · primary add-to-collection; orange = prominent non-acquisitive — RETRY / ADD&nbsp;FRIEND / SHARE) — formalized **design-spec 0.24 + Catalog v0.8 + the Burt F-02 check**. **Minor fixes applied:** F-01 inline/skeleton off-ladder cards (styler `.sk-card`→138×193 · game-page CardDetail 120→138, compare 105→**96** owner-pick, `.hidden-cell` 78→64) · F-05 canvas `.guide-v` pink→orange · F-02 canvas SHARE gold→orange · F-07 discover `.dropline` squared · F-03 find-add `.head-act` press→Scanline + stale captions. **Left for owner:** Discover ADD-FROM-COLLECTION gold (blessed as primary add-to-collection) · `.half-panel` bezel-fragment radius (add-game/styler/canvas) · store `.rtile` gold-border selection · profile avatar inline hex (nit) · stale "pink-pip" caption prose (OQ-067 doc tail).
- **▶ RIPPLED ✅ (design-system, 2026-06-18): flat / 2D buttons = Scanline Energize (LOCKED).** The screen Keycap system is now **flat** (no `shadow.key` 3D); pressed/active = **Scanline Energize** — CRT scanlines over a hairline-darkened fill, **no motion, no pip**. **Formalized:** design-spec **0.20** (F-03 rescoped — keycap travel = shell nav only · `shadow.key` deprecated for screen · `motion.keyPress` → shell-only + new `motion.scanlineEnergize` · `KeycapButton`/`ToolKeycap`/`CountKeycap` defs flattened) + **Catalog v0.6** specimens (flat + scanline; render-verified). **Rippled across all 14 boards** — collection · h2-underlay · discover · profile · store · add-game · styler + styler-flat-chips · canvas · device · settings · report · game-page-states + game-page-dual-dossier — each a labelled per-board override block, **render-verified** (idle-flat + scanline-press, no motion). Kept: physical `NavBand` keys 3D · segmented/tab **accent-border** active · clip-path corner-notches · CountKeycaps (`.ccount`) flatten-only. Owner-pick path: A inversion → B recess → **F Scanline Energize (locked, no stepped-corner fill)**. **Superseded → history:** the exploration drafts `collection-flat-buttons-drafts.html` (round-1 A/B/C) · `collection-flat-buttons-drafts-2.html` (round-2 B/D/E/F) · `collection-button-style-spec.html` (stepped-corner) · `game-page-dual-dossier-flat-buttons.html` (the earlier B-recess variant). **Remaining:** Burt DS-audit spot-checks; segmented-control app-wide consolidation stays **OQ-063**.
- **✅ DONE (design-system): On-screen state marker** — owner chose **Draft A** = a solid **orange pixel-square** (`scr.accent`, flat, **one pixel notched off each corner**, right-angle pixel-art) → **[`mockups/design-system/onscreen-marker-drafts.html`](mockups/design-system/onscreen-marker-drafts.html)** (B tick · C lit edge kept for history). **Applied across all live boards 2026-06-15:** the pink `ChipPip`/`PipLight` → the orange pixel-square in **collection · h2-underlay · profile · report · add-game · styler · discover · canvas** (canvas's `.spip`/`.base-sw`/`.csw`/`.cat-pip` were extra finds beyond the audit) **+ the Catalog's 3 on-screen ChipPip demos**; selection borders were already orange; **shell NavBand + DeviceShell LEDs stay pink** (F-05). Verified — no on-screen pink-pip glow remains on any live board; report-states spot-checked in render. Audit [`audit/2026-06-14/onscreen-pip-marker-audit.md`](audit/2026-06-14/onscreen-pip-marker-audit.md). **Done (design-spec 0.18 · OQ-067 resolved):** F-09 reworded + the `ChipPip`/`PipLight` → `StateMark` rename across §1.5 + the Catalog labels. *(Device + game-page already pip-free.)*
- **✅ FORMALIZED 2026-06-22 (spec-owner) — Game page (4.2).** Candidate B → **[`game-page-states.html`](mockups/game-page/game-page-states.html)**: the dossier merge folded into the canonical states board — **8 core artboards** (M1 PLAY dual-face + dossier readout · M2 EDIT form · M3 CARDS switcher tap-select + inline options, OQ-056 · M4 community gallery → atomic-adopt `CardDetail` + confirm · M5 ABOUT · M6 neutral PLAY-locked · M7 friend dual-face + privacy-gate + compare · M8 upcoming/be-first) **+ the deferred lifecycle states** (L1 loading `Skeleton` · L2 `LoadError` "Signal Lost"+RETRY · L3 `Offline` writes-gated + error→`Toast`, SYS-10 · L4 soft-hidden/reported, **MOD-02/08/09** — grammar reused verbatim from collection/discover/device/settings). The **flat Scanline-Energize keycaps** (F-03, formalized 0.20) carried; the **`.btn.sec`→`.btn.cream`** secondary-keycap modifier split off so the KeycapButton no longer reuses the SectionHeader `.sec` (Bugfix #4). **Design-spec ✅ formalized 0.21 (§2.4b + §1.5 Game-page set; decisions 0020/0021/0024/0025) · API ✅ page-audited, no gap.** OQ-058 (rating) · OQ-061 (delete/`ConfirmSheet`) remain open; OQ-056/OQ-063 cross-referenced. The `dual-dossier`(+`-flat-buttons`/`-typescale`) · `trophy-*` · `draft-*` · `mix-takes` lineage retired, kept for history. Brief: `plans/2026-06-13-game-page-drafts-brief.md`.
- **✅ Device editor (4.5) CONVERGED** (`device-states.html`) — owner picked **A "in-frame live edit"**; the **`SectionSwitch`** rail won (bottom-docked · icon in-line · no pip · **4 sections** incl. **LOOKS**). Owner refinements baked in: **stickers on plastic only** (screen + 5 keys no-go, DEV-03/04/F-04) · saved looks **identified by shell·theme** (no name) · **SAVE CURRENT** only. Full D1–D10 matrix (editing · shell-switch · theme-preview · sticker place/scale/rotate · **OQ-045 on-shell preview** · LOOKS · reconcile · Skeleton · Offline writes-gated · LoadError). Drafts a/b/c + `switcher-takes`/`switcher-rail`/`draft-a-looks` kept for history. Brief `plans/2026-06-14-device-editor-drafts-brief.md`. **Owed (spec-owner): design-spec formalization (`SectionSwitch`/`SectionCard` · `StickerStage`/`TransformBox`/`PlacedSticker`/`StickerTray` · `SavedLook`/`LooksGrid`) + API page-audit; inbox OQ-045 (closed design-side) · OQ-062 · OQ-063 (rail app-wide?) · OQ-064.**
0. **🎉 The 0014 editor arc is COMPLETE** — Add Game → Styler → Canvas all converged. OQ-007 (breakout) + OQ-040 (first-print ritual) resolved design-side in the Canvas board.
1. *Formalization batches owed (spec-owner):* the **Styler converge set** (design-spec §2.5 already landed 0.13 — the catalog component shells) + the **Canvas converge set** (design-spec §2.x + the editor's open-composition components: `CanvasStage` · `AssetShelf` · `ElementTray` · `LayerRack`/slips · `PrintRitual`); + the **Canvas API page-audit** (writes already in 0.16/0.18 — confirm no gap). *(Report converge set — **done 0.14**: §1.5 `ReportSheet` lifecycle + `ReportConfirm` + §2.6.)*
2. *Coverage-driven:* **Settings** ✅ **CONVERGED** (`settings-states.html` — gate ruling 2026-06-14: **C "type-first triage"** feedback + **notifications as its own page**; the lean-list + row-opens-a-page grammar; new components **`Toggle`/`ConfirmSheet`/`LogAttach`/`FeedbackConfirm`/`TriageCard`**; drafts kept for history). **Owner refinements 2026-06-14:** feedback SUBMIT → mid-width `KeycapButton/primary` (orange) · destructive confirm → bottom `ConfirmSheet` (was `ConfirmDialog`). **Design-spec ✅** (formalized → **0.16**: §1.5 Settings set + §2.8; toggle + confirm-dialog gaps closed; Catalog HTML sync lags) · **API ✅** (no gap). **Settings-track work fully complete.** **Device editor** (DEV-01..04; carries the OQ-045 sticker-on-shell debt) is now **in pass** (brief authored; 3 drafts → gate — see ▶ IN PASS above). *(Inbox for next triage: **Settings OQ-060** (log-bundle shape) + the **`ConfirmSheet` unification** with the game-page track (OQ-061) · OQ-038 offline-cache-scope · OQ-058/059 (game-page) · the OQ-002/009/010/011 set.)*

## Legend
**State:** ✅ converged · 🔶 in pass / partial · 🔜 queued (#n) · ⬜ not started ·
**Implements from:** the exact mockup file(s) the frontend build composes from — filled at converge (normally the states board); — until one exists ·
**States board:** the §1.8/§1.6 state-matrix mockup exists ·
**Design-spec:** ✅ design-spec (Part-2 composition + §1.5 components) deliberately synced to this page's converged board · ⬜ formalization owed · — nothing to formalize yet ·
**API:** ✅ contract deliberately synced to this page's functionality · 🔶 endpoints drafted, not yet page-audited · ⬜ not mapped

| § | Screen | State | Mockup (version) | Implements from | States board | Design-spec | API | Notes / next |
|---|---|---|---|---|---|---|---|---|
| 3.1 | Collection (home) | ✅ converged | `h2-underlay-v2` + `collection-states.html` (per 0011–0013 + **COL-11**) | `collection/collection-states.html` + `collection/h2-underlay-v2-c5-hybrid-ds-enforced.html` | ✅ (friend-view **+ COL-11 browse tools**, lifecycle, offline) | ✅ (§2.1; 0.8/0013 · **COL-11 friend-tools 0.18** · **COL-12 peek-flip 0.25**) | ✅ (page-audited 0.17 · **COL-11 widen 0.20** · **COL-12 ripple 0.24** — `designer` rider + `percentComplete`) | 0.17 audit: list totals + item shape · `/me/collection/reorder` · now-playing pin · `/catalog/popular` (OQ-051, 0019). **COL-11 (decision 0021):** friend-view gains **full read-only browse tools** — bar parity on both friend artboards + a **sorted-by-hours list** & **read-only sort/filter drawer** (no Arrange); design-spec §2.1 friend-tools **formalized 0.18**. · **COL-12 peek-flip (decision 0026 — reverses OQ-059/0025):** shelf+grid card flips in place to the CARD-01 stats back — tap-to-flip · VIEW GAME → 4.2 · long-press shortcut · friend-view privacy-gated; **no on-face indicator** (owner). Board: the **States · card peek-flip (COL-12)** stage (grid · shelf · friend) folded into the canonical **`collection-states.html`** (variant retired, 2026-06-24). |
| 3.2 | Discover | ✅ converged | `discover-states.html` (Two rooms; gate ruling 2026-06-13 — **bottom/smaller toggle · standard header · real games · Browse-By cut · simplified pages**) | `discover/discover-states.html` | ✅ (Up Next + Discover; lifecycle: skeleton · Signal-Lost+RETRY · offline writes-gated) | ✅ (§2.7 + §1.5 **Discover set**: `SegmentedKeycap`/`QueueRow`/`ReleaseRow`/`RecRow`/`AdoptCount`/`NotifyToggle`; 0.15) | ✅ (**page-audited 0.21**: `/me/queue` shape · `/discover/trending-cards` shape · upcoming **notify-me** + `release` pref · friend-recs **feed** (`GET /me/recommendations`) · `/discover/browse` parked → Game page; **OQ-053/054/055/057 resolved**) | **A "Two rooms" won the gate** + owner edits; drafts a/b/c kept for history. **Aesthetic call (2026-06-13): rows/rails won** — the card-fan sibling `discover-states-fan.html` is retired (kept for history) |
| 3.3 | Friends | ✅ converged | `friends-states.html` (A "Feed-first"; converged 2026-06-23) | `friends/friends-states.html` | ✅ (P1–P6 + lifecycle **L1 Skeleton · L2 Signal-Lost+RETRY · L3 Offline** writes-gated · **Q1 quiet-feed/empty-requests**) | ✅ (§1.5 Friends/Social set + §2.10; 0.23 — `FeedRow`/`FriendRow`/`RequestRow`/`InviteHook`; **`PresenceDot`/`StatPeek` CUT**) | ✅ (page-audited 0.23 — `GET /me/feed` item shape OQ-071; recommend-compose OQ-075 = design gap) | **CONVERGED 2026-06-23 → `friends-states.html`** (owner ruling: **A "Feed-first"** won; **B/C retired to history**). Carries Draft A's **P1–P6** (feed landing · active feed · roster SOC-01 · ADD FRIENDS hub · cold-start · actions sheet — VIEW/COMPARE→4.6/RECOMMEND/UNFRIEND/REPORT→4.16/BLOCK SOC-09, **no SHARE** OQ-052); **flat buttons rippled Inset-Recess→Scanline Energize** (F-03, the draft was skipped by the 2026-06-18 sweep); **F-06 clean** (no drift). **Lifecycle now drawn** + the **quiet/empty** state (Draft A only described these). **Find/add + full requests inbox = 4.8** (header key + banner jump there — the IA seam reconciled). **Deferred: SOC-05 recommend-compose** surface (entry-only) → **OQ-075**; feed shape → OQ-071. **Burt PASS** (1 owner-ratification: `.achv` gold-as-achievement glyph, inherited). Presence + mutual counts stay CUT (owner 2026-06-18). Brief `mockups/friends/friends-drafts-brief.md`. **Formalized 2026-06-23: design-spec §1.5 + §2.10 + api 0.23 (OQ-071 resolved). OQ-075 (recommend-compose) stays open.** |
| 3.4 | Store (incl. 4.11/4.12) | ✅ converged | `store-states.html` (5 rulings + 0017) | `store/store-states.html` | ✅ (incl. lifecycle, writes-gated offline) | ✅ (§2.3; 0.10) | ✅ | OQ-045 sticker preview → Device editor pass |
| 3.5 | Profile | ✅ converged | `profile-states.html` (per 0011–0013) | `profile/profile-states.html` (+ the h2-underlay board's Profile artboard) | ✅ (edit, friend-view, privacy, terminal, offline) | ✅ (§2.2; synced 0.8/0013) | ✅ (page-audited, 0.17) | 0.17 audit: `/me` + `/users/:id` enumerated field-level (cooldown · device payload · limited card · `relationship`); friend-view SHARE chip cut (OQ-052 resolved, 0019) |
| 4.1 | Add Game (flow) | ✅ converged | `add-game/add-game-states.html` (c6 + P9–P10 + PIXELS + `ReportSheet/drawer`) | `add-game/add-game-states.html` | ✅ (P1–P10 + P3b/P7b; lifecycle + writes-gated offline) | ✅ (§2.4; 0.12 + catalog v0.4) | ✅ (0016 + MOD-01 ripples) | — |
| 4.2 | Game page (adaptive) | ✅ converged | `game-page-states.html` (B, the dossier merge; converged 2026-06-17) · history: `dual-dossier`(+`-flat-buttons`/`-typescale`) · `trophy-dualface`/`trophy-collapsing`/`trophy-takes` · `draft-a/b/c` · `mix-takes` | `game-page/game-page-states.html` | ✅ (M1–M8 + lifecycle L1–L4: `Skeleton` · `LoadError` · `Offline` writes-gated + `Toast` · soft-hidden/reported MOD-02/08/09) | ✅ (formalized 0.21 — §2.4b + §1.5 Game-page set; decisions 0020/0021/0024/0025) | ✅ (page-audited 0.21 — no gap: `PATCH/DELETE /me/collection/:entryId` · `GET /me/collection/:entryId/cards` · `GET /games/:gameId/cards` (CARD-22 `equipped`) · `POST /cards/:id/adopt` · `DELETE /cards/:id`+`/unpublish` · `GET /users/:id/collection` (0020/0021) · `GET /catalog/games/:id`; compare client-side, decision 0020) | **CONVERGED 2026-06-17 → `game-page-states.html`:** candidate B folded into the full state matrix — 8 core (M1–M8) + the deferred lifecycle L1–L4 (Skeleton · LoadError · Offline writes-gated + Toast · soft-hidden/reported MOD-02/08/09, grammar reused from the sibling boards); flat **Scanline-Energize** keycaps (F-03, formalized 0.20) carried; the **`.btn.sec`→`.btn.cream`** KeycapButton/SectionHeader split (Bugfix #4). Reconciliations: card-back clean → OQ-069 (CARD-01 adoption-count, encoded decision 0024); WISHLIST omitted → OQ-070 (encoded decision 0025); OQ-058 (rating, encoded 0024) · OQ-061 remain open. **Design-spec ✅ (formalized 0.21: §2.4b + §1.5 Game-page set) · API ✅ (page-audited 0.21, no gap).** · **Prior — Re-brainstorm → card-as-trophy → owner picked A → high-fi (`trophy-collapsing`) → iteration (`trophy-dualface`, 2026-06-14):** the **hero shows both card sides side-by-side** (face + stats back, no flip; facts as a plain meta line) · **EDIT enlarges the stats card & edits the values directly on the card back** (provenance auto) · **Your Cards: tap-select (orange outline) + inline on-screen options** (SET-MAIN/EDIT/DELETE→`ConfirmSheet`, no hold-drawer) · **Share Card Image = a single docked footer at the bottom** (removed from per-card; owned only). Conventions held (**orange `KeycapButton`**, gold = card-creating only; `shadow.key`; C5; F-01/F-06/F-09). Panels A1 dual-face hero · A2 on-card stat edit · A3 scrolled (select+inline-options+bottom-share+delete-confirm) · A4 friend dual-face. **Refined (owner 2026-06-14):** screen title → **"GAME"** · **facts as a plain meta line under the game title** (boxed chips reverted) · the **stats card frame mirrored on the Y axis** (C5 step on opposite corners) · **EDIT is inline** (same screen, no mode) with the **stats card as the only card** while editing · **Your Cards selection = a thin orange border** (pip dropped) · **collapsed header = play stats only** (styler readout dropped) · **card-back credit = "CARD ART DESIGNED BY" + the account name** (adoption count off the back → reconcile CARD-01 at converge). **Next: owner review → converge → `game-page-states.html`** (full matrix + deferred lifecycle), then Design-spec + API page-audit. Carries rating PENDING OQ-058 · delete OQ-061. · **CHOSEN: candidate B (`game-page-dual-dossier`, 2026-06-15)** — owner *"the dossier take looks better."* The dossier's section switcher (PLAY·CARDS·ABOUT) **docked at the bottom** as the **`SectionSwitch`** (now **one-line**: icon inline w/ label; **pink pip dropped**, active = orange border + orange icon) · **dual-face hero kept** · **stats edited via a dossier form** (not on the card) + pinned DONE bar · the **CARDS switcher gained tap-select** (thin **orange ring + gap** on the selected card) + **inline options** (SET AS MAIN · EDIT IN STYLER · DELETE + `EquipReadout`). **8 artboards** capture all functionality. **Next: converge B → `game-page-states.html`; A retired.** · **Cards → catalog `/hero` (2026-06-16):** the dual-face hero (face + stats back, all 3 pairs — M1 owner-rest · M2 form-edit · M6/M7 friend-view) sized to the **catalog GameCard `/hero` (138×193, F-01)** — was a local `face2` (146×204); stale local `.hero` (168×235) corrected to the catalog token, dead `.face2` rule removed. Pure size alignment (no spec/API ripple). · **Dual-face hero → catalog `/grid` 2-up (2026-06-17):** owner clarified the `/grid` size targets the **dual-face hero (THE FACE + YOUR STATS)** — both now render **2-up at 161×225**, filling the PLAY row like the collection grid (base art 7px + plate 10px), **superseding the 2026-06-16 `/hero` (138×193)** above. The catalog `/grid` specimen was corrected (110px → the real collection 2-up size, 161×225). The **CARDS switcher (M3) + community gallery (M4)** stay **3-up `cell`** (an earlier same-day 2-up pass was reverted per owner). No spec/API ripple (catalog tracks the converged §3.1 collection; page in-pass). · **Type-scale pass merged (2026-06-17):** the on-screen type-scale changes from `game-page-dual-dossier-typescale.html` (a parallel-session fork) merged into the board — every on-screen font snapped to the **4-step scale (F-06): 21 headers/titles · 15 stat values · 11 buttons/chips/subtext/tabs · 9 section-heads/tags/labels**; glyphs snapped, inline `font-size` overrides removed, **print exempt** (card plates + card-back stats scale with the card). The `/grid` dual-face (161×225) + 3-up `cell` switcher/gallery were preserved (the fork predated that work, so re-applied). No spec/API ripple. |
| 4.3 | Card editor — Styler | ✅ converged | `styler/styler-states.html` (carousel; gate ruling 2026-06-12 — **+NAMEPLATE, overlay cut**) | `styler/styler-states.html` | ✅ (skeleton · load-error · offline draft-safe, writes gated) | ✅ (§2.5; 0.13 + catalog v0.4) | ✅ (**0.16 styler sync**: card-bases + surprise · save-private · acquire-batch · `nameplate` enums; OQ-050 resolved) | **OQ-039 ruled** (decision 0018: COSM-01 +nameplate · TITLE→font+ink · Nameplates aisle); OQ-048 (intensity) + OQ-049 (save-private) resolved (0019) |
| 4.3 | Card editor — Canvas | ✅ converged | `canvas/canvas-states.html` (print shop; gate ruling 2026-06-13 — **C wins** + undo/redo relocated, proof size-ladder) | `canvas/canvas-states.html` | ✅ (skeleton · load-error · offline: editing+PROOF from cache, PRESS writes-gated) | ✅ (formalized → **0.18**: §2.5b Canvas + §1.5 **Canvas set** — `CanvasStage`/`AssetShelf`/`ElementTray`/`LayerRack`(slips)/`editbar`/EDIT slip-sheet/PROOF size-ladder/PRESS finish-up/`PrintRitual`) | ✅ (**page-audited, no gap** — all writes exist 0.16/0.18: draft · autosave · publish · save-private · acquire-batch · assets · share-image; cap-30/save-private product-spec-only, 0019) | **OQ-007 + OQ-040 resolved design-side** (diegetic breakout · the first-print ritual); cap 30 (OQ-008) |
| 4.4 | Admin / Moderator console | ⬜ | — | — | — | — | 🔶 | Mod-only (MOD-04) |
| 4.5 | Device editor | ✅ converged | `device-states.html` (in-frame; A won + `SectionSwitch` rail + LOOKS) | `device/device-states.html` | ✅ (D1–D10: editing · shell-switch · theme-preview · sticker place/scale/rotate · OQ-045 on-shell preview · LOOKS · reconcile · Skeleton · Offline writes-gated · LoadError) | ⬜ (formalization owed — spec-owner: §2.x + §1.5 `SectionSwitch`/`SectionCard` · `StickerStage`/`TransformBox`/`PlacedSticker`/`StickerTray` · `SavedLook`/`LooksGrid`) | 🔶 (GET·PATCH `/me/device` {activeShellId·screenThemeId·stickerComposition} + acquire-batch + cosmetics exist; **page-audit owed**: stickerComposition shape + plastic-only zones OQ-062 · saved-looks model OQ-064) | **CONVERGED** — owner picked **A "in-frame live edit"**; the **`SectionSwitch`** won (**bottom-docked · icon in-line · no pip · 4 sections incl. LOOKS**). Owner refinements: **stickers on plastic only** (screen + 5 keys no-go, DEV-03/04/F-04) · saved looks **identified by shell·theme** (no name) · **SAVE CURRENT** only. **OQ-045 closed design-side** (continuous on-shell preview). Drafts a/b/c + `switcher-takes`/`switcher-rail`/`draft-a-looks` kept for history. **Owed (spec-owner): design-spec formalization + API page-audit (OQ-062/063/064).** |
| 4.6 | Compare Hours | ✅ converged | `compare-states.html` (converged · Versus) | `compare-hours/compare-states.html` | ✅ (P1–P5 + L1/L2 lifecycle) | ✅ (§1.5 Compare set + §2.12, 0.26) | ✅ (page-audited 0.25) | **CONVERGED 2026-06-24 → `compare-states.html`** (the owner-picked Versus / head-to-head direction): **P1** has-overlap (totals **face-off** + per-game **card-vs-card matchups** — their game-frame card · the hours, winner orange · your slate+foil card; real `#art-*` symbols; **card-tap → Game page §4.2**) · **P2** no-shared-games · **P3** friends leaderboard · **P4** Skeleton · **P5** privacy-limited (PROF-03) · **L1** Signal-Lost+RETRY · **L2** Offline (read-from-cache, SYS-10). Inherits the social-cluster shell (Teal/Midnight · flat Scanline-Energize · orange StateMark · F-06 · §1.6). **Non-commerce — no gold**; **completion % OUT**; read-only (PROF-03). **Burt clean.** Drafts A + C + `draft-b` retained for history. **Formalized 2026-06-24:** design-spec **0.26** (§1.5 Compare set + §2.12 composition) + api-contract **0.25** (`/me/compare/:friendId` payload + PROF-03 gating); **OQ-074 + OQ-077 resolved**. · **Prior — owner picked B (2026-06-23)** + matchup rework: dropped the paired comparison bars for draft A's **numeric** per-game stats in a new **card-vs-card** layout — **their `GameCard`/cell (96×134) left · stats middle, winner orange · your card right** (each card owner-labelled); cards up-sized to `/cell` for legibility (2026-06-24). Face-off hero + leaderboard + P2–P5 unchanged; **Burt clean**. · **Prior — IN PASS (2026-06-22):** track kicked off — brief + **3 distinct draft directions** (`mockups/compare-hours/`): **A "Side-by-side ledger"** (aligned YOU\|THEM columns — the calm scannable pole) · **B "Versus / head-to-head"** (a totals face-off + per-game matchup bars — the competitive pole) · **C "Diff-first / overlap"** (overlap + per-game deltas sorted by biggest gap — the insight pole). The marquee social payoff (SOC-03, the core return-driver), reached from the friend Profile's *Compare hours* action (SOC-02/PROF-05). Each renders **P1 has-overlap · P2 no-shared-games · P3 friends leaderboard · P4 Skeleton · P5 privacy-limited (PROF-03)**; Offline/LoadError deferred to converge. New components: `CompareHeader` · `CompareTotals` · `ComparePair`/`CompareRow` · `FriendsLeaderboard`/`LeaderRow` (composed from `StatTile`/`ListRow`/`GameCard`/`Avatar`). Inherits the social-cluster shell (Teal/Midnight · flat Scanline-Energize keycaps · orange StateMark · F-06 · §1.8). **Non-commerce — no gold** (Compare creates no card, ECON-01/F-02); **completion % OUT** (hours+games only). **Gap flagged: OQ-074** (`/me/compare/:friendId` payload shape unenumerated). Brief `mockups/compare-hours/compare-hours-brief.md`. |
| 4.7 | Lists / Top-5 editor | ⬜ | — | — | — | — | 🔶 | — |
| 4.8 | Find / Add Friends | ✅ converged | `find-add-friends-states.html` (off Friends draft A) | `find-add-friends/find-add-friends-states.html` | ✅ (P1 hub · P2/P2b search · P3 invite+QR · P4 requests+cooldown · P5 invite-landing SOC-10 · P6 PersonRow state-swatch+sent · L1–L3 lifecycle) | ✅ (§1.5 Friends/Social set + §2.11; 0.23) | ✅ (page-audited 0.23 — `/users/search` OQ-072 · `/invites/:token` OQ-073) | **CONVERGED + FORMALIZED 2026-06-23** (owner-iterated to clean) — ONE coherent board **built off Friends draft A** (same shell/tokens/flat keycaps/F-06/§1.8 grammar; reuses `RequestRow`/`InviteHook`/avatar). New components: **`PersonRow`** — the **relationship-state spine** (`none`→ADD · `outgoing`→CANCEL · `incoming`→ACCEPT/DECLINE-silent · `friends`→profile · `cooldown`→disabled+reason · `blocked`→limited; SOC-07/08/09) · bottom-docked **`SearchField`** (OQ-035) · **`QrCard`** (SOC-07) · **`InviteLanding`/`SenderSummary`** (SOC-10 one-tap prefilled ADD). Non-commerce — no gold, no friend SHARE (OQ-052), no contacts-matching; Block LIST lives in Settings (4.15). **Resolved (page-audit 0.23): OQ-072** (`/users/search` PersonRow shape + relationship enum) · **OQ-073** (`/invites/:token` resolve shape + client-side QR). **Back-nav (owner directive 2026-06-22):** the collection-states friend-view `‹ RETURN TO …` link **under the header** (hub→FRIENDS · invite/requests→ADD FRIENDS; P2/P2b keep CANCEL). **P5 = `FlowTakeover`** (owner pick 2026-06-22 — NavBand untouched; **✕ close top-right** per owner; prefilled ADD resolves into the sender's Profile; ✕ dismiss → home/back). Sent-confirm **Toast normalized** to the canonical Toast (scr-well + hairline; orange left-accent dropped). Brief `mockups/find-add-friends/find-add-friends-brief.md`. **Burt PASS ✅ — 0 deviations** (caret recolored pink→orange so no on-screen pink remains; both earlier deviations resolved by the 2026-06-18 DS sweeps on this board: flat buttons rippled Inset-Recess→**Scanline Energize** (F-03), `.err-card .bang` snapped 34→21px (F-06)). **Formalized 2026-06-23: design-spec §1.5 + §2.11 + api 0.23.** |
| 4.9 | Contributor profile | 🔶 in pass (B picked) | B → `contributor-draft-b-trophy.html` (history: A/C) | — | — | 🔶 | **B PICKED + reworked (2026-06-26):** owner chose **B The Trophy Wall** (by prestige) + 3 changes applied to the draft — **removed achievement badges**, **added contributor percentile tags on the stat tiles** (small gold `PctPill`s, **the Profile PROF-07 pattern**: GAMES · TOP 25%, ADOPTIONS · TOP 10%; on the P1/P3 stat row + P4's public totals), **dropped field-edits**; **(4)** each of the **cards + games sections gets an equal "see more" link** (`VIEW ALL CARDS ›` · `AND 3 MORE GAMES ›`, same `.emore` chip) pulled **outside** its container — cards **keep showcase + grid** (owner: not rows). Lifecycle unchanged. **Burt clean.** *(First take was a dedicated standing bar; owner redirected to the Profile-style tags-on-numbers treatment.)* The badges→percentile-tags change **diverges from CAT-07** (drops "fields you added" + "achievement badges", adds percentile standing) → **OQ-079** (spec-owner: ripple product-spec CAT-07 + define the percentile data model — metric/thresholds/gating/privacy). `GET /users/:id/contributions` still prose-only → API page-audit owed at converge. Drafts A (Record) + C (Impact) retained for history. **Layout variant (owner ask, 2026-06-26):** `contributor-draft-b-profilelike.html` — B's content re-laid in **Profile's grammar** (IdentityBlock + STATS well + PINNED-FAVOURITE hero + TOP-5 mini grids + TertiaryLinks); Burt clean; **owner to compare with `-trophy`** before converge. **Prior (2026-06-25):** 3 distinct models drafted, each P1–P5, Burt PASS ×3. Brief + gate ruling `mockups/contributor-profile/contributor-profile-brief.md`. **Next: converge B → `contributor-states.html` + design-spec formalization + API page-audit (OQ-079).** |
| 4.10 | Achievements | ⬜ | — | — | — | — | 🔶 | OQ-005 egg presentation owed |
| 4.11 | Store item detail / purchase | ✅ (folded into 3.4) | `store-states.html` P2/P2b/P3/P4/P5 | `store/store-states.html` (P2–P5 + P1b/P2b) | ✅ | ✅ (§2.3) | ✅ | Sheet grammar; hold-to-buy |
| 4.12 | Wallet | ✅ (folded into 3.4) | `store-states.html` P8 | `store/store-states.html` (P8) | ✅ (incl. ECON-09 negative) | ✅ (§2.3) | ✅ | — |
| 4.13 | Welcome & Auth | ⬜ | — | — | — | — | 🔶 | Logged-out root (AUTH) |
| 4.14 | Onboarding | ⬜ | — | — | — | — | 🔶 | NOTIF-04 pre-prompt close |
| 4.15 | Settings | ✅ converged | `settings-states.html` (gate ruling 2026-06-14: **C-triage feedback** + **notifications as its own page**; +owner refinements) | `settings/settings-states.html` | ✅ (16-device matrix: lean list ×2 · unverified · notif-page +OS-declined · blocked · delete `ConfirmSheet` · C-triage feedback ×6 · Skeleton · Offline · LoadError) | ✅ (formalized → **0.16**: §2.8 + §1.5 **Settings set** — `Toggle`/`ConfirmSheet`/`LogAttach`/`FeedbackConfirm`/`TriageCard`; **toggle + confirm-dialog gaps closed**; Catalog HTML sync lags v0.4 as for Report/Discover) | ✅ (no gap — feedback `POST /feedback`+`/feedback/:id/logs` 0.22; username/verify/blocks/notification-prefs/account-delete all exist; **notifications-as-page is pure presentation**) | **CONVERGED** — owner picked **C "type-first triage"** + ruled **notifications → its own page** (lean list; each heavier feature opens a focused page). **Owner refinements 2026-06-14 (both former residuals resolved):** feedback SUBMIT = **mid-width `KeycapButton/primary`** (orange) · destructive confirm = **bottom `ConfirmSheet`** (renamed from `ConfirmDialog`; aligns w/ game-page `ConfirmSheet`, OQ-061 — spec-owner to unify). `settings-page.html` + feedback drafts a/b/c kept for history. **OQ-060** (log-bundle shape) honored by reserved UI room. Brief + ruling `plans/2026-06-13-settings-drafts-brief.md` |
| 4.16 | Report (modal) | ✅ converged | `report-sheet/report-states.html` (converged board, A `/drawer`; `report-sheet-drafts.html` A/B/C + `add-game-states.html` P3b kept for history) | `report-sheet/report-states.html` | ✅ (3 targets × full lifecycle matrix) | ✅ (formalized 0.14 — §1.5 `ReportSheet` lifecycle + new `ReportConfirm` + new §2.6 Report; catalog v0.4 static, lifecycle swatches optional) | ✅ (page-audited — `POST /reports` {card\|game\|user + reason + `details`} incl. duplicate + `/me/blocks` SOC-09) | **Promoted to its own board** (9 devices): card · catalog (+duplicate) · user **+ Block** (SOC-09) across launch · picker · required-details (MOD-01, mod-only/outside MOD-07) · dormant-submit · **in-flight · filed-confirm (MOD-02 soft-hide, no thresholds) · post-block confirm · offline writes-gate (SYS-10) · error→Toast** (§1.8). All drawn affordances contract-backed (no OQ logged) |

## How to update (the protocol)
At the end of **every draft pass**: update your screen's row (state · mockup version · states board ·
at converge: fill **Implements from** with the board's path · **re-judge the Design-spec column
against the converged board** and the **API column against what the page actually shows**), refresh
UP NEXT, bump the date, and surface the changed rows to the owner in the wrap-up. The Design-spec and
API columns are the tripwires — a converged board's formalization batch is owed before its Design-spec
reads ✅, and functionality drawn on a page must reach the contract (or the inbox) before its API
reads ✅.

**If the pass resolved any OQs design-side**, also complete [`00-INDEX`](../00-INDEX.md) §4 Step-3 —
relocate them from `## Open` to `## Resolved` in [`open-questions.md`](../open-questions.md) and refresh
**design-spec §2.8** traceability — don't only inline-tag them. (The OQ analogue of the §4 register-update
step added in `a4d93d9`.)
