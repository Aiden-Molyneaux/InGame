# SCREEN STATUS — the page dashboard

> **What this is:** the always-current map of every app surface — design state, queue position,
> mockup version, **the implements-from file** (what the frontend build will compose from), whether
> its states board exists, whether **design-spec has formalized that board**, and whether the
> **api-contract covers the functionality shown on that page**. Updated **at the end of every draft pass** (both design
> sessions); the owner reads UP NEXT first. Row order = the IA in
> [`ui-design-requirements.md`](ui-design-requirements.md) Part 2. Mockup files live in
> **per-page subfolders** under [`mockups/`](mockups/) (file map: its README); the catalog
> stays at the mockups root.

**Last updated:** 2026-07-17 (**M6 BUILD (2026-07-17): the overnight build landed ALL the social client surfaces — Friends (3.3) + Find/Add (4.8) + Compare (4.6) + Contributor (4.9) + Achievements (4.10) + the Settings §0.10 slice (4.15) + Report (4.15/4.16) + the Discover §0.8 slice (3.2) + the Collection TOP curated view (3.1/4.7) + the Profile three-doors/teasers (3.5) + the Game-page friend/M7 artboard + `friendsWhoOwn` (4.2) — receipts [`m6/social-receipt.md`](../planning/m6/social-receipt.md) (server) + [`m6/surfaces-receipt.md`](../planning/m6/surfaces-receipt.md) (client); still owed = the owner first-article FRIENDS walk + the device taste gates + the OQ-151 ruling.** · **Prior — M5 BUILD (2026-07-13): Store (3.4/4.11/4.12) BUILT + Game-page CARDS community gallery → adopt (4.2's M5 sub-states) BUILT + the Styler/Canvas/Device premium layer (CARD-13 reconcile · publish ritual · KeepBar) BUILT — receipts [`m5/economy-receipt.md`](../planning/m5/economy-receipt.md) + [`m5/surfaces-receipt.md`](../planning/m5/surfaces-receipt.md) filed; still owed = the owner device walk + G-D re-fire sitting + P2b/G-J on provisioning.** · **Prior — Profile showcase → Top-3 set-pieces + Top-10 view + nameplate F-06-bound (decision 0047)** — a pre-M1 audit ([`audit/2026-06-29/nameplate-sub9-legibility-audit.md`](audit/2026-06-29/nameplate-sub9-legibility-audit.md)) found game-card nameplates rendering **below the 9px F-06 floor** on small card variants; the owner ruled the nameplate a **UI label** bound by F-06 (reverses the print-exemption — `/mini`+`/thumb` now drop the plate, the title is named beside/below). Applied to the Profile: `TOP 5` → **TOP 3** legible `/grid` set-pieces + a **VIEW TOP 10** two-per-row view; to grow the showcase the owner chose **SOC-04 Top-5 → Top-10** (cap 5→10). **product-spec 0.39 · api 0.39 · design-spec 0.43**; 00-INDEX synced; board `profile-states.html` applied (drafts `profile/profile-top5-legibility-drafts.html`, A/B/C — owner picked **B**). **Owed (tracked):** §4.7 Lists board re-pass 5→10 seats + the cross-board nameplate sweep (audit Group A/B/D) + Catalog F-06. · **Prior — Admin console (4.4) CONVERGED + formalized** → decision 0037: the board `admin-console-states.html` implements the full required **in-app** set (tier-adaptive Admin I/II hub · reports split by target · hide/restore/resolve · `SuspendSheet`/unsuspend MOD-09 · edit-suggestions MOD-06 · catalog dedup/merge/restore MOD-05 + ops · the remediation surfaces · §1.8 lifecycle), with all P3–P5 correctly **absent**. A functionality pass confirmed coverage; this converge **assigns the OQ-081 IDs** — **MOD-11** field remediation · **MOD-12** dossier · **MOD-13** action-notice · **MOD-14** direct catalog edit · **MOD-15** junk removal (takedown = MOD-08, unsuspend = MOD-09 reused) — **page-audits `/admin/*`** (every write → a MOD-10 audit row), and **formalizes design-spec §2.18 + §1.5 Admin-console set**. **product-spec 0.36 · api 0.36 · design-spec 0.38**; 00-INDEX register synced; **OQ-081 → Resolved** (OQ-080 external tool parked). §4.4 row → ✅. · **Prior — Achievements (4.10) converge-pass fix:** a converge review of `achievements-states.html` caught that the **P5 unlock-celebration (ACH-06) `CelebrationMoment` CSS was dropped during the fold** — the `.celebrate`/`.cel-*` rules never made it from `achievements-draft-a-trophycase.html` into the converged board, so the marquee state rendered unstyled. **Ported the block verbatim + adapted it for the converged markup's `.cel-badge-wrap`** (rays now centred on the spotlit badge, not the panel); render-verified in headless Edge (radial+scanline backdrop · gold burst-rays · notched badge · both RewardChips · orange CONTINUE). **Burt PASS** (F-06 21/11 · F-07 clip-path notch not radius · F-02 gold only via the ratified PRESTIGE tier). The CSS fix is presentation-only; **separately, the §4.10 formalization is now COMPLETE** (decision 0038, spec-owner, owner-ratified): product-spec **+ACH-09** (the presentation `tier`; 0.36) · api **0.36** (the three `/achievements*` payloads enumerated — earned-only friend-view · honest-count limited · bounded/no-cursor) · design-spec **0.39** (§1.5 Achievements set + §2.19). **F-02 + F-05 carve-outs** amended (**Catalog v0.11** + design-spec §1.1): gold = also non-acquisitive **PRESTIGE** (resolves the cross-screen badge-gold flag — Friends + Achievements + future Profile teaser) · on-screen **magenta SECRET** (`brand.secret`/`scr.secret` `#e85ad0`). **OQ-082 + OQ-005 → Resolved.** §4.10 row → Design-spec ✅ · API ✅. · **Prior 2026-06-28 — Profile RoleTag / staff badge re-formalized (§4.4 ripple closure · design-spec 0.33):** the §4.4 admin work (decisions 0033→0034) rippled a staff badge onto the Profile — **already drawn** on the converged `profile-states.html` (`.roletag`) but missing from design-spec. Closed: §1.5 now catalogs **`RoleTag`** (flat accent-outlined chip, F-06 9px caps + 11px glyph, by `IdentityBlock`; **NOT gold**, F-02) · §2.2 folds it into Compose / Friend-view / state-matrix (self-view = tier ADMIN I–IV · friend/public = generic STAFF/ADMIN · absent for `role = user`; payload-driven `/me` `role`+`adminTier` · `/users/:id` `staff`); PROF-09 / SYS-08. Debt closure, **no OQ / no new spec ID**. Row 3.5 Design-spec cell → 0.33. **The other two §4.4 ripple debts stay OPEN:** §4.15 Settings gated Admin-console row → §2.8 · §4.12 Wallet `admin_adjustment` ledger → §2.3. · **Prior 2026-06-28 — Lists / Top-5 editor (4.7) — design track kicked off (drafts → gate):** 2 distinct interaction models drafted under `mockups/lists/` — **A "Frame-first / ranked slots"** (the Top-5 structure leads) · **B "Collection-first / multi-select"** (the collection leads, ranking a second beat) — each rendering the full §4.7 + §1.8 matrix; both **Burt PASS**. Non-commerce → no gold; #1 rank marker drawn orange (owner-ratify vs the gold `RankChip/first` specimen). **2 gaps flagged: OQ-083** (dedicated SOC-04 editor screen vs inline Profile edit-mode — owner ruling needed; row stays 🔶 until ruled) · **OQ-084** (`/me/lists` payload prose-only). Brief + README in the track folder. **Owner gate pending — do NOT converge.** · **Prior 2026-06-27 — Admin console (4.4) IA + ROLE MODEL SETTLED (spec-owner)** → decisions 0033 + **0034**: the §4.4 role-lifecycle gap is closed ahead of its design pass, and a critical functionality pass reshaped the role model. **0033:** grant/revoke **out-of-band** (no in-app role mgmt, no endpoint) · console **reached via Settings** · reports queue **split by target type** w/ a bottom **SectionSwitch /rail** · suspension from a user report (MOD-09) · actions logged · utility styling. **0034 (supersedes 0033's enum/badge):** **moderator dropped** → **`role ∈ user\|admin` + `adminTier 1..4`** (nested: **I Content · II Catalog · III Support · IV Platform**) bundling permission groups **P1 Content · P2 Catalog** (in-app console) · **P3 Economy/Support · P4 Config/Authoring · P5 Governance** (external tool); **PROF-09** → **generic public STAFF badge, tier self-view only**; the console = **tiers I/II (P1+P2)**, with the in-app remediation additions (field reset · user dossier · action-notice · direct catalog edit · junk removal · proactive takedown) reserved → **OQ-081**; the **economy/config/governance external operator tool** (incl. role grant/revoke + the audit ledger) **parked → OQ-080**. **Formalized:** product-spec **0.30** · api **0.30** · design-req **0.20** (§3.5/§4.4/§4.15). 4.4 stays ⬜ design but is now **spec-ready**; §3.5 + §4.15 carry a small design-ripple debt (staff badge + gated Admin-console row) due at the 4.4 pass. · **Prior — Contributor profile (4.9) FORMALIZED (spec-owner)** → decision 0032: the converged Trophy-Wall/Profile-like board reaches the owning docs — **product-spec CAT-07 revised + new CAT-10** (0.28) · **design-spec §1.5 Contributor-profile set + §2.16** (0.31) · **api 0.28** (`GET /users/:id/contributions` friend/full vs non-friend/limited + paginated `…/cards?cursor=`·`…/games?cursor=` VIEW ALL). **CAT-10** standing = the Profile `PctPill` percentiles computed vs the **contributor cohort** (SYS-04 threshold + PROF-03 privacy gated) — replaces the dropped achievement badges; **CAT-06 field-edits dropped** from the surface; one net-new component **`SectionEmpty`** (per-section nudge). Two completeness edges ruled: standing rides the honest aggregates on the limited view; own MOD-02 soft-hidden card inherits MOD-02. **OQ-079 → Resolved.** · **Prior 2026-06-27 — Device editor (4.5) FORMALIZED (spec-owner)** → decision 0030: the converged in-frame board reaches the owning docs — **design-spec §1.5 Device-editor set + §2.15** (0.30) · **product-spec DEV-05** + DEV-01/03 clarifications (0.27) · **api 0.27** (`stickerComposition` shape + `/me/device/looks` CRUD). Four rulings: OQ-062 (sticker shape + plastic-only zones, server-validated) · **OQ-063** (`SectionSwitch` unified app-wide → one grammar + `/pair`·`/chips`·`/rail` variants; `SegmentedKeycap`/`SectionChips` kept as aliases, converged Discover/Styler untouched) · OQ-064 (saved looks = `device_looks`, no name, delete + cap) · OQ-065 (premium live try-on → atomic KEEP-batch, owned-only device). **OQ-045/062/063/064/065/076 → Resolved.** Follow-up: the LOOKS board owes a per-tile delete affordance. · **Prior 2026-06-26 — Design-system — F-05 on-screen position pips squared** → Catalog v0.10 · design-spec 0.28; the round-orange carousel swipe pips go square / pixel-stepped / flat, shell LEDs stay round. · **Prior 2026-06-25 — Contributor profile (§4.9 · CAT-07) — IN PASS:** the next un-started screen by road-to-market build-order (the only M5 surface). **3 distinct organizing models drafted** off Profile's `MY CONTRIBUTIONS` gateway → `mockups/contributor-profile/` — **A The Record** (by type: totals → games-added+reach · cards-designed+adoptions · field-edits · badges) · **B The Trophy Wall** (by prestige: badge case + most-adopted card hero, lists demoted) · **C The Impact Dashboard** (by reach: footprint heroes + items ranked by impact). Each renders **P1–P5** (self · empty contributor-hook · friend-view · privacy-limited PROF-03 `lock-well` · Skeleton); Offline/LoadError deferred to converge. **Burt PASS ×3** (0 blocker/major) — F-02 held (gold only on the P2 acquisitive hooks + Store key + C's `RankChip`/first value-marker), **badges drawn non-gold** (owner-ratify vs the Friends `.achv` gold glyph). Backing **`GET /users/:id/contributions` is prose-only** → API page-audit + OQ owed at converge. Brief + README under the track folder. **Next: owner gate → converge `contributor-states.html`.** · **Prior (2026-06-24) — Collection peek-flip — COL-12 added (spec-owner):** decision 0026 reverses OQ-059/0025 — the Collection gains an in-place **card peek-flip** (shelf+grid; **tap → CARD-01 stats back · tap-back returns · VIEW GAME → 4.2 · long-press shortcut**; friend-view privacy-gated). **product-spec 0.24 · api 0.24 · design-spec 0.25** (§2.1 + COL-12); **no on-face indicator** (owner) — discoverability via a first-run coachmark. **Board:** the COL-12 stage (grid · shelf · friend) was folded into the canonical **`collection-states.html`** (scratch variant retired, 2026-06-24); on-back VIEW GAME reworked to the real `KeycapButton/secondary`. · **Prior (same day): Catalog v0.5 — the flat in-screen switch family cataloged (owner-requested):** the [`InGame Design System Catalog`](mockups/InGame%20Design%20System%20Catalog.dc.html) Buttons card gains **SEGMENTBUTTON** — the flat **SegmentedKeycap/pair** (Discover UP&nbsp;NEXT↔DISCOVER toggle) + **SectionSwitch** (game-page dossier PLAY·CARDS·ABOUT + the device-editor section rail), **active = accent border + accent icon** (F-09 — a border, not a keycap; **no pip**), with the `+CountTag` badge; extracted verbatim from [`discover-states.html`](mockups/discover/discover-states.html) `.seg-pair` + the converged [`game-page-states.html`](mockups/game-page/game-page-states.html) `.tabsw`. **Closes the catalog's "segmented control (Discover)" gap line.** design-spec §1.5 already names `SegmentedKeycap` (0.15) — but its prose voices active as *"pressed + StateMark"*; the **flat** treatment the converged boards render is the same direction as the **flat-button ripple** (owner picked Inset Recess 2026-06-17) — so the catalog now shows the converged flat form, and the §1.5 prose reconcile + app-wide `SectionSwitch` unification stay tracked at **OQ-063** / the flat-button ripple. No converged-board state/API change. · **Prior (same day):** **Game page (4.2) CONVERGED → `game-page-states.html`:** candidate B (the dossier merge) folded into the canonical states board — the **8 core artboards** (M1 PLAY dual-face · M2 EDIT form · M3 CARDS switcher · M4 gallery+adopt · M5 ABOUT · M6 neutral · M7 friend+compare · M8 upcoming/be-first) **+ the deferred lifecycle states** (L1 loading `Skeleton` · L2 `LoadError` · L3 `Offline` writes-gated + error→`Toast`, SYS-10 · L4 soft-hidden/reported, **MOD-02/08/09**), the lifecycle grammar reused verbatim from the sibling boards; the owner-picked **flat Inset-Recess keycaps** carried; the **`.btn.sec`→`.btn.cream`** KeycapButton/SectionHeader split applied (Bugfix #4). Reconciliations flagged to the inbox: **OQ-069** (CARD-01 card-back adoption-count) · **OQ-070** (WISHLIST in the owned editor); OQ-058/061 kept open. **Design-spec formalization + API page-audit now owed (spec-owner).** The `dual-dossier`(+`-flat-buttons`/`-typescale`) · `trophy-*` · `draft-*` · `mix-takes` lineage retired, kept for history. · **Prior: Design-system — flat / 2D button exploration (owner-requested, drafted on the Collection Shelf):** [`collection-flat-buttons-drafts.html`](mockups/collection/collection-flat-buttons-drafts.html) renders the Shelf main page **4-up — the current 3D original (baseline) + 3 columns with the in-screen keycaps flattened** — `shadow.key` (the `0 3px 0` raised edge / clipped drop-shadow) **and** the press-down translate removed — each with a distinct flat pressed-affordance: **A** tonal inversion (fill↔ink) · **B** inset recess (inner shadow, no motion) · **C** accent ring (inset, hugs the C5 notch). Scope = screen `KeycapButton`/`ToolKeycap` only (LOG HOURS · sort/filter/view · ADD); the shell `NavBand` keys (`0 4px 0`) stay physical. **Owner PICKED B · Inset Recess (2026-06-17)** — first applied to a new game-page variant ([`game-page-dual-dossier-flat-buttons.html`](mockups/game-page/game-page-dual-dossier-flat-buttons.html)), verified flat across all 8 artboards (idle + pressed-recess, no over-reach; CSS-coverage audit clean — adversarial workflow). **Round 2 (owner asked for 3 more options):** [`collection-flat-buttons-drafts-2.html`](mockups/collection/collection-flat-buttons-drafts-2.html) drafts **D notch-ignite (lit C5 step-corners) · E snap-pip (leading pixel marker) · F scanline-energize (CRT texture)** beside B — generated/selected by an ideation panel, all verified flat / no-motion / clip-safe on the notched ADD; final affordance pick under review. **Still owed (once the affordance is locked): design-spec F-03 `shadow.key` ripple + apply across all remaining live boards (cf. the marker audit). No converged-board state/API change yet.** · **Prior:** **Game-page track (4.2) — dual-face hero cards sized to the catalog `/grid` (collection 2-up)** ([`game-page-dual-dossier.html`](mockups/game-page/game-page-dual-dossier.html)): owner clarified the `/grid` size targets the **dual-face hero (THE FACE + YOUR STATS)**, not the switcher/gallery — so the two hero cards now render **2-up at 161×225**, filling the PLAY row like the collection grid (base art 7px + plate 10px), **superseding their 2026-06-16 `/hero` (138×193) sizing**; the catalog `/grid` specimen was corrected from an idealized 110px to the **real collection 2-up size (161×225)**. An earlier same-day pass had taken the **CARDS switcher (M3) + community gallery (M4)** 2-up — **reverted** per owner; they stay **3-up `cell`**. *(Catalog correction reflects the converged §3.1 collection — no spec/API ripple; game page still in-pass.)* **· Prior (2026-06-16): dual-face hero cards sized to the catalog `/hero`** ([`game-page-dual-dossier.html`](mockups/game-page/game-page-dual-dossier.html)): the **front (face) + back (stats)** dual-face cards were a local `face2` (146×204) → now the **catalog GameCard `/hero` (138×193, F-01)**; the stale local `.hero` (168×235) was corrected to the catalog token and the now-dead `.face2` rule removed (pure size alignment — no spec/API ripple; brings the mockup into compliance with the already-formalized token). **· Prior (2026-06-15): owner PICKED the dossier merge (B) + refined** ([`game-page-dual-dossier.html`](mockups/game-page/game-page-dual-dossier.html)): owner *"the dossier take looks better"* → **B chosen over `trophy-dualface` (A)**; 3 refinements — **(1)** the bottom **`SectionSwitch` is one-line** (icon inline w/ label; **pink pip dropped** → matches the device-editor rail + the marker audit; active = orange border + orange icon) · **(2)** **CARDS tap-select** (thin **orange ring + gap** on the selected card) · **(3)** that card's **inline options** (its `EquipReadout` + **SET AS MAIN · EDIT IN STYLER · DELETE**). **Next: converge B → `game-page-states.html`.** · **Prior (same day): the merge drafted** — the dossier's section switcher (PLAY·CARDS·ABOUT) moves to the **bottom of the screen** in the `device-switcher` **SectionSwitch** style; the **dual-face hero is kept**; **stats edited via a dossier form** (not on the card back); all functionality across **8 artboards** (M1 PLAY · M2 form-edit · M3 switcher · M4 gallery+adopt · M5 ABOUT · M6 neutral · M7 friend dual-face+compare · M8 upcoming/be-first); buttons corrected (orange `/primary`, gold = card-creating). Sits beside `trophy-dualface` as candidate **B** for the owner's pick. · **Prior — On-screen state-marker audit (design-system)** — the pink `ChipPip`/`PipLight` leaks onto flat, square screen chrome in 3 roles (tool keycaps · F-09 selection · segments); **3 replacement drafts** ([`mockups/design-system/onscreen-marker-drafts.html`](mockups/design-system/onscreen-marker-drafts.html)) swap it for an `scr.accent` **orange · flat · square** marker (F-05/F-07/F-09-as-written); shell LEDs stay pink. Audit [`audit/2026-06-14/onscreen-pip-marker-audit.md`](audit/2026-06-14/onscreen-pip-marker-audit.md). Owner picks → spec-owner ripple. · **Prior (2026-06-13): Game-page track (4.2) — A WON the gate, rev 4 applied**: owner ruled "moving forward with A" ("card-led scroll"); B/C retired. Three owner changes now in `game-page-draft-a-cardled` (rev 4): a dedicated **owned-landing** panel (P2 — the just-opened resting state; the stats panel becomes P2b) · the **⋮ overflow is now a pull-up drawer** · **COMPARE now opens a drawer** (P6) — every heavy surface shares the one bottom-sheet grammar. Still carries rev-3 (catalog buttons · rating PENDING OQ-058 · card-back no "made in InGame"). Next: any further A tweaks → converge → `game-page-states.html` (+ deferred lifecycle states). Ruling appended to the brief. · **Collection track — §3.1 friend-view browse parity (COL-11 / decision 0021)**: `collection-states.html` gains the friend-view **read-only browse bar** on both artboards + 2 new artboards (**sorted-by-hours list** · **read-only sort/filter drawer**, no Arrange); board ✅, API ✅ (widened 0.20), **Design-spec ⬜** (§2.1 friend-tools formalization owed). · **spec-owner — §4.2 Game page prepped (decision 0020)**: friend card-tap → the Game page **friend-view state** · their card + **equipped readout** (CARD-22) + per-game ctx · **opt-in compare** · **atomic adopt** (SOC-11); spec 0.20 / api 0.19 / design-req 0.15. · **Discover track — §3.2 Discover CONVERGED** (`discover-states.html`): owner picked **A "Two rooms"** + edits (bottom/smaller `SegmentedKeycap` · standard header · real games · Browse-By cut → OQ-057 · simplified Up Next + Discover); board ✅, **Design-spec ✅** (formalized → 0.15: §1.5 Discover set — `SegmentedKeycap`/`QueueRow`/`ReleaseRow`/`RecRow`/`AdoptCount`/`NotifyToggle` + §2.7), **API ✅** (page-audited → 0.21: queue shape · trending-cards shape · upcoming notify-me · friend-recs feed · DISC-02 parked; OQ-053/054/055/057 resolved). Drafts a/b/c kept for history. Brief `plans/2026-06-13-discover-drafts-brief.md`. Prior: report track — **§4.16 Report converged + design-spec formalized** (`report-states.html`, A `/drawer`; 3 targets × full matrix; MOD-01/02 · SOC-09 · SYS-10): board ✅, API ✅ (page-audited), **Design-spec ✅ (formalized → 0.14: §1.5 lifecycle + `ReportConfirm` + §2.6)**. Prior: canvas track — Canvas converged (print shop · `canvas-states.html`), the 0014 editor arc complete, OQ-007 + OQ-040 resolved; spec-owner triage 0019.)

## Up next (the queue)
- **✅ DONE (design, 2026-06-30): TOP view-mode MERGED into the Collection board (decision 0050).** The former `lists-states.html` Top-view is now **drawn in `collection-states.html`** as the **"View mode · TOP" stage** — 3 artboards: **self read** (hero #1 + 3-up `/cell` shelf + ARRANGE) · **self ARRANGE + CardPicker** (drag-rerank + add/remove, cap-10) · **friend read-only** ("COLLECTION — RIKO", ‹ RETURN TO PROFILE). **Nav in** = the Collection `view` keycap (SHELF · GRID · LIST · **TOP** — chip added to both self + friend sheets) or the Profile's VIEW TOP 10 / a Top-3 card; **nav back** = the same view keycap (it's a view-mode). Built with collision-free `tv-*` classes; render-verified. **`lists-states.html` RETIRED** (history banner). design-spec **0.49** (§2.17 reframed) · 00-INDEX synced. §3.1 implements-from now sole-sources `collection-states.html`.
- **✅ DONE (design + spec-graph, 2026-06-30): Collection & TOP navigation model nailed down — decision 0050 (completes 0049 · closes OQ-083).** **The model:** a person's games live in **one place, their Collection**; **TOP** is the curated Top-10 as the **4th `view` mode** (COL-13); the **Profile only teases (Top-3) + launches in**. **Three doors (PROF-05):** **VIEW TOP 10** → Collection TOP view · **Top-3 card tap** → Collection TOP view **focused on that game** · **friend-only VIEW COLLECTION** → their full Collection (self uses the NavBand). **Edit your own Top-10** = in the Collection TOP view (ARRANGE drag-rerank + `CardPicker`, cap-10) — **no standalone editor**. **A friend's TOP = read-only.** **Applied:** product-spec **0.42** (+COL-13 · COL-07 · PROF-05 · COL-10/11) · design-spec **0.48** (§2.1/§2.2/§2.17 reconciled — the stale 0047 "standalone Top-10 view / PROFILE-active / OQ-083-dedicated" refs corrected) · 00-INDEX synced · **boards:** `profile-states.html` (friend **VIEW COLLECTION** action + VIEW TOP 10/card-tap captions), `lists-states.html` (= the Collection TOP-view board; **+S7 friend read-only TOP** artboard; stale "(5)" labels fixed). No api change (`/me/lists` cap-10 covers it). *(Hygiene flag: design-spec discrete changelog rows 0.45–0.47 (0048/0049) were never added by the parallel session — the version blurb carries them.)*
- **✅ DONE (design, 2026-06-29): cross-board nameplate sweep + Catalog F-06 — decision 0047 fully applied.** The F-06 ruling (nameplate is a UI label, ≥9px or drop it) applied to **every remaining live board**: the redundant sub-9px plates **dropped** where a legible title already sits beside — **Discover · Collection (friend strips) · Friends (feed peek) · Admin-console + Report-sheet (target previews) · Add-game (type-ahead) · Game-page (card galleries) · Onboarding (status rows)** + the cosmetic-sample plates on **Store · Styler**; the **Contributor** CARDS-DESIGNED `/cell` made legible (10px, its sole label) + GAMES-ADDED `/thumb` dropped; **Canvas** PROOF size-ladder reconciled (legible at /grid-96, plate dropped at /mini+/thumb, caption reworded). **Catalog → v0.12** (F-06 card + plate-floor specimen amended). All board-compliance with the already-formalized design-spec F-06 (0.43) — no new spec bump. **Decision 0047 is now fully discharged.**
- **✅ DONE (design + spec-graph, 2026-06-29): Lists / Top-10 editor re-pass (decision 0047).** The owed §4.7 consequence of growing SOC-04 to Top-10: `lists-states.html` re-designed from 5 fixed seats to **10** — a `#1 /hero` headliner + a **3-up `/cell` shelf of seats 2–10**, the `/cell` nameplate snapped to the legible **10px floor** (was 7.5px). S1 empty (headliner + 9 ghosts) · S2 editing + CardPicker · **S3 full (10)** · S4–S6 lifecycle. **design-spec §2.17 0.44 · api 0.39 (`/me/lists` cap 10).** Render-verified; **Burt PASS** (F-01 catalog sizes; F-06 plate reconciled; #1 marker stays orange). **Clears the §4.7 re-pass debt.** Remaining from 0047: the cross-board nameplate sweep (audit Group A/B/D) + Catalog F-06.
- **✅ DONE (design + spec-graph, 2026-06-29): Profile Top-3 / Top-10 + nameplate F-06-bound (decision 0047).** The Profile showcase `TOP 5` → **TOP 3** legible `GameCard/grid` set-pieces (10px plate held) + **VIEW TOP 10** → a ranked **two-per-row** view of all 10; **SOC-04 grew Top-5 → Top-10** (cap 5→10). The card nameplate is now a **UI label** bound by the F-06 9px floor (`/mini`+`/thumb` drop the plate). **Board `profile/profile-states.html` applied** across every artboard + a new Top-10 view; render-verified (legible plates, Store key gold). **product-spec 0.39 · api 0.39 (`top10`, `/me/lists` cap 10) · design-spec 0.43 (F-06 reworded · §2.2 recomposed · §2.17 cap 10).** Drafts `profile/profile-top5-legibility-drafts.html` (owner picked B). **Owed:** ① §4.7 Lists board re-pass 5→10 seats (🔶 below) · ② the cross-board nameplate sweep (audit Group A: Contributor CARDS-DESIGNED · Lists seats · Canvas demo; Group B/D: drop redundant sub-9px plates) · ③ Catalog F-06 card.
- **✅ CONVERGED (design — Lists / Top-5 editor 4.7, 2026-06-28) → `lists/lists-states.html` (owner picked A · Frame-first; OQ-083 still unruled).** §4.7 the Top-5 curator (**SOC-04**), reached from the Profile (PROF-05); serves **M6 Social**. **Two genuinely distinct interaction models** in `mockups/lists/` — **A "Frame-first / ranked slots"** ([`lists-draft-a-slots.html`](mockups/lists/lists-draft-a-slots.html): five fixed ranked showcase seats ARE the frame → tap an empty seat → a CardPicker sheet → drop in; drag to re-order; the cap is **structural**) · **B "Collection-first / multi-select"** ([`lists-draft-b-multiselect.html`](mockups/lists/lists-draft-b-multiselect.html): the full collection grid is the frame → multi-select ≤5 with a running **n/5** CapMeter + 5-cap refusal → a distinct **PICK → ARRANGE** draggable rank rail). Each renders the full **§4.7 + §1.8** matrix (S1 empty · S2 editing · S3 full(5) · S4 Skeleton · S5 LoadError · S6 Offline writes-gated, SYS-10). Inherits the collection+profile shell verbatim (Teal/Midnight · flat Scanline-Energize · orange StateMark · F-06 · GameCard `/cell` 96×134 + `/thumb`; the COL-07 ARRANGE gesture reused). **Non-commerce — no gold;** the **#1 rank marker drawn orange not gold** (deliberate divergence from the gold `RankChip/first` specimen — owner-ratify). **Both Burt PASS ✅** (F-06 tag/badge fixes). **Gaps flagged (spec untouched): OQ-083** (dedicated SOC-04 editor screen **vs** inline Profile edit-mode panel — owner ruling; the api-contract calls re-rank "the Profile edit-mode ARRANGE gesture" yet §4.7 specs a standalone editor) · **OQ-084** (`/me/lists` payload prose-only). Brief + README `mockups/lists/`. **Next: owner gate → pick A/B + rule OQ-083 → converge `lists-states.html`.**
- **✅ CONVERGED (design — Contributor profile 4.9, 2026-06-26) → `contributor-states.html`.** §4.9 "My Contributions" (CAT-07), the M5 surface. Owner picked **B** then iterated to the **Profile-like layout**, now converged to the canonical board: `IdentityBlock` → **boxless STATS tiles** (gold PROF-07 `PctPill`s) → **SIGNATURE CARD** PINNED-FAVOURITE hero (+ VIEW CARD) → **CARDS DESIGNED** as a legible `/cell` grid + **GAMES ADDED** as title rows, each with `VIEW ALL ›`; **full matrix** P1 self · P2 empty · P3 friend-view · P4 privacy-limited + lifecycle **L1 Skeleton · L2 Signal-Lost+RETRY · L3 Offline** (SYS-10). **Burt PASS** (0 blocker/major; dead `mini`/`sk-mini` CSS pruned). **Owed (spec-owner): design-spec formalization + API page-audit → OQ-079** (ripple CAT-07 — drop "fields you added" + achievement badges, add the **percentile standing**: metric/thresholds/gating/privacy; enumerate `GET /users/:id/contributions`). History: drafts A · B(`-trophy`) · C + the `-profilelike` source. Brief + gate ruling `mockups/contributor-profile/contributor-profile-brief.md`.
- **✅ DONE (design-system, 2026-06-26): F-05 on-screen position pips squared (owner directive).** The round-orange **CardFan / carousel swipe pips** — the last round-orange on-screen holdout after the 2026-06-15 marker pass — go **square · pixel-stepped · flat** (the `StateMark` notch geometry, 6px, no glow); **shell LEDs** (NavBand active · power) stay round lit pips. **Formalized:** design-spec **0.28** (F-05 reworded → shell LEDs vs on-screen pixels) + **Catalog v0.10** (F-05 card + its 3 carousel specimens squared). **Rippled** the `.fdot`/`.sdots` rule across **add-game · onboarding · styler · styler-flat-chips** (`discover-states-fan` retired → skipped); render-verified square on onboarding O2. **Not swept:** on-screen status/notify dots (`er-dot`/`kb-dot`) — a separate role, left round lit pending owner.
- **✅ DONE (design — Collection, 2026-06-24): COL-12 peek-flip folded into the canonical board.** The peek-flip (product-spec **COL-12** · api **0.24** · design-spec **§2.1 / 0.25**, decision 0026 reversing OQ-059/0025) now lives in the canonical **[`collection-states.html`](mockups/collection/collection-states.html)** as the **States · card peek-flip (COL-12)** stage — 3 artboards (grid · shelf · friend, privacy-gated); the scratch variant was retired. On-back **VIEW GAME** reworked from a bespoke chip to the real `KeycapButton/secondary` (cream · flat · Scanline · `mini`+`block`). **Burt PASS ✅ (0 blocker/major).** No on-face indicator (owner); discoverability = a first-run coachmark. Owner review of the flip UI still welcome.
- **▶ AUDITED ✅ (design-system, 2026-06-23): Burt 13-board conformance audit — all PASS, 0 blocker / 0 major.** Every `*-states` board verified vs **F-01..F-09 + §1.8 lifecycle + standalone hygiene**; each finding adversarially re-checked (1 false positive killed). **F-02 gold clarified → _acquisitive_** (card-creating · PIXELS economy · primary add-to-collection; orange = prominent non-acquisitive — RETRY / ADD&nbsp;FRIEND / SHARE) — formalized **design-spec 0.24 + Catalog v0.8 + the Burt F-02 check**. **Minor fixes applied:** F-01 inline/skeleton off-ladder cards (styler `.sk-card`→138×193 · game-page CardDetail 120→138, compare 105→**96** owner-pick, `.hidden-cell` 78→64) · F-05 canvas `.guide-v` pink→orange · F-02 canvas SHARE gold→orange · F-07 discover `.dropline` squared · F-03 find-add `.head-act` press→Scanline + stale captions. **Left for owner:** Discover ADD-FROM-COLLECTION gold (blessed as primary add-to-collection) · `.half-panel` bezel-fragment radius (add-game/styler/canvas) · store `.rtile` gold-border selection · profile avatar inline hex (nit) · stale "pink-pip" caption prose (OQ-067 doc tail).
- **▶ RIPPLED ✅ (design-system, 2026-06-18): flat / 2D buttons = Scanline Energize (LOCKED).** The screen Keycap system is now **flat** (no `shadow.key` 3D); pressed/active = **Scanline Energize** — CRT scanlines over a hairline-darkened fill, **no motion, no pip**. **Formalized:** design-spec **0.20** (F-03 rescoped — keycap travel = shell nav only · `shadow.key` deprecated for screen · `motion.keyPress` → shell-only + new `motion.scanlineEnergize` · `KeycapButton`/`ToolKeycap`/`CountKeycap` defs flattened) + **Catalog v0.6** specimens (flat + scanline; render-verified). **Rippled across all 14 boards** — collection · h2-underlay · discover · profile · store · add-game · styler + styler-flat-chips · canvas · device · settings · report · game-page-states + game-page-dual-dossier — each a labelled per-board override block, **render-verified** (idle-flat + scanline-press, no motion). Kept: physical `NavBand` keys 3D · segmented/tab **accent-border** active · clip-path corner-notches · CountKeycaps (`.ccount`) flatten-only. Owner-pick path: A inversion → B recess → **F Scanline Energize (locked, no stepped-corner fill)**. **Superseded → history:** the exploration drafts `collection-flat-buttons-drafts.html` (round-1 A/B/C) · `collection-flat-buttons-drafts-2.html` (round-2 B/D/E/F) · `collection-button-style-spec.html` (stepped-corner) · `game-page-dual-dossier-flat-buttons.html` (the earlier B-recess variant). **Remaining:** Burt DS-audit spot-checks; segmented-control app-wide consolidation stays **OQ-063**.
- **✅ DONE (design-system): On-screen state marker** — owner chose **Draft A** = a solid **orange pixel-square** (`scr.accent`, flat, **one pixel notched off each corner**, right-angle pixel-art) → **[`mockups/design-system/onscreen-marker-drafts.html`](mockups/design-system/onscreen-marker-drafts.html)** (B tick · C lit edge kept for history). **Applied across all live boards 2026-06-15:** the pink `ChipPip`/`PipLight` → the orange pixel-square in **collection · h2-underlay · profile · report · add-game · styler · discover · canvas** (canvas's `.spip`/`.base-sw`/`.csw`/`.cat-pip` were extra finds beyond the audit) **+ the Catalog's 3 on-screen ChipPip demos**; selection borders were already orange; **shell NavBand + DeviceShell LEDs stay pink** (F-05). Verified — no on-screen pink-pip glow remains on any live board; report-states spot-checked in render. Audit [`audit/2026-06-14/onscreen-pip-marker-audit.md`](audit/2026-06-14/onscreen-pip-marker-audit.md). **Done (design-spec 0.18 · OQ-067 resolved):** F-09 reworded + the `ChipPip`/`PipLight` → `StateMark` rename across §1.5 + the Catalog labels. *(Device + game-page already pip-free.)*
- **✅ FORMALIZED 2026-06-22 (spec-owner) — Game page (4.2).** Candidate B → **[`game-page-states.html`](mockups/game-page/game-page-states.html)**: the dossier merge folded into the canonical states board — **8 core artboards** (M1 PLAY dual-face + dossier readout · M2 EDIT form · M3 CARDS switcher tap-select + inline options, OQ-056 · M4 community gallery → atomic-adopt `CardDetail` + confirm · M5 ABOUT · M6 neutral PLAY-locked · M7 friend dual-face + privacy-gate + compare · M8 upcoming/be-first) **+ the deferred lifecycle states** (L1 loading `Skeleton` · L2 `LoadError` "Signal Lost"+RETRY · L3 `Offline` writes-gated + error→`Toast`, SYS-10 · L4 soft-hidden/reported, **MOD-02/08/09** — grammar reused verbatim from collection/discover/device/settings). The **flat Scanline-Energize keycaps** (F-03, formalized 0.20) carried; the **`.btn.sec`→`.btn.cream`** secondary-keycap modifier split off so the KeycapButton no longer reuses the SectionHeader `.sec` (Bugfix #4). **Design-spec ✅ formalized 0.21 (§2.4b + §1.5 Game-page set; decisions 0020/0021/0024/0025) · API ✅ page-audited, no gap.** OQ-058 (rating) · OQ-061 (delete/`ConfirmSheet`) remain open; OQ-056/OQ-063 cross-referenced. The `dual-dossier`(+`-flat-buttons`/`-typescale`) · `trophy-*` · `draft-*` · `mix-takes` lineage retired, kept for history. Brief: `plans/2026-06-13-game-page-drafts-brief.md`.
- **✅ FORMALIZED 2026-06-27 (spec-owner) — Device editor (4.5)** (`device-states.html`; decision 0030). The converged in-frame board (owner picked **A "in-frame live edit"**; the **`SectionSwitch`** rail won — bottom-docked · icon in-line · no pip · **4 sections** incl. **LOOKS**; stickers on **plastic only**; looks **identified by shell·theme**; **SAVE CURRENT** only) is now in the owning docs: **design-spec §1.5 Device-editor set + §2.15** (0.30) · **product-spec DEV-05** + DEV-01/03 clarifications (0.27) · **api 0.27** (`stickerComposition` shape + `/me/device/looks` CRUD). **Four rulings (decision 0030):** OQ-062 (sticker shape + plastic-only zones, server-validated) · **OQ-063** (`SectionSwitch` unified app-wide → one grammar + `/pair`·/`chips`·/`rail` variants; `SegmentedKeycap`/`SectionChips` kept as aliases, converged boards untouched) · OQ-064 (saved looks = `device_looks` snapshots, no name, delete + cap) · OQ-065 (premium live try-on → atomic KEEP-batch, owned-only device). **OQ-045/062/063/064/065/076 → Resolved.** Follow-up: the LOOKS board owes a per-tile delete affordance. Brief `plans/2026-06-14-device-editor-drafts-brief.md`.
0. **🎉 The 0014 editor arc is COMPLETE** — Add Game → Styler → Canvas all converged. OQ-007 (breakout) + OQ-040 (first-print ritual) resolved design-side in the Canvas board.
1. *Formalization batches owed (spec-owner):* the **Styler converge set** (design-spec §2.5 already landed 0.13 — the catalog component shells) + the **Canvas converge set** (design-spec §2.x + the editor's open-composition components: `CanvasStage` · `AssetShelf` · `ElementTray` · `LayerRack`/slips · `PrintRitual`); + the **Canvas API page-audit** (writes already in 0.16/0.18 — confirm no gap). *(Report converge set — **done 0.14**: §1.5 `ReportSheet` lifecycle + `ReportConfirm` + §2.6.)*
2. *Coverage-driven:* **Settings** ✅ **CONVERGED** (`settings-states.html` — gate ruling 2026-06-14: **C "type-first triage"** feedback + **notifications as its own page**; the lean-list + row-opens-a-page grammar; new components **`Toggle`/`ConfirmSheet`/`LogAttach`/`FeedbackConfirm`/`TriageCard`**; drafts kept for history). **Owner refinements 2026-06-14:** feedback SUBMIT → mid-width `KeycapButton/primary` (orange) · destructive confirm → bottom `ConfirmSheet` (was `ConfirmDialog`). **Design-spec ✅** (formalized → **0.16**: §1.5 Settings set + §2.8; toggle + confirm-dialog gaps closed; Catalog HTML sync lags) · **API ✅** (no gap). **Settings-track work fully complete.** **Device editor** (DEV-01..04; carries the OQ-045 sticker-on-shell debt) is now **in pass** (brief authored; 3 drafts → gate — see ▶ IN PASS above). *(Inbox for next triage: **Settings OQ-060** (log-bundle shape) + the **`ConfirmSheet` unification** with the game-page track (OQ-061) · OQ-038 offline-cache-scope · OQ-058/059 (game-page) · the OQ-002/009/010/011 set.)*

## Legend
**State:** ✅ converged · 🔶 in pass / partial · 🔜 queued (#n) · ⬜ not started ·
**Implements from:** the exact mockup file(s) the frontend build composes from — filled at converge (normally the states board); — until one exists ·
**States board:** the §1.8/§1.6 state-matrix mockup exists ·
**Design-spec:** ✅ design-spec (Part-2 composition + §1.5 components) deliberately synced to this page's converged board · ⬜ formalization owed · — nothing to formalize yet ·
**API:** ✅ contract deliberately synced to this page's functionality · 🔶 endpoints drafted, not yet page-audited · ⬜ not mapped

| § | Screen | State | Mockup (version) | Implements from | States board | Design-spec | API | Notes / next |
|---|---|---|---|---|---|---|---|---|
| 3.1 | Collection (home) | ✅ converged | `collection-states.html` (per 0011–0013 + **COL-11**) | `collection/collection-states.html` | ✅ (friend-view **+ COL-11 browse tools**, lifecycle, offline) | ✅ (§2.1; 0.8/0013 · **COL-11 friend-tools 0.18** · **COL-12 peek-flip 0.25**) | ✅ (page-audited 0.17 · **COL-11 widen 0.20** · **COL-12 ripple 0.24** — `designer` rider + `percentComplete`) | 0.17 audit: list totals + item shape · `/me/collection/reorder` · now-playing pin · `/catalog/popular` (OQ-051, 0019). **COL-11 (decision 0021):** friend-view gains **full read-only browse tools** — bar parity on both friend artboards + a **sorted-by-hours list** & **read-only sort/filter drawer** (no Arrange); design-spec §2.1 friend-tools **formalized 0.18**. · **COL-12 peek-flip (decision 0026 — reverses OQ-059/0025):** shelf+grid card flips in place to the CARD-01 stats back — tap-to-flip · VIEW GAME → 4.2 · long-press shortcut · friend-view privacy-gated; **no on-face indicator** (owner). Board: the **States · card peek-flip (COL-12)** stage (grid · shelf · friend) folded into the canonical **`collection-states.html`** (variant retired, 2026-06-24). · **`h2-underlay-v2` RETIRED as an implements-from (2026-06-30, owner):** the legacy Phase-A home board is superseded by `collection-states.html` and carried pre-current debt (sub-9px Top-5 / now-playing plates · a `ratchet-art.png` raster ref · on-screen plate radius); **kept for history only**, no longer a build source. · **BUILT (M6 §P10, 2026-07-17, receipt `m6/surfaces-receipt.md`):** the TOP view goes CURATED (COL-13) — `/me/lists` live, ARRANGE drag re-rank + `CardPicker` add/remove (cap-10), the hours-sorted placeholder retired (code: `TopCurated` + shared `DragRankList`, component-map §12); owed on device: the drag re-rank feel + the A4 list-vs-grid call. |
| 3.2 | Discover | ✅ converged | `discover-states.html` (Two rooms; gate ruling 2026-06-13 — **bottom/smaller toggle · standard header · real games · Browse-By cut · simplified pages**) | `discover/discover-states.html` | ✅ (Up Next + Discover; lifecycle: skeleton · Signal-Lost+RETRY · offline writes-gated) | ✅ (§2.7 + §1.5 **Discover set**: `SegmentedKeycap`/`QueueRow`/`ReleaseRow`/`RecRow`/`AdoptCount`/`NotifyToggle`; 0.15) | ✅ (**page-audited 0.21**: `/me/queue` shape · `/discover/trending-cards` shape · upcoming **notify-me** + `release` pref · friend-recs **feed** (`GET /me/recommendations`) · `/discover/browse` parked → Game page; **OQ-053/054/055/057 resolved**) | **A "Two rooms" won the gate** + owner edits; drafts a/b/c kept for history. **Aesthetic call (2026-06-13): rows/rails won** — the card-fan sibling `discover-states-fan.html` is retired (kept for history) · **BUILT (M6 §P10, 2026-07-17, receipt `m6/surfaces-receipt.md`):** the §0.8 slice — DISCOVER live as the 5th nav tab (board-grounded), UP NEXT full (queue/reorder/rec-accept/now-playing pin via `QueueRow`/`RecRow`/`NowPlayingPin`), the browse-slice (trending `TrendRow` live · upcoming `ReleaseRow` honest-empty→now live via the P7 `/catalog/upcoming` rider) + the CAT-12 friends-active rail; owed = notify-me (M7, the endpoint isn't live yet). |
| 3.3 | Friends | ✅ converged | `friends-states.html` (A "Feed-first"; converged 2026-06-23) | `friends/friends-states.html` | ✅ (P1–P6 + lifecycle **L1 Skeleton · L2 Signal-Lost+RETRY · L3 Offline** writes-gated · **Q1 quiet-feed/empty-requests**) | ✅ (§1.5 Friends/Social set + §2.10; 0.23 — `FeedRow`/`FriendRow`/`RequestRow`/`InviteHook`; **`PresenceDot`/`StatPeek` CUT**) | ✅ (page-audited 0.23 — `GET /me/feed` item shape OQ-071; recommend-compose OQ-075 = design gap) | **CONVERGED 2026-06-23 → `friends-states.html`** (owner ruling: **A "Feed-first"** won; **B/C retired to history**). Carries Draft A's **P1–P6** (feed landing · active feed · roster SOC-01 · ADD FRIENDS hub · cold-start · actions sheet — VIEW/COMPARE→4.6/RECOMMEND/UNFRIEND/REPORT→4.16/BLOCK SOC-09, **no SHARE** OQ-052); **flat buttons rippled Inset-Recess→Scanline Energize** (F-03, the draft was skipped by the 2026-06-18 sweep); **F-06 clean** (no drift). **Lifecycle now drawn** + the **quiet/empty** state (Draft A only described these). **Find/add + full requests inbox = 4.8** (header key + banner jump there — the IA seam reconciled). **Deferred: SOC-05 recommend-compose** surface (entry-only) → **OQ-075**; feed shape → OQ-071. **Burt PASS** (1 owner-ratification: `.achv` gold-as-achievement glyph, inherited). Presence + mutual counts stay CUT (owner 2026-06-18). Brief `mockups/friends/friends-drafts-brief.md`. **Formalized 2026-06-23: design-spec §1.5 + §2.10 + api 0.23 (OQ-071 resolved). OQ-075 (recommend-compose) stays open.** · **BUILT (M6 §P8, 2026-07-17, receipt `m6/surfaces-receipt.md`):** FIRST ARTICLE — the feed-first landing (`FeedRow`), roster + the 6-action sheet (`FriendActionsSheet`, incl. the OQ-075 minimal `RecommendSheet` ASSUMPTION-flagged), the requests inbox (`RequestRow`, full SOC-08 lifecycle, walked live: a real accept formed a real friendship); owed: **the owner's first-article FRIENDS walk** (the standing pattern-ratifying gate — P9–P13 built past it per the overnight directive, parvati+murr stood in). |
| 3.4 | Store (incl. 4.11/4.12) | ✅ converged | `store-states.html` (5 rulings + 0017) | `store/store-states.html` | ✅ (incl. lifecycle, writes-gated offline) | ✅ (§2.3; 0.10) | ✅ | OQ-045 sticker preview → Device editor pass · **BUILT (M5 §P6, 2026-07-13, receipt `m5/surfaces-receipt.md`):** the commerce kit (§7) + `app/store.tsx` (browse · daily-claim · Top-Up · mock BUY → LandedMoment · Wallet + ledger) live on P1/P2/P4 seams (api 0.58–0.63); STORE keycap gold-active. **First-article owner walk done → F-1 fix-round built.** Premium aisle content = EXPECTED(P10 roster, landed 0075). Owed on device: hold-to-buy feel · reduce-motion BUY (OQ-046) |
| 3.5 | Profile | ✅ converged | `profile-states.html` (per 0011–0013) | `profile/profile-states.html` | ✅ (edit, friend-view, privacy, terminal, offline) | ✅ (§2.2; synced 0.43 — Top-3/Top-10 + nameplate F-06-bound) | ✅ (page-audited 0.39 — `top10`) | 0.17 audit: `/me` + `/users/:id` enumerated field-level (cooldown · device payload · limited card · `relationship`); friend-view SHARE chip cut (OQ-052 resolved, 0019); STATS box dropped → flat tiles (design-spec 0.29, 2026-06-27). **§4.4 ripple FORMALIZED (design-spec 0.33, 2026-06-28):** the **`RoleTag`** staff badge (PROF-09 — self-view shows the tier "ADMIN II"; friend/public view = generic "STAFF", tier hidden; absent for normal users) catalogued in §1.5 + folded into §2.2 (Compose · Friend-view · state-matrix). The other two §4.4 ripple debts stay OPEN: §4.15 Settings gated row → §2.8 · §4.12 Wallet `admin_adjustment` → §2.3. **Top-3/Top-10 + nameplate F-06-bound (decision 0047, 2026-06-29):** `TOP 5` → **TOP 3** legible `/grid` set-pieces + a **VIEW TOP 10** two-per-row view (+ edit-mode `EDIT TOP 10` → the dedicated §4.7 editor); **SOC-04 grew to Top-10**; the nameplate is now a UI label (≥9px). product-spec 0.39 · api 0.39 (`top10`) · design-spec §2.2 0.43; board applied across every artboard + a new Top-10 view (drafts `profile-top5-legibility-drafts.html`, owner picked B). · **h2-underlay Profile artboard RETIRED as an implements-from (2026-06-30):** `profile-states.html` is the sole source; the legacy artboard (old Top-5 + sub-9px plates) is **history only**. · **BUILT (M6 §P10, 2026-07-17, receipt `m6/surfaces-receipt.md`):** the three doors (PROF-05) live — VIEW TOP 10 → Collection TOP · Top-3 card tap → Collection TOP focused · friend VIEW COLLECTION → their full Collection — plus the Top-3/Top-10 teasers (self + friend, `AchievementSheet`/`BadgeTile` teasers off Profile); owed: device taste on the curated feel. |
| 4.1 | Add Game (flow) | ✅ converged | `add-game/add-game-states.html` (c6 + P9–P10 + PIXELS + `ReportSheet/drawer`) | `add-game/add-game-states.html` | ✅ (P1–P10 + P3b/P7b; lifecycle + writes-gated offline) | ✅ (§2.4; 0.12 + catalog v0.4) | ✅ (0016 + MOD-01 ripples) | — |
| 4.2 | Game page (adaptive) | ✅ converged | `game-page-states.html` (B, the dossier merge; converged 2026-06-17) · history: `dual-dossier`(+`-flat-buttons`/`-typescale`) · `trophy-dualface`/`trophy-collapsing`/`trophy-takes` · `draft-a/b/c` · `mix-takes` | `game-page/game-page-states.html` | ✅ (M1–M8 + lifecycle L1–L4: `Skeleton` · `LoadError` · `Offline` writes-gated + `Toast` · soft-hidden/reported MOD-02/08/09) | ✅ (formalized 0.21 — §2.4b + §1.5 Game-page set; decisions 0020/0021/0024/0025) | ✅ (page-audited 0.21 — no gap: `PATCH/DELETE /me/collection/:entryId` · `GET /me/collection/:entryId/cards` · `GET /games/:gameId/cards` (CARD-22 `equipped`) · `POST /cards/:id/adopt` · `DELETE /cards/:id`+`/unpublish` · `GET /users/:id/collection` (0020/0021) · `GET /catalog/games/:id`; compare client-side, decision 0020) | **CONVERGED 2026-06-17 → `game-page-states.html`:** candidate B folded into the full state matrix — 8 core (M1–M8) + the deferred lifecycle L1–L4 (Skeleton · LoadError · Offline writes-gated + Toast · soft-hidden/reported MOD-02/08/09, grammar reused from the sibling boards); flat **Scanline-Energize** keycaps (F-03, formalized 0.20) carried; the **`.btn.sec`→`.btn.cream`** KeycapButton/SectionHeader split (Bugfix #4). Reconciliations: card-back clean → OQ-069 (CARD-01 adoption-count, encoded decision 0024); WISHLIST omitted → OQ-070 (encoded decision 0025); OQ-058 (rating, encoded 0024) · OQ-061 remain open. **Design-spec ✅ (formalized 0.21: §2.4b + §1.5 Game-page set) · API ✅ (page-audited 0.21, no gap).** · **Prior — Re-brainstorm → card-as-trophy → owner picked A → high-fi (`trophy-collapsing`) → iteration (`trophy-dualface`, 2026-06-14):** the **hero shows both card sides side-by-side** (face + stats back, no flip; facts as a plain meta line) · **EDIT enlarges the stats card & edits the values directly on the card back** (provenance auto) · **Your Cards: tap-select (orange outline) + inline on-screen options** (SET-MAIN/EDIT/DELETE→`ConfirmSheet`, no hold-drawer) · **Share Card Image = a single docked footer at the bottom** (removed from per-card; owned only). Conventions held (**orange `KeycapButton`**, gold = card-creating only; `shadow.key`; C5; F-01/F-06/F-09). Panels A1 dual-face hero · A2 on-card stat edit · A3 scrolled (select+inline-options+bottom-share+delete-confirm) · A4 friend dual-face. **Refined (owner 2026-06-14):** screen title → **"GAME"** · **facts as a plain meta line under the game title** (boxed chips reverted) · the **stats card frame mirrored on the Y axis** (C5 step on opposite corners) · **EDIT is inline** (same screen, no mode) with the **stats card as the only card** while editing · **Your Cards selection = a thin orange border** (pip dropped) · **collapsed header = play stats only** (styler readout dropped) · **card-back credit = "CARD ART DESIGNED BY" + the account name** (adoption count off the back → reconcile CARD-01 at converge). **Next: owner review → converge → `game-page-states.html`** (full matrix + deferred lifecycle), then Design-spec + API page-audit. Carries rating PENDING OQ-058 · delete OQ-061. · **CHOSEN: candidate B (`game-page-dual-dossier`, 2026-06-15)** — owner *"the dossier take looks better."* The dossier's section switcher (PLAY·CARDS·ABOUT) **docked at the bottom** as the **`SectionSwitch`** (now **one-line**: icon inline w/ label; **pink pip dropped**, active = orange border + orange icon) · **dual-face hero kept** · **stats edited via a dossier form** (not on the card) + pinned DONE bar · the **CARDS switcher gained tap-select** (thin **orange ring + gap** on the selected card) + **inline options** (SET AS MAIN · EDIT IN STYLER · DELETE + `EquipReadout`). **8 artboards** capture all functionality. **Next: converge B → `game-page-states.html`; A retired.** · **Cards → catalog `/hero` (2026-06-16):** the dual-face hero (face + stats back, all 3 pairs — M1 owner-rest · M2 form-edit · M6/M7 friend-view) sized to the **catalog GameCard `/hero` (138×193, F-01)** — was a local `face2` (146×204); stale local `.hero` (168×235) corrected to the catalog token, dead `.face2` rule removed. Pure size alignment (no spec/API ripple). · **Dual-face hero → catalog `/grid` 2-up (2026-06-17):** owner clarified the `/grid` size targets the **dual-face hero (THE FACE + YOUR STATS)** — both now render **2-up at 161×225**, filling the PLAY row like the collection grid (base art 7px + plate 10px), **superseding the 2026-06-16 `/hero` (138×193)** above. The catalog `/grid` specimen was corrected (110px → the real collection 2-up size, 161×225). The **CARDS switcher (M3) + community gallery (M4)** stay **3-up `cell`** (an earlier same-day 2-up pass was reverted per owner). No spec/API ripple (catalog tracks the converged §3.1 collection; page in-pass). · **Type-scale pass merged (2026-06-17):** the on-screen type-scale changes from `game-page-dual-dossier-typescale.html` (a parallel-session fork) merged into the board — every on-screen font snapped to the **4-step scale (F-06): 21 headers/titles · 15 stat values · 11 buttons/chips/subtext/tabs · 9 section-heads/tags/labels**; glyphs snapped, inline `font-size` overrides removed, **print exempt** (card plates + card-back stats scale with the card). The `/grid` dual-face (161×225) + 3-up `cell` switcher/gallery were preserved (the fork predated that work, so re-applied). No spec/API ripple. · **M5 sub-states BUILT (§P8, 2026-07-13, receipt `m5/surfaces-receipt.md`):** the **CARDS community gallery** (`CommunityGallery` flattened 3-up · personalized `PriceChip`/FREE · `AdoptCount` · DESIGNED-BY) → **`AdoptCardSheet`** (0072 component-confirm · TOP-UP bridge · online-only) → **adopt lands in the switcher** (COL-06 origin-union, F-2 fix) on `GET /games/:gameId/cards`·`POST /cards/:id/adopt`·`/me/collection/:entryId/cards` (api 0.60–0.63); CARD-21 SHARE + block-the-designer (`POST /me/blocks`) live. Owed on device: browse→adopt→equip both price paths (owner gate-5 taste). · **BUILT (M6 §P9, 2026-07-17, receipt `m6/surfaces-receipt.md`):** the M7 friend artboard is live — `/user/[id]` relationship chip (incl. cooldown microcopy) · report/block entry · staff badge · read-only friend collection (COL-10/11) · SOC-11 friend entry detail (CARD-22 equipped readout live, ADOPT re-pointed) · the game-page **`friendsWhoOwn`** list; the friend shape now also serves stats/device/nowPlaying (C4 fix closed the loop, also un-zeroed `adoptionsReceived` + killed the ruled-never-implemented F-16/0055 `privacy` leak). The 0012 view-in-their-device chrome takeover is EXPECTED (a cross-screen override lift, M7 seam named). |
| 4.3 | Card editor — Styler | ✅ converged | `styler/styler-states.html` (carousel; gate ruling 2026-06-12 — **+NAMEPLATE, overlay cut**) | `styler/styler-states.html` | ✅ (skeleton · load-error · offline draft-safe, writes gated) | ✅ (§2.5; 0.13 + catalog v0.4) | ✅ (**0.16 styler sync**: card-bases + surprise · save-private · acquire-batch · `nameplate` enums; OQ-050 resolved) | **OQ-039 ruled** (decision 0018: COSM-01 +nameplate · TITLE→font+ink · Nameplates aisle); OQ-048 (intensity) + OQ-049 (save-private) resolved (0019) · **PREMIUM LAYER BUILT (M5 §P7, 2026-07-13, receipt `m5/surfaces-receipt.md`):** the `EXPECTED(M5)` rows come due — PX `CurrencyCounter` · PriceChips/PREVIEW flags · cost-stack · **`ReconcileSheet`** (funded/short) · KeepBeat PX-spent line, on the P3/P4 acquire seams (CARD-13). Additive slip into the signed M4 surface (murr diff-guarded). Owed on device: the ReconcileSheet funded path end-to-end (web-lane skia-rail quirk blocked it). |
| 4.3 | Card editor — Canvas | ✅ converged | `canvas/canvas-states.html` (print shop; gate ruling 2026-06-13 — **C wins** + undo/redo relocated, proof size-ladder) | `canvas/canvas-states.html` | ✅ (skeleton · load-error · offline: editing+PROOF from cache, PRESS writes-gated) | ✅ (formalized → **0.18**: §2.5b Canvas + §1.5 **Canvas set** — `CanvasStage`/`AssetShelf`/`ElementTray`/`LayerRack`(slips)/`editbar`/EDIT slip-sheet/PROOF size-ladder/PRESS finish-up/`PrintRitual`) | ✅ (**page-audited, no gap** — all writes exist 0.16/0.18: draft · autosave · publish · save-private · acquire-batch · assets · share-image; cap-30/save-private product-spec-only, 0019) | **OQ-007 + OQ-040 resolved design-side** (diegetic breakout · the first-print ritual); cap 30 (OQ-008) · **PREMIUM LAYER BUILT (M5 §P7, 2026-07-13, receipt `m5/surfaces-receipt.md`):** ◆ PUBLISH enabled → the **CARD-19 press checklist** UI → the full-tier **`PrintRitual`** wired to P3 publish (CARD-19/20; api 0.60) — walked live (card published, contributor stat ticked). Owed on device: the PrintRitual motion (CDP froze on it). |
| 4.4 | Admin console | ✅ converged | `admin-console-states.html` (decision 0037) | `admin-console/admin-console-states.html` | ✅ (H1/H2 · Q1–Q3 · R1–R4 · D1 · C1–C4 · M1–M3 · L1–L3) | ✅ (§2.18 + §1.5 Admin-console set, 0.38) | ✅ (page-audit 0.36) | **CONVERGED 2026-06-28 (decision 0037)** — board implements the full required in-app set (functionality pass clean); **OQ-081 IDs assigned** (MOD-11 field remediation · MOD-12 dossier · MOD-13 action-notice · MOD-14 direct edit · MOD-15 junk removal; takedown = MOD-08, unsuspend = MOD-09); `/admin/*` page-audited (api 0.36, every write → MOD-10); §2.18 + §1.5 Admin-console set formalized (design-spec 0.38). All P3–P5 correctly absent. **Prior — IN PASS (2026-06-27):** Admin-only (MOD-04). ONE coherent, **tier-adaptive** direction building → `mockups/admin-console/admin-console-states.html` (brief authored). Both tier views (**Admin I** P1 / **Admin II** P1+P2), reports queue split by target w/ a bottom `SectionSwitch /rail`, report detail + hide/restore/resolve, `SuspendSheet` (MOD-09), edit-suggestion review (MOD-06), catalog dedup `MergePicker`+`RestoreRow` (MOD-05), catalog ops (direct edit · junk removal · takedown MOD-08), the OQ-081 remediation surfaces (dossier · field-remediation · action-notice), + §1.8 lifecycle. New: `QueueRow`/`ReviewPanel`/`SuspendSheet`/`MergePicker`/`RestoreRow`/`TierBanner`/`CountTag`/`RoleTag`. Utility styling (no on-screen gold; destructive = `ConfirmSheet`+alert-red). **3 cross-board ripples — ALL FORMALIZED 2026-06-28** (design-spec 0.33 + 0.35): §3.5 Profile (`RoleTag` PROF-09) · §4.15 Settings (gated row → §2.8) · §4.12 Wallet (`admin_adjustment` ledger → §2.3). **`/admin/*` page-audit owed at converge** (OQ-081 surfaces have no enumerated endpoints). **IA settled → decisions 0033 + 0034** (2026-06-27). **Role model (0034):** moderator dropped → **`role ∈ user\|admin` + `adminTier 1..4`** (nested: **I Content · II Catalog · III Support · IV Platform**), bundling permission groups **P1 Content · P2 Catalog** (in-app console) · **P3 Economy/Support · P4 Config/Authoring · P5 Governance** (external tool). **+PROF-09** generic public STAFF badge (tier self-view only). Grant/revoke **out-of-band** (no endpoint, P5/external). **Console (this screen) = tiers I/II / P1+P2**: reports queue **split by target type** (card/game/user) w/ a **bottom rail** (SectionSwitch /rail) · suspension from a user report (MOD-09) · dedup/merge · **+ remediation additions → OQ-081** (field reset · user dossier · action-notice · direct catalog edit · junk removal · proactive takedown); utility styling (no gold; destructive ConfirmSheet). External economy/config/governance tool **parked → OQ-080**. **Economy/support tooling (decision 0035):** +**ECON-11** operator Pixel/entitlement adjustments (IAP refunds = RevenueCat; ours = audited service-layer ops, `admin_adjustment` ledger) + **MOD-10** append-only admin audit log (foundation); operator **UI deferred** (OQ-080). **Console is tier-adaptive** — draws an **Admin I view** (P1) vs **Admin II view** (P1+P2), never P3–P5. **Spec-ready** (product-spec 0.31 · api 0.31 · design-req 0.21 §4.4). **Ripple debt (converged boards): CLEARED 2026-06-28** — §3.5 Profile (staff badge, 0.33) + §4.15 Settings (gated Admin-console row, 0.35) + §4.12 Wallet (the `admin_adjustment` ledger entry, 0.35) all formalized ahead of the §4.4 design pass. |
| 4.5 | Device editor | ✅ converged | `device-states.html` (in-frame; A won + `SectionSwitch` rail + LOOKS) | `device/device-states.html` | ✅ (D1–D10: editing · shell-switch · theme-preview · sticker place/scale/rotate · OQ-045 on-shell preview · LOOKS · reconcile · Skeleton · Offline writes-gated · LoadError) | ✅ (§2.15 + §1.5 **Device-editor set**: `SectionSwitch` unified (/pair·/chips·/rail) · `SectionCard` · `StickerStage`/`TransformBox`/`PlacedSticker`/`StickerTray` · `SavedLook`/`LooksGrid` · `KeepBar`; 0.30 / decision 0030) | ✅ (page-audited 0.27 — `stickerComposition` shape `{version, stickers[id·assetId·zone·x·y·scale·rotation]}` zone-normalized + server-validated; `GET·POST /me/device/looks` + `DELETE /me/device/looks/:id`; apply=`PATCH /me/device`, KEEP=acquire-batch; OQ-062/064/065) | **CONVERGED** — owner picked **A "in-frame live edit"**; the **`SectionSwitch`** won (**bottom-docked · icon in-line · no pip · 4 sections incl. LOOKS**). Owner refinements: **stickers on plastic only** (screen + 5 keys no-go, DEV-03/04/F-04) · saved looks **identified by shell·theme** (no name) · **SAVE CURRENT** only. **OQ-045 closed design-side** (continuous on-shell preview). Drafts a/b/c + `switcher-takes`/`switcher-rail`/`draft-a-looks` kept for history. **FORMALIZED 2026-06-27 (decision 0030):** design-spec §1.5 Device-editor set + §2.15 (0.30) · product-spec **DEV-05** (0.27) · api 0.27; **OQ-045/062/063/064/065/076 → Resolved**. OQ-063 = `SectionSwitch` unified app-wide (variants `/pair`·`/chips`·`/rail`; `SegmentedKeycap`/`SectionChips` kept as aliases, converged boards untouched). · **PREMIUM LAYER BUILT (M5 §P7 + F-2b, 2026-07-13, receipt `m5/surfaces-receipt.md`):** the D7/D7b/D7c premium row — **`KeepBar` cart → `acquire-batch`**, cost-flags, PIXELS CountTag — plus the **server-side premium gate** on `PATCH /me/device` (409 `PREMIUM_UNRECONCILED` pre-write, migration 0012) + un-adopt REMOVE; walked live (BERRY 6 PX → CANCEL reverted clean; api 0.63). Owed on device: KeepBar hold-to-buy feel. |
| 4.6 | Compare Hours | ✅ converged | `compare-states.html` (converged · Versus) | `compare-hours/compare-states.html` | ✅ (P1–P5 + L1/L2 lifecycle) | ✅ (§1.5 Compare set + §2.12, 0.26) | ✅ (page-audited 0.25) | **CONVERGED 2026-06-24 → `compare-states.html`** (the owner-picked Versus / head-to-head direction): **P1** has-overlap (totals **face-off** + per-game **card-vs-card matchups** — their game-frame card · the hours, winner orange · your slate+foil card; real `#art-*` symbols; **card-tap → Game page §4.2**) · **P2** no-shared-games · **P3** friends leaderboard · **P4** Skeleton · **P5** privacy-limited (PROF-03) · **L1** Signal-Lost+RETRY · **L2** Offline (read-from-cache, SYS-10). Inherits the social-cluster shell (Teal/Midnight · flat Scanline-Energize · orange StateMark · F-06 · §1.6). **Non-commerce — no gold**; **completion % OUT**; read-only (PROF-03). **Burt clean.** Drafts A + C + `draft-b` retained for history. **Formalized 2026-06-24:** design-spec **0.26** (§1.5 Compare set + §2.12 composition) + api-contract **0.25** (`/me/compare/:friendId` payload + PROF-03 gating); **OQ-074 + OQ-077 resolved**. · **Prior — owner picked B (2026-06-23)** + matchup rework: dropped the paired comparison bars for draft A's **numeric** per-game stats in a new **card-vs-card** layout — **their `GameCard`/cell (96×134) left · stats middle, winner orange · your card right** (each card owner-labelled); cards up-sized to `/cell` for legibility (2026-06-24). Face-off hero + leaderboard + P2–P5 unchanged; **Burt clean**. · **Prior — IN PASS (2026-06-22):** track kicked off — brief + **3 distinct draft directions** (`mockups/compare-hours/`): **A "Side-by-side ledger"** (aligned YOU\|THEM columns — the calm scannable pole) · **B "Versus / head-to-head"** (a totals face-off + per-game matchup bars — the competitive pole) · **C "Diff-first / overlap"** (overlap + per-game deltas sorted by biggest gap — the insight pole). The marquee social payoff (SOC-03, the core return-driver), reached from the friend Profile's *Compare hours* action (SOC-02/PROF-05). Each renders **P1 has-overlap · P2 no-shared-games · P3 friends leaderboard · P4 Skeleton · P5 privacy-limited (PROF-03)**; Offline/LoadError deferred to converge. New components: `CompareHeader` · `CompareTotals` · `ComparePair`/`CompareRow` · `FriendsLeaderboard`/`LeaderRow` (composed from `StatTile`/`ListRow`/`GameCard`/`Avatar`). Inherits the social-cluster shell (Teal/Midnight · flat Scanline-Energize keycaps · orange StateMark · F-06 · §1.8). **Non-commerce — no gold** (Compare creates no card, ECON-01/F-02); **completion % OUT** (hours+games only). **Gap flagged: OQ-074** (`/me/compare/:friendId` payload shape unenumerated). Brief `mockups/compare-hours/compare-hours-brief.md`. · **BUILT (M6 §P9, 2026-07-17, receipt `m6/surfaces-receipt.md`):** `/compare/[friendId]` live — face-off (`FaceOff`, fuses `CompareHeader`+`CompareTotals`) · per-game matchups (`ComparePair`) · friends leaderboard (`FriendsLeaderboard`/`LeaderRow`) with `isMe` · zero gold held. |
| 4.7 | Lists / Top-10 → Collection TOP view | ✅ merged into §3.1 | ✅ §2.17→§2.1 (0.49) | `collection/collection-states.html` (TOP stage) | ✅ in §3.1 | ✅ | ✅ 0.39 | **✅ MERGED INTO THE COLLECTION (decision 0050, 2026-06-30):** the TOP view-mode is now drawn in `collection-states.html` — the **"View mode · TOP" stage** (self read · self **ARRANGE + CardPicker** · **friend read-only**), reached by the Collection `view` keycap (SHELF·GRID·LIST·**TOP**) or the Profile **VIEW TOP 10** / a **Top-3 card**, exited the same way. **`lists-states.html` RETIRED** (history banner; no longer a build source). OQ-083 reversed; no standalone editor. · **Prior — ✅ TOP-10 RE-PASS DONE (decision 0047, 2026-06-29):** SOC-04 grew **Top-5 → Top-10**; the five fixed ranked seats re-designed for **10** — a `#1 /hero` headliner + a **3-up `/cell` shelf of seats 2–10**, the `/cell` nameplate snapped to the legible **10px floor** (was 7.5px, sub-9px). S1 empty = headliner + 9 ghosts · S3 **full (10)** · S2 picker · S4–S6 lifecycle. design-spec §2.17 (0.44) · api 0.39 (`/me/lists` cap 10, `kind: top10`). **Burt PASS** (F-01 `/cell` 96 + `/hero` 138; #1 marker stays orange `StateMark`, not gold). · **CONVERGED + FORMALIZED 2026-06-28 → `lists/lists-states.html`.** Owner picked **A "Frame-first / ranked slots"** — a headliner `/hero` #1 over a 4-up `/cell` shelf (the PROF-05 trophy-case shape), ghost seats → CardPicker sheet → drop-in, drag to re-rank; cap structural. Full **§4.7 + §1.8** matrix: **S1 empty · S2 editing · S3 full (5) (five hero rows) · S4 Skeleton · S5 LoadError · S6 Offline (SYS-10)**. Non-commerce — no gold; #1 marker orange. **Burt PASS ✅.** **OQ-083 ruled DEDICATED** · **OQ-084 → Resolved** (`/me/lists` payload enumerated, api 0.34). **design-spec §2.17 + §1.5 Top-5-editor set (0.36).** Drafts a/b kept for history. |
| 4.8 | Find / Add Friends | ✅ converged | `find-add-friends-states.html` (off Friends draft A) | `find-add-friends/find-add-friends-states.html` | ✅ (P1 hub · P2/P2b search · P3 invite+QR · P4 requests+cooldown · P5 invite-landing SOC-10 · P6 PersonRow state-swatch+sent · L1–L3 lifecycle) | ✅ (§1.5 Friends/Social set + §2.11; 0.23) | ✅ (page-audited 0.23 — `/users/search` OQ-072 · `/invites/:token` OQ-073) | **CONVERGED + FORMALIZED 2026-06-23** (owner-iterated to clean) — ONE coherent board **built off Friends draft A** (same shell/tokens/flat keycaps/F-06/§1.8 grammar; reuses `RequestRow`/`InviteHook`/avatar). New components: **`PersonRow`** — the **relationship-state spine** (`none`→ADD · `outgoing`→CANCEL · `incoming`→ACCEPT/DECLINE-silent · `friends`→profile · `cooldown`→disabled+reason · `blocked`→limited; SOC-07/08/09) · bottom-docked **`SearchField`** (OQ-035) · **`QrCard`** (SOC-07) · **`InviteLanding`/`SenderSummary`** (SOC-10 one-tap prefilled ADD). Non-commerce — no gold, no friend SHARE (OQ-052), no contacts-matching; Block LIST lives in Settings (4.15). **Resolved (page-audit 0.23): OQ-072** (`/users/search` PersonRow shape + relationship enum) · **OQ-073** (`/invites/:token` resolve shape + client-side QR). **Back-nav (owner directive 2026-06-22):** the collection-states friend-view `‹ RETURN TO …` link **under the header** (hub→FRIENDS · invite/requests→ADD FRIENDS; P2/P2b keep CANCEL). **P5 = `FlowTakeover`** (owner pick 2026-06-22 — NavBand untouched; **✕ close top-right** per owner; prefilled ADD resolves into the sender's Profile; ✕ dismiss → home/back). Sent-confirm **Toast normalized** to the canonical Toast (scr-well + hairline; orange left-accent dropped). Brief `mockups/find-add-friends/find-add-friends-brief.md`. **Burt PASS ✅ — 0 deviations** (caret recolored pink→orange so no on-screen pink remains; both earlier deviations resolved by the 2026-06-18 DS sweeps on this board: flat buttons rippled Inset-Recess→**Scanline Energize** (F-03), `.err-card .bang` snapped 34→21px (F-06)). **Formalized 2026-06-23: design-spec §1.5 + §2.11 + api 0.23.** · **BUILT (M6 §P8, 2026-07-17, receipt `m6/surfaces-receipt.md`):** the Find/Add hub live — `PersonRow` 6-state relationship spine · `QrCard` (via the one sanctioned dep `react-native-qrcode-svg`, rule-08 justified, G-M glance queued) · `InviteLanding`/`SenderSummary` both branches live (`/invite/[token]`); owed: the owner's first-article FRIENDS walk covers this surface too. |
| 4.9 | Contributor profile | ✅ converged | `contributor-states.html` (Profile-like B; converged 2026-06-26) | `contributor-profile/contributor-states.html` | ✅ (P1 self · P2 empty · **P2b self·partial** · P3 friend-view · **P3b friend·empty** · P4 privacy-limited · **V1/V2 VIEW ALL** + lifecycle **L1 Skeleton · L2 Signal-Lost+RETRY · L3 Offline**, SYS-10) | ✅ (§1.5 Contributor-profile set + §2.16; 0.31) | ✅ (page-audited 0.28 — `GET /users/:id/contributions` friend/full vs limited + paginated `…/cards?cursor=`·`…/games?cursor=` VIEW ALL) | **CONVERGED + FORMALIZED 2026-06-27 → `contributor-states.html`** — the owner-picked **Profile-like B layout**: `IdentityBlock` → **boxless STATS tiles** (gold PROF-07 `PctPill`s — GAMES·TOP 25%, ADOPTIONS·TOP 10%) → **SIGNATURE CARD** PINNED-FAVOURITE hero (+ VIEW CARD) → **CARDS DESIGNED** as a legible `/cell` grid (rank chips) + **GAMES ADDED** as title rows (legible name + reach + chevron), each with a `VIEW ALL ›` TertiaryLink → the **VIEW ALL full-list views** (V1 all-cards `/cell` grid · V2 all-games title rows, each → its entry — **added 2026-06-27 per owner**; profile is now a top-N summary, e.g. Maverick 7 cards / 7 games); + the §1.8 lifecycle (L1–L3). **Completeness pass (2026-06-27):** added a 4th STATS tile **REACHED** (games-side impact = collections reached), **P2b self·partial** (games-but-no-cards → gold DESIGN-A-CARD section nudge + PROF-07 below-threshold no-chip stats) and **P3b friend·empty** (quiet, no create-hooks); noted (→ OQ-079) the friend-view VIEW ALL variant + VIEW ALL pagination + MOD-02 soft-hidden-content edge. **Burt PASS** (0 blocker/major; dead `mini`/`sk-mini` CSS pruned at converge). **FORMALIZED 2026-06-27 (spec-owner) → decision 0032:** product-spec **CAT-07** revised (dropped field-edits + achievement badges; added the standing + collections-reached + VIEW ALL) + new **CAT-10** (contributor standing — `PctPill` percentiles vs the *contributor cohort*, SYS-04-threshold + PROF-03-privacy gated) · design-spec **§1.5 Contributor-profile set + §2.16** (0.31; one new component `SectionEmpty`) · api **0.28** (`GET /users/:id/contributions` friend/full vs limited + the two VIEW-ALL cursor endpoints). Two completeness edges ruled in 0032: standing rides the honest aggregates on the limited view; own MOD-02 soft-hidden card inherits MOD-02. **OQ-079 → Resolved.** **History:** drafts A (Record) · B (Trophy, `-trophy`) · C (Impact) + the `-profilelike` source draft retained. Brief + gate ruling `mockups/contributor-profile/contributor-profile-brief.md`. · **BUILT (M6 §P13, 2026-07-17, receipt `m6/surfaces-receipt.md`):** `/contributor/[id]` live to the 14-state board — E8a closed (every DESIGNED-BY tap routes app-wide); gold `PctPill` standing tiles live; fix wave F2 (ShellNav PROFILE-active) + F3 (gallery BY-credit un-nested from the cell Pressable, structural jest guard) closed same-night. |
| 4.10 | Achievements | ✅ converged | `achievements-states.html` (Trophy Case A; converged 2026-06-27) | `achievements/achievements-states.html` | ✅ (§2.19 + §1.5 Achievements set; 0.39) | ✅ (page-audited 0.36 — the three `/achievements*` payloads; bounded, no cursor) | **CONVERGED (design) 2026-06-27 → `achievements-states.html`** — owner picked **A Trophy Case** (by status) + asked for **VIEW ALL** on each section. **14 artboards:** P1 self top-N summary (SUMMARY → EARNED → IN PROGRESS → SECRETS, each w/ `VIEW ALL ›` → its full-list view; + tier legend) · P2 empty (NON-gold) · **V1/V2/V3 VIEW ALL** (all earned · all in-progress · all secrets, `‹ ACHIEVEMENTS` back-seam) · **D1/D2/D3 node-detail bottom sheet** (tap a node → tier · what-for · when-earned · reward; D2 in-progress = criterion+progress+prize; D3 locked `???` stays sealed) · P3 friend-view (earned only, no leak) · P4 privacy-limited (`lock-well`) · P5 unlock celebration (ACH-06, tier-coloured) · **L1 Skeleton · L2 Signal-Lost+RETRY · L3 Offline** (SYS-10). **Tiles unified → glyph+label only** (dropped the mixed EARNED/REWARD/50-GAMES sub-text; criterion/reward/date live in the detail sheet; owner 2026-06-27). **OQ-005 → resolved (pole A — `???` mystery slots).** **★ 3-TIER colours (OQ-082):** PRESTIGE = gold · STANDARD = theme accent (`--scr-accent`, re-themes DEV-04) · SECRET = magenta (`--scr-secret`); resolves the badge-gold flag. Components: `BadgeTile`/`MysterySlot`/`ProgressMeter`/`RewardChip`/`CelebrationMoment`/**`AchievementSheet`** + tier legend + VIEW-ALL view. **Burt PASS** (F-02 gold-prestige + F-05 magenta = owner-ratified carve-outs). Drafts A/B kept for history. **Converge-pass fix 2026-06-28:** restored the **P5 `CelebrationMoment` CSS** (`.celebrate`/`.cel-*`) that the fold dropped (the marquee ACH-06 state was rendering unstyled) — ported from `achievements-draft-a-trophycase.html`, adapted for `.cel-badge-wrap`, render-verified, Burt PASS; presentation-only, no spec/API/OQ change. **FORMALIZED 2026-06-28 (spec-owner) → decision 0038:** product-spec **+ACH-09** (the presentation `tier` — `prestige` gold · `standard` theme-accent re-themes DEV-04 · `secret` magenta; which-tier = content OQ-004; 0.36) · design-spec **§1.5 Achievements set + §2.19** (0.39; `BadgeTile`/`MysterySlot`/`ProgressMeter`/`RewardChip`/`TierLegend`/`AchievementSheet`/`CelebrationMoment`) · api **0.36** (the three `/achievements*` payloads enumerated — earned-only friend-view, honest-count limited; bounded, no cursor; ACH-06 push-driven). **F-02 + F-05 carve-outs** amended (**Catalog v0.11** + design-spec §1.1): gold = also non-acquisitive PRESTIGE (resolves the cross-screen badge-gold flag) · on-screen magenta SECRET (`brand.secret`/`scr.secret`). **OQ-082 + OQ-005 → Resolved.** Brief `mockups/achievements/achievements-brief.md`. · **BUILT (M6 §P11+§P6, 2026-07-17, receipt `m6/surfaces-receipt.md`):** `/achievements` trophy case live (`BadgeTile`/`MysterySlot`/`ProgressMeter`/`RewardChip`/`TierLegend`/`AchievementSheet`, sealed `???` D3 with jest no-leak assertions, tier colours off themed tokens) + the server engine (7 criterion kinds, atomic badge+PX+entitlement rewards, count-from-genesis) + `CelebrationMoment` (reduce-motion-safe; refetch-delta trigger ASSUMPTION, M7 push replaces it) + Profile teasers (self+friend). **OQ-151 pending** (count-from-genesis makes satisfied-but-unfired counters read e.g. "3/1" — owner ruling owed; PLAYER TWO sits at 1/1 on the demo account, ready to fire live at the owner's first friend-accept). |
| 4.11 | Store item detail / purchase | ✅ (folded into 3.4) | `store-states.html` P2/P2b/P3/P4/P5 | `store/store-states.html` (P2–P5 + P1b/P2b) | ✅ | ✅ (§2.3) | ✅ | Sheet grammar; hold-to-buy · **BUILT (M5 §P6, folded into 3.4):** `ItemSheet`/`PreviewStage`/`BuyBar` + the OQ-046 non-hold alt built + unit-tested; live item sheets reachable once the roster fills (0075) |
| 4.12 | Wallet | ✅ (folded into 3.4) | `store-states.html` P8 | `store/store-states.html` (P8) | ✅ (incl. ECON-09 negative) | ✅ (§2.3) | ✅ | **Ripple DRAWN (2026-06-27 · §4.4 pass):** the P8 ledger now carries the **operator-adjustment** entry (`ECON-11` `admin_adjustment` — "OPERATOR ADJUSTMENT · GOODWILL" +20, a credit/debit the user didn't initiate; reuses the `LedgerRow` styling). **FORMALIZED 2026-06-28 → design-spec §2.3 (0.35)** — Wallet `LedgerRow` history + state matrix. · **BUILT (M5 §P6, folded into 3.4):** the Wallet hero + `LedgerRow` history live on `/me/wallet[/ledger]` (incl. `admin_adjustment` + the ECON-09 negative variant); balance always derives from the ledger (G-I). |
| 4.13 | Welcome & Auth | ✅ converged | `welcome-auth-states.html` (games-forward, decision 0029; **Draft-A sign-in + 5 lifecycle/error states**, 2026-06-27) · drafts: `welcome-auth-form-drafts.html` (A/B/C) | `welcome-auth/welcome-auth-states.html` | ✅ (**18 states** — W1/W1b landing · W2/W3/W4 create+availability+validation · W5/W5b/W5c/W12 sign-in +rate-limit+suspended+in-flight · W6 SIWA · W7/W8a/W8b/W8c reset +expired · W9 verify · W10/W11/W13 Signal-Lost/Offline/Skeleton) | ✅ (§2.13 + `NavBand/locked` §1.5; design-spec **0.32**) | ✅ (page-audited — `/auth/*` incl. `username-available` AUTH-11 + `/stats/public` SYS-12 + the `RATE_LIMITED`/`ACCOUNT_SUSPENDED`/`SERVER_ERROR` model + W8c invalid-token / W4 register-rejection codes; api 0.32, **no new endpoints**) | CONVERGED + formalized: Draft-A sign-in declunk + completed state matrix (every case routed) this session |
| 4.14 | Onboarding | ✅ converged | `onboarding-states.html` (0029 + converge 2026-06-27) | `onboarding/onboarding-states.html` | ✅ (O1–O10 + L1–L3) | ✅ (§2.14; 0.27 + converge 0.34) | ✅ (auth/collection/catalog/queue + CAT-09 popular + **CAT-11 `/catalog/new-releases`**, 0.33) | **CONVERGED + refined 2026-06-27:** O2 = two add-rails (POPULAR + **NEW RELEASES**/CAT-11) · count badge dropped (count on CONTINUE) · `SearchField` above CONTINUE on continuous-blue · **COL-02 status chips = scanline+StateMark, no border** (rippled → game-page `.schip`) · back = `‹ BACK` return-link · square pips + friends-rail removal landed. Decision 0029 + converge refinements. |
| 4.15 | Settings | ✅ converged | `settings-states.html` (gate ruling 2026-06-14: **C-triage feedback** + **notifications as its own page**; +owner refinements) | `settings/settings-states.html` | ✅ (16-device matrix: lean list ×2 · unverified · notif-page +OS-declined · blocked · delete `ConfirmSheet` · C-triage feedback ×6 · Skeleton · Offline · LoadError) | ✅ (formalized → **0.16**: §2.8 + §1.5 **Settings set** — `Toggle`/`ConfirmSheet`/`LogAttach`/`FeedbackConfirm`/`TriageCard`; **toggle + confirm-dialog gaps closed**; Catalog HTML sync lags v0.4 as for Report/Discover) | ✅ (no gap — feedback `POST /feedback`+`/feedback/:id/logs` 0.22; username/verify/blocks/notification-prefs/account-delete all exist; **notifications-as-page is pure presentation**) | **CONVERGED** — owner picked **C "type-first triage"** + ruled **notifications → its own page** (lean list; each heavier feature opens a focused page). **Owner refinements 2026-06-14 (both former residuals resolved):** feedback SUBMIT = **mid-width `KeycapButton/primary`** (orange) · destructive confirm = **bottom `ConfirmSheet`** (renamed from `ConfirmDialog`; aligns w/ game-page `ConfirmSheet`, OQ-061 — spec-owner to unify). `settings-page.html` + feedback drafts a/b/c kept for history. **OQ-060** (log-bundle shape) honored by reserved UI room. Brief + ruling `plans/2026-06-13-settings-drafts-brief.md`. **§4.4 ripple (2026-06-27):** a **STAFF** section with the role-gated **ADMIN CONSOLE** row added (MOD-04 · renders only when `adminTier ≥ 1`; invisible to normal users; no role-management here). **FORMALIZED 2026-06-28 → design-spec §2.8 (0.35)** — the STAFF `ListRow` folded into the list + state matrix. · **BUILT (M6 §P12, 2026-07-17, receipt `m6/surfaces-receipt.md`):** the §0.10 Settings shell live + the BLOCKED page (`apps/mobile/app/settings/blocked.tsx`, MOD-09 unblock walked live) + `Toggle`/`ConfirmSheet` in production use; C2 fixed (/settings joined the onProfile nav predicate, new ShellNav test suite guards the fall-through both ways). Notifications page + feedback deferred to M7. |
| 4.16 | Report (modal) | ✅ converged | `report-sheet/report-states.html` (converged board, A `/drawer`; `report-sheet-drafts.html` A/B/C + `add-game-states.html` P3b kept for history) | `report-sheet/report-states.html` | ✅ (3 targets × full lifecycle matrix) | ✅ (formalized 0.14 — §1.5 `ReportSheet` lifecycle + new `ReportConfirm` + new §2.6 Report; catalog v0.4 static, lifecycle swatches optional) | ✅ (page-audited — `POST /reports` {card\|game\|user + reason + `details`} incl. duplicate + `/me/blocks` SOC-09) | **Promoted to its own board** (9 devices): card · catalog (+duplicate) · user **+ Block** (SOC-09) across launch · picker · required-details (MOD-01, mod-only/outside MOD-07) · dormant-submit · **in-flight · filed-confirm (MOD-02 soft-hide, no thresholds) · post-block confirm · offline writes-gate (SYS-10) · error→Toast** (§1.8). All drawn affordances contract-backed (no OQ logged) · **BUILT (M6 §P12+§P7, 2026-07-17, receipt `m6/surfaces-receipt.md`):** live end-to-end — the full `ReportSheet` matrix on card/game (+user via P9) with block-alongside, deferred rows ABSENT not disabled; a real report filed 201 at review; server-side capture-only (the 0.70-pinned MOD-01 reason enum, no target-existence lookup — the anti-oracle stance). |

## How to update (the protocol)
At the end of **every draft pass**: update your screen's row (state · mockup version · states board ·
at converge: fill **Implements from** with the board's path · **re-judge the Design-spec column
against the converged board** and the **API column against what the page actually shows**), refresh
UP NEXT, bump the date, and surface the changed rows to the owner in the wrap-up. The Design-spec and
API columns are the tripwires — a converged board's formalization batch is owed before its Design-spec
reads ✅, and functionality drawn on a page must reach the contract (or the inbox) before its API
reads ✅.

**If the pass resolved any OQs design-side**, also complete [`00-INDEX`](../00-INDEX.md) §4 Step-3 —
relocate them from `## Open` to `## Resolved` in [`open-questions.md`](../open-questions.md) and refresh
**design-spec §2.8** traceability — don't only inline-tag them. (The OQ analogue of the §4 register-update
step added in `a4d93d9`.)
