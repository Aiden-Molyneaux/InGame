# InGame — Design Spec

> The design-side source of visual truth. **Part 1 (Foundations)** names every token and component;
> **Part 2 (Screens)** composes from the catalog by name. Mirrors the product-spec's shape per
> [`design-process.md`](design-process.md). No net-new component without adding it here first.

**Version:** 0.32 (Phase B + state passes + Store + Add Game + Styler + Canvas + Report + Discover + Settings + Flat buttons / Scanline Energize + Game page + DS conformance sweep + Friends / Find-Add + F-02 gold-economy clarification + Collection peek-flip (COL-12) + Compare Hours (4.6) + **Welcome & Auth (4.13) / Onboarding (4.14)** + F-05 on-screen pips squared + Profile STATS de-welled + Device editor (4.5) + **Contributor profile (4.9)** + **Welcome & Auth Draft-A sign-in + 5 lifecycle/error states**) · **Last updated:** 2026-06-27 · **Owner:** Claude Design
**Source mockups:** `H2 Underlay — Corner System C5 Hybrid` (Collection + Profile + shell, Phase A direction) · `Button System Drafts` (OQ-006 exploration) · `Collection States` + `Profile States` (state matrices) · `Store States` (converged Store board, store track) · `Currency Identity v2` (the PIXELS ruling) · `Report States` (converged ReportSheet board, report track) · `Discover States` (converged Two-rooms board, discover track) · `Settings States` (converged Settings board, settings track) · `Canvas States` (converged print-shop board, canvas track) · `Game page States` (converged game-page board, game-page track) · `Design System Catalog` v0.4
**Canonical baseline:** **Teal shell + Midnight screen theme** (all token values below are Midnight unless noted).

---

## Part 1 — Foundations

### 1.0 Foundation rules (F-IDs)

| ID | Rule |
|---|---|
| **F-01** | **Never crop a Game Card.** Every surface renders the full face, scaled — never cropped, never slivered. (OQ-014; ties to CARD-07) |
| **F-02** | **The step diagonal belongs to the card.** The TL+BR pixel-step is the GameCard's signature; all other chrome is square 90°. A button may borrow the step at **half scale (2/4)**, and **colour disambiguates intent** (OQ-036): **gold + step = an _acquisitive_ action** — creating a card (ADD), **moving PIXELS** (the currency mark, `PriceChip`, `BuyBar`/CLAIM — gold is the economy's colour, ECON-01), or a **primary add-to-collection / queue** (ratified 2026-06-23) · **system-orange + step = a prominent _non-acquisitive_ action** (RETRY, ADD FRIEND, SHARE). Step + a card's own colour = a GameCard; step + gold = get / make / spend; step + orange = borrows the geometry, not the identity. Gold stays **off** secondary or incidental controls (SHARE · RETRY · nav · a generic CTA). *(The avatar "design" affordance is a circular corner badge — no step — and is exempt.)* |
| **F-03** | **Keycap physics on the shell; flat + Scanline Energize on screen.** The shell's 5 nav keys stay tactile keycaps — a hard `0 4px 0` drop edge that travels down when pressed. **On-screen** action buttons + tool keycaps are **flat 2D** (no raised edge, no press-travel); **pressed/active = Scanline Energize** — fine CRT scanlines over a hairline-darkened fill, no motion (`motion.scanlineEnergize`). The C5 square chrome + the F-02 corner-step stay. *(OQ-006 → Keycap take 2026-06-11; the on-screen `0 3px 0`/`0 2px 0` raised edge + press-travel **deprecated** → flat / Scanline, owner-locked 2026-06-18.)* |
| **F-04** | **Nav legibility beats customization.** Shell colours/stickers may never obscure the 5 nav keycaps or their labels. (DEV-01) |
| **F-05** | **Pips: shell LEDs are lights, on-screen pips are pixels.** The shell's indicator LEDs — the `NavBand` active pip and the power LED — are **round** lit pips (physical lights, not panels). **On-screen position pips** (CardFan / carousel swipe dots) are **square, pixel-stepped, flat** — the `StateMark` notch geometry (`scr.accent` lit / `scr.grip` off, 6px, no glow), on-screen chrome per F-07. *(On-screen status/notification dots — `er-dot`/`kb-dot` — stay round lit pending owner; owner squared the position pips 2026-06-26.)* |
| **F-06** | **The 4-step type scale is law on screen** (21/15/11/9). Card plates are *print on the card object* and scale with the card instead. |
| **F-07** | **Radius lives on plastic.** Rounded corners exist only on the physical shell (device, bezel, keycaps). On-screen chrome is square (C5). |
| **F-08** | **One font per surface.** Chakra Petch is THE screen font (every text on the app screen, incl. card plates, buttons, tags, links); Paytone One is THE plastic font (silkscreened shell labels). No third voice. |
| **F-09** | **No sunken containers.** Panels, wells, tiles, rows, and selection states never render inset/"carved-in" — surfaces are flat planes one step lighter than their background; **selection = an accent border (+ the `StateMark`)**, never an inset shadow or a darkened recess. The on-screen selection marker is the **orange pixel-square** (`scr.accent`, flat, square, one-pixel-notched corners — `StateMark`), **not** the pink LED pip. *Exceptions by name:* **shell nav keycaps** (they physically travel — F-03's shell metaphor) and **text inputs** (the cream inset field). *(On-screen pressed buttons no longer travel or sink — they **Scanline Energize**, flat; 2026-06-18.)* **Selection borders + the `StateMark` use the screen accent (`scr.accent`), not `brand.accent`** (owner pass-5 — pink stays the **SHELL LED** voice only, F-05; it is no longer the on-screen selection marker). (Owner rulings: "flat panels — not something Game Boy had," 2026-06-10; the report-drawer selection, 2026-06-12; the on-screen marker audit, 2026-06-15.) |

### 1.1 Colour tokens

#### Shell palettes (`shell.*`) — 5 colourways, Teal canonical
Each palette sets the full physical set; derived alphas use `shell.ink` via `color-mix`. Carbon inverts `shell.silk` to a light tone (labels on dark plastic).

| Token | Teal ★ | Grape | Sunset | Pink | Carbon |
|---|---|---|---|---|---|
| `shell.plastic` | `#2bb6b0` | `#8a76e8` | `#ff9051` | `#ff8ab0` | `#3a3a42` |
| `shell.plasticHi` | `#3ec9c2` | `#9f8df2` | `#ffa76f` | `#ffa5c4` | `#4a4a54` |
| `shell.plasticLo` | `#1f9a94` | `#7460cc` | `#ea7437` | `#e96d96` | `#2c2c33` |
| `shell.silk` (labels) | `#0d524e` | `#392c7a` | `#7c3a12` | `#8a2247` | `#b9b9c6` |
| `shell.ink` (logo/grille/key-shadow @ 28–70%) | `#08433f` | `#2d2164` | `#6b2f0c` | `#76183a` | `#0a0a0e` |
| `shell.screwHi / screwLo` | `#2aa39d / #137671` | `#7d6cd8 / #5b49b4` | `#f08148 / #c75e26` | `#f07da6 / #c75880` | `#45454f / #26262d` |
| `shell.pipOff` | `#117672` | `#5e4cb8` | `#c96a2e` | `#c0577f` | `#16161c` |

#### Screen themes (`scr.*`) — 3 dark + 3 light, Midnight canonical

| Token | Midnight ★ (dark) | Deep Sea (dark) | Berry (dark) | Paper (light) | Mint (light) | Lilac (light) |
|---|---|---|---|---|---|---|
| `scr.bg` | `#232045` | `#0f2e2b` | `#2f1733` | `#ece5d1` | `#ddeee9` | `#e6e1f4` |
| `scr.well` | `#2c2950` | `#173d38` | `#3d2042` | `#f9f5e9` | `#f4faf7` | `#f7f5fc` |
| `scr.tools` | `#1a1733` | `#0a1f1d` | `#1f0e23` | `#dfd6bd` | `#c8e0d8` | `#d2c9ec` |
| `scr.accent` / `accentInk` | `#ff9f43` / `#2a1430` | `#ffd23f` / `#3c2a09` | `#ff3d77` / `#2a1430` | `#c75c12` / `#fff` | `#117672` / `#fff` | `#6c4fd8` / `#fff` |
| `scr.head` | `#f5f1e4` | `#f5f1e4` | `#f5f1e4` | `#1d2a4a` | `#143a36` | `#2c2260` |
| `scr.text` / `dim` / `soft` | `#fff / #9b97c0 / #c9c5e6` | `#fff / #84aaa2 / #bcd6cf` | `#fff / #b18ab8 / #dcc4e0` | `#2a2633 / #8a8270 / #5d5747` | `#16302c / #5f827b / #3c5a54` | `#2a2444 / #7d76a0 / #4a4466` |
| `scr.panel` / `hairline` | `α-white .065 / .08` | `α-white .06 / .08` | `α-white .06 / .09` | `α-navy .06 / .12` | `α-teal .07 / .14` | `α-violet .07 / .13` |
| `scr.grip` | `#565178` | `#3f655d` | `#6b4a72` | `#b9b09a` | `#9cc0b7` | `#a89ed0` |
| `scr.chip` | `#f5f1e4` | `#f5f1e4` | `#f5f1e4` | `#fff` | `#fff` | `#fff` |

#### Brand constants (`brand.*`) — theme-invariant
| Token | Value | Use |
|---|---|---|
| `brand.accent` | `#ff3d77` | Lit pips, notification dots, Collection nav keycap |
| `brand.gold` | `#ffd23f` | Value marker: ADD keycap, CountKeycap, RankChip/first, PctPill, Store nav keycap |
| `brand.cream` | `#f5f1e4` | Keycaps, dark-theme chips |
| `brand.navy` | `#1d2a4a` | Keycap iconography, `PlateButton/secondary` |
| `brand.bezel` | `#14122a` | Screen bezel |
| `brand.alert` | `#e3414e` | Destructive keycaps (KeycapButton/destructive) |
| `brand.success` | `#7fcf9b` | **Positive / affirmative status** — verified · setting-on · attached · ledger credits. The green counterpart to `brand.alert`; theme-invariant (light-theme contrast variant parked, decision 0023). |

### 1.2 Typography (`type.*`)
- **Families (F-08):** `type.ui` = Chakra Petch — the screen font (all on-screen text incl. card plates, buttons, tags, links) · `type.shell` = Paytone One — the plastic font (shell labels only). *Silkscreen retired in v0.3.*
- **Scale (screen, F-06):** `type.display` 21px · `type.emphasis` 15px · `type.body` 11px · `type.micro` 9px. Weights 600–700; micro labels letterspaced 1–2px, uppercase.
- **F-06 is law — the mockups CONFORM, F-06 is NOT amended (owner ruling, decision 0024 / OQ-066):** the 2026-06-16 DS-conformance audit found the `*-states.html` boards consistently drifting **off-scale** (state-titles 17px shared across ~7 screens · some titles 13px · dense body/sub 10–10.5px · "mini" action buttons 9px, Settings SEND 12px). The ruling is to **snap each off-scale value to the nearest of 21/15/11/9** — no new `state-title`/`mini-button` role is carved out. *Off-scale → snapped:* 17 → **15** (emphasis) · 13 → **15** · 12/10.5/10 → **11** (body) · stray 9px buttons → the button tier's 11px. **The actual ~7-board conformance SWEEP is a separate follow-up task — flagged owed below, NOT done in this pass** (no board edited here).
- **Card print (size-exempt, F-06):** plate = `type.ui` 700, ≈ 10px @ hero/grid → 4.5px mini → 3.2px thumb (scales with the card).
- **Plate legibility floor (owner pass-2, add-game track):** in **interactive fan/picker contexts** plates render **≥10px forefront / ≥9px neighbors** — the plate band grows to fit (two-line wrap allowed); deep-mini contexts (shelf strips, deal thumbs) stay exempt — the surrounding meta carries identity there. *Editor ripple: start-from templates keep a minimum plate-band ratio so baked plates (CARD-01/15) stay legible at fan sizes.*

### 1.3 Spacing, corners, elevation
- **Spacing scale:** 2 · 4 · 6 · 8 · 12 · 16 (+18 panel padding). Grid gutter 12; Top-5 gutter 6; screen gutter 12–16.
- **Corner system (C5):** on-screen chrome square. GameCard step (TL+BR): 4/8 double-step @ hero/grid · 3 single @ cell/mini · 2.5 @ thumb; art and plate repeat the diagonal at smaller steps. ADD exception = 2/4 (F-02). Shell radii: device 30 · bezel 20 · keycap 15 · screen 13 · mini-device 7.
- **Elevation:** shell = soft physical (inset highlights + `0 4px 0` ink edges). Screen keycaps = **flat** — `shadow.key` (the `0 3px 0`/`0 2px 0` raised edge + inset bevel + the clipped `drop-shadow`) is **deprecated** for on-screen (2026-06-18); pressed/active = `motion.scanlineEnergize`, not a raised edge. `shadow.card` `0 4px 7px rgba(0,0,0,.35)` drop · `glow.pip` `0 0 6–9px` accent.

### 1.4 Motion (`motion.*`)
| Token | Spec | Status |
|---|---|---|
| `motion.keyPress` (**shell nav only**) | translateY 3px, drop edge →1px, ~80ms ease-out; the 5 plastic nav keys (on-screen travel **retired** 2026-06-18) | ✓ designed |
| `motion.scanlineEnergize` (**screen keycaps**) | pressed/active = fine horizontal CRT scanlines (2px pitch) over a hairline-darkened fill (`color-mix` ~12% black), **no motion**; the flat replacement for the on-screen `motion.keyPress` travel | ✓ designed |
| `motion.foilSweep` | 18° highlight band, 3.4s ease-in-out loop, screen blend (custom cards) | ✓ designed (parked) |
| `motion.stateMark` | the on-screen **`StateMark`** appears on activation — a flat `scr.accent` pixel-square, no glow (the **SHELL LED** pip keeps its pink-`brand.accent`-glow swap, F-05) | ✓ designed |
| `motion.holdToBuy` | BUY keycap sits pressed while a darker fill sweeps it (~rated hold); release early = cancel, nothing spent. The spend confirm for instant-PX purchases (OQ-046; a11y non-hold alternative owed) | ✓ designed (store track) |
| `motion.counterTick` | `CurrencyCounter` glows + a `+N` tick chip beside it; the quiet success beat for grants/claims | ✓ designed (store track) |
| `motion.cardFlip` · `powerOn` · `celebration` | — | **Phase C/D** |

### 1.5 Component catalog
**Shell (3D, F-03):** `DeviceShell` (frame/screws/grille/LED/logo) · `NavBand` + `NavKeycap` (5 tabs; variants: gold Store, pink Collection; **`/locked`** = logged-out grayed/desaturated + non-interactive "SIGN IN TO UNLOCK", keys keep shell physics, F-04 — the Welcome & Auth pattern, decision 0029; active = pressed + `PipLight`) · `MiniDevice`.
**The card:** `GameCard/hero|grid|cell|mini|thumb` (face = art + plate title only; plate in `type.ui` 700; step per 1.3; **`/cell` = 96×134, the 3-up grid / picker cell — ratified 2026-06-18 (F-01), between `/hero` and `/mini`; the boards' grids/pickers/fans use it, not the 2-up `/grid` 161×225**) · `GameCard+custom` (owner shell colour + `FoilTag`; `motion.foilSweep`) · `NowTag` · `FoilTag` (tags: `type.ui` 700 9px) · `RankChip` (+`/first`).
**Buttons (screen — the Keycap system, F-03):**
- `KeycapButton` — action tier. `type.ui` 700 11px (letterspaced 1px), **flat keycap** (no `shadow.key` — flattened 2026-06-18). Variants: `/primary` (**`scr.accent` fill + `accentInk`** — the theme's action colour; *the soft "bronze" `scr.btn` token is **retired**, owner pass-5: action buttons read orange/on-theme*) — **as ADD: `brand.gold` fill + the 2/4 step via drop-shadow** (gold marks card-creating actions, F-02) · `/action-alt` (`scr.accent` **+ the 2/4 step** — prominent *non-card* actions: RETRY, ADD FRIEND; differs from `/primary` by the step, not the fill) · `/secondary` (`brand.cream`, navy ink; HTML modifier `.btn.cream` — **not `.sec`**, which is the `SectionHeader` class and leaked margin/layout into button height; split at the Game-page converge, Bugfix #4) · `/destructive` (`brand.alert`, white ink — also the **report SUBMIT**). Pressed = `motion.scanlineEnergize` (flat — CRT scanlines + hairline-darken, no travel); the clip-path corner-step is kept.
- `ToolKeycap` — tool tier. Cream **flat** keycap 28–32px (icon, or icon+label), navy ink (no raised edge); active = `motion.scanlineEnergize` + the `StateMark` when stateful.
- `TertiaryLink` — `type.ui` 600 11px accent (`►` prefix optional); dim variant for CANCEL.
**Screen furniture:** `ScreenHead` (display title + `CountKeycap`) · `CountKeycap` (`brand.gold`, **flat** — no keycap edge) · `Well` (hairline-ruled panel) · `ToolsBar` (grip + `ToolKeycap`s + spacer + `KeycapButton/primary`) · `SectionHeader` (micro caps + optional `TertiaryLink`) · `StatTile` · `PctPill` (`brand.gold` fill, dark-gold ink) · `GTag` (+`/add` dashed) · `ListRow` (`RowIcon` + label + value + chevron) · `Strip` (thumb card + meta + chevron) · `Avatar` (+`DesignBadge` — circular corner badge opening the avatar in the Card editor, PROF-08) · `IdentityBlock` · `StateMark` (the on-screen selection/active marker — a flat `scr.accent` pixel-square with one-pixel-notched corners; **not** the pink shell LED, F-05/F-09; renamed from `ChipPip`, 2026-06-15 marker audit).
**Inputs & search:** `SearchField/in-place` (cream inset field; live-filters the current view, COL-09; **bottom-docked posture** in flows — typing puts the field on the keyboard, OQ-035 by construction) · `MatchTag` (dev/publisher hit).
**Forms & flow (the Add Game set, add-game track 2026-06-12):** `FlowTakeover` (the tier-2 spatial pattern: a flow occupies the screen content with its own header; **frame + NavBand stay** — the tiers: sheet · takeover · breakout, decision 0014) · `FlowHeader` (✕/◂ keycap + flow title + optional `CountKeycap`) · `TextField` (cream inset, navy ink, labeled; **`/area`** ~3 rows; `/error` = alert hairline + micro message; required marker = the question as the label + dim caps "REQUIRED FOR THIS REASON") · `SelectField` (label + current value + ▾ → `OptionSheet`) · `ResultRow` (the search-hit **contract** — title · year·studio · in-collection ✓ · report path; worn card-shaped in card-led flows) · `InlineBanner` (thumb-bearing inline warning + action row — the CAT-03 dedup banner; never a toast) · `CardFan` (+`/pick`) (the 3-up swipeable fan: forefront full face + plate, neighbors genuinely visible, **zero plate occlusion** (§1.2 floor), dots + ‹ › + SWIPE + position count) · `CardDetail` (hero card + meta ledger incl. the CAT-09 presence rows + contributor credit + actions) · `EquipReadout` (equipped-cosmetics chips + DESIGNED BY credit + adoption count (CARD-05) + PIXELS cost; sits **above** the cards; dims under CleanPeek) · `CleanPeek` (press-and-hold fades all overlays to bare art; CLEAN VIEW pill) · `ReportSheet` (**`/drawer` ruled app-wide** — F-09 selectable reasons + the required `TextField/area` (moderator-only, **outside MOD-07**) + red **SUBMIT REPORT** (`/destructive`) + cream CANCEL; **three targets** — card · catalog entry (+ the **report-duplicate** reason) · user, which adds the **BLOCK strip** past an OR divider (instant · silent · files nothing, SOC-09); `/modal` and `/takeover` drafted, kept for history; **lifecycle:** dormant → in-flight (keycap held pressed, kin `motion.holdToBuy`) → submitted, plus the offline writes-gate (`OfflineStrip`, SYS-10) and the `Toast` submit-failure — full walk in §2.6) · `ReportConfirm` (the report's post-action seal — `scr.accent` check "we'll take a look", reflecting MOD-02 soft-hide with **no threshold named**; **`/block`** = red ⊘ "blocked", severed · invisible · silent, SOC-09).
**States & feedback (§1.6):** `EmptyState/inviting` · `Skeleton` (solid fills) · `LoadError` ("Signal Lost" + RETRY) · `Unavailable` (terminal — person glyph, no retry, MOD-09) · `Offline` + `OfflineStrip` (calm, SYS-10) · `Toast` (transient failure — full-bleed under-header banner, plain words + orange RETRY).
**Currency & commerce (the Store set, store track 2026-06-12):** the **PIXELS mark** (the Customizer currency's in-app face, ECON-01/02 — pixel-gem v3: flat solid facets, opaque ink outline, solid blue glint pixels; gold = value F-02, blue lives **inside the mark only**; grammar PIXELS · 1 PIXEL · ticker PX; replaces the ◈ placeholder app-wide) · `CurrencyCounter` (gold header keycap → Wallet; `/negative` alert-red, ECON-09; tick moment) · `PriceChip` (+`/big`; the adopt chip "1 [gem]", ECON-03) · `BuyBar` (sheet-bottom row; **hold-to-buy** = the instant-PX spend confirm, OQ-046; bridge + disabled states) · `PackTile` (2×2 grid tile w/ **value math** PX-per-$; `+/starter` one-time spanning tile, OQ-044; **$ prices ride `KeycapButton/secondary`** — the only dollars, ECON-01/06) · `LedgerRow` (signed badge · what · when · gem amount; earn/spend/reversal) · `OwnedTag` / `LockedTag` / `EarnedOnlyTag` (earned = gold-**outlined**, never gold-filled — can't read as a price, COSM-04) · `ItemTile` (sample preview + name + type micro + PriceChip/tag) · `DailyBonusBar` (+1 PX claimed ON the Store, OQ-043; `/claimed` quiet) · `AisleIndex` (THE INDEX category rows) · `PreviewStrip` (gold whole-page theme-preview banner, DEV-04) · `PreviewStage` (preview-on-YOUR-stuff: card / the one pocket device / whole screen; pack contents grid for multi-item packs — sticker packs are SHELL items, OQ-047).
**The editor set (Styler posture, styler track 2026-06-12):** `AttributeSection` (one closed attribute per page swiping beneath the **persistent card hero** — FRAME · EFFECT · FINISH · **NAMEPLATE** · TITLE(font+ink); the carousel grammar, the owner's mandated treatment; the option **tiles** select per F-09) · `SectionChips` (the jump rail; active = **pressed + the `StateMark`** — the F-03 keycap exemption, per the converged board) · `BaseRail` (start-from: DEFAULT · templates · preset kits · 🎲 **Surprise me** · the received-base adopt-then-edit variant — CARD-16/18; rides `GET /games/:id/card-bases`, api 0.16) · `IntensitySlider` (**the catalog's slider** — effect intensity; the value prints into its reconcile row) · `ReconcileSheet` (the CARD-13 keep-gate: acquire all / per-item remove · hold-to-buy · the short-by-N `PackTile` bridge; rides 0017's acquire) · `KeepBeat` (the 0015 **light** celebration tier) — outcomes: SAVE PRIVATE + the **Canvas stub door**; **no Publish** (canvas-tier, decision 0014).
**The Canvas set (open-composition / print-shop board, canvas track 2026-06-13):** the breakout-tier editor (`FlowTakeover` → the diegetic **breakout**, decision 0014 stage 3) — *(none of these existed in §1.5 before this board)* — `CanvasStage` (the **workshop + press-bed**: the device swings open like a cabinet onto a grained bench, the composition lying **bare** on a registration-cornered **press bed** with the thumbnail **safe-area** dashed; the diegetic **shell-swing breakout** is OQ-007's resolution — reduce-motion = a fade, CARD-16) · `AssetShelf`/`ElementTray` (the **ADD bottom-sheet** glyph roster — categories SHAPES/LETTERS/NUMBERS/ICONS/BASE/★ · the all-free vector grid (CARD-02/17, every vector free per 0017) · search + ★ favourites · the BASE solid/gradient row; **REUSES the app's one summoned-drawer grammar** — the same pulled sheet as Collection sort/filter and the Styler drawers, never a second summons; a picked glyph lands as a new pulled slip and counts against the cap) · `LayerRack` + `slip`s (the **Ops dashboard** at the foot: each layer a physical **slip** you **pull to isolate** — the pulled slip edits while the rest ghost to ~28% (CARD-08 solved spatially); a **`cap-meter`** at **30** (OQ-008; gold → alert-red at cap); **Z-order = rack order** (drag a slip L↔R); **long-press → the ops row** RENAME · LOCK · HIDE · DUPLICATE · GROUP · DELETE (CARD-08); the pulled slip carries the `StateMark`) · `editbar` (the relocated **undo/redo** + a scoped **RESET SLIP** (CARD-09) docked **just above the rack** for thumb reach — change 1; dim when nothing to undo) · `NumPop` (the numeric **X/Y coordinate popover** — precision placement readout/nudge, CARD-09) · the **EDIT slip-sheet** (the second drawer up: **opacity** (the `IntensitySlider`) · **fill** + eyedropper (SOLID/GRADIENT) · **stroke/glow/blend** · **flip/radius/dup/delete** (CARD-10); **text slips** add **font · ARC curve · spacing/align/case · screening** (CARD-11/MOD-07); the bed stays in isolation above it, no scrim-dim on the work — the Styler's no-scrim lesson) · the **PROOF** hold-to-preview **size-ladder** (press-and-hold stamps the **true print** — flattened, the Styler's closed attributes live: GILT · EFFECT · FINISH · the RIBBON plate — then renders it at the **GameCard set sizes GRID · MINI · THUMB** so the **thumbnail safe-area** legibility check happens at proof time, change 2; CARD-07/15; works offline — client-side flatten) · the **PRESS** finish-up sheet (the gold PRESS raises the **CARD-19 checklist** — complexity · hash-dedup · premium-owned → the Styler's `ReconcileSheet` + `POST /cosmetics/acquire-batch` when unowned attributes ride — over the true preview; canonical labels **PUBLISH · SAVE PRIVATE · TO THE STYLER**; published prints are immutable, CARD-20; private lands in the switcher + `/me/cards`, OQ-049) · `PrintRitual` (the **OQ-040 first-print three-beat**, full tier: ① the press runs (platen scan-line, client-rendered, never network-bound) · ② the slips fly in (the composition JSON replays the rack in order, the effect ignites last) · ③ the print is lifted off the press, glowing at its largest, bed edges going gold; SHARE (CARD-21) · TO MY SHELF · the NOTIF-04 adoption-ask ride the peak; reduce-motion = crossfade, CARD-16; **full tier only** — first-ever cards · canvas completions · publishes; Styler keeps stay light, 0015). *(Two ruled changes are baked into the converged board: **(1)** undo/redo relocated to the `editbar` above the rack; **(2)** PROOF renders the card at its set sizes. Canvas is the **breakout** tier — `FlowTakeover` + NavBand persist through Add Game/Styler, but PRESS/breakout is the third spatial step, decision 0014.)*
**The Discover set (Two-rooms board, discover track 2026-06-13):** `SegmentedKeycap` (the owed **segmented control** — the binary page-switch **UP NEXT ↔ DISCOVER**; the **`/pair`** form ruled: two keycaps butted, **bottom-docked** in a smaller footer below the scroll / above the `NavBand`, active segment = **pressed + the `StateMark`** (the orange pixel-square — on-screen chrome takes the screen marker, not the pink shell LED; marker audit 2026-06-15) — the screen-side echo of `NavKeycap`, F-03 + the F-09 pressed-keycap exception; F-04 untouched, it never crowds the 5 plastic keycaps) · `QueueRow` (the Up Next **drag-to-reorder** row: grip + rank + `GameCard/thumb` + title/meta + a **source/status tag** — IN COLLECTION · gold **WISHLIST** (unowned, COL-02) · **REC'D BY @x** (SOC-05); a drop-line marks the landing, `PATCH /me/queue/reorder`) · `ReleaseRow` (the **Upcoming** item: `GameCard` + studio + a **future-date chip** + `NotifyToggle`, DISC-01/CAT-08) · `RecRow` (the **friend-recommendation** row: `GameCard/thumb` + who + note + ADD-TO-QUEUE, SOC-05) · `AdoptCount` (the **trending** adoption readout — gold-outlined ▲ count, CARD-05; a clout signal, **never a price**) · `NotifyToggle` (the NOTIFY-ME stateful chip — off → on/NOTIFYING, DISC-01 → NOTIF-01). *(Discover is **non-commerce**: the PIXELS mark stays out — trending shows adoption counts, not prices.)*
**The Settings set (converged board, settings track 2026-06-14):** `Toggle` (the **switch** — a SQUARE two-position keycap-knob: a cream knob with the F-03 drop edge slides L↔R over a FLAT track (F-07 + F-09, never sunken); **ON = `scr.accent` track + knob right · OFF = `scr.grip` + knob left** — the orange fill carries state, no pip (pink stays the LED voice, F-05); `.disabled` dims both. Notification prefs · privacy PROF-03 · the bug-logs opt-in) · `ConfirmSheet` (the **destructive-confirm BOTTOM DRAWER** — the §1.8 destructive-confirm mandate worn as the app's one-drawer grammar, kin to `ReportSheet` (summoned, **no grab handle**): red alert `seal` + plain-words ripple + red `KeycapButton/destructive` + cream CANCEL. **Closes the old "confirm dialog" gap**; **shared with the game-page track's destructive `ConfirmSheet` (OQ-061)** — one component, two uses: account-delete AUTH-07 · card-delete) · `LogAttach` (the **bug-only device-logs opt-in**, SYS-11: a `Toggle` **off by default** + a one-line **consent** note (support-only · may hold personal info · never shown to others) + **room reserved for the TBD OQ-060 payload** — an "INGAME DIAGNOSTIC LOGS · ATTACHED / WHAT'S INCLUDED?" panel with a dashed slot for size+summary once defined) · `FeedbackConfirm` (the calm **support-facing submitted seal** — `scr.accent` check "WE GOT IT", kin to `ReportConfirm`; deliberately not the gold celebration tier) · `TriageCard` (the **type-chooser** card — glyph tile + title + one-liner + chevron; the BUG card flags "+ ATTACH LOGS" before the tap; the C-triage entry). *(Settings is **non-commerce** — no PIXELS. `ListRow`/`RowIcon` get their first heavy composition here; a right-edge **scroll-rail** marks position on long lists. The **feedback SUBMIT is a mid-width `KeycapButton/primary`** — on-theme orange (owner 2026-06-14), not red, so a support send doesn't read as an alarm; the DELETE-account button stays red `/destructive`.)*
**The Game-page set (adaptive hub, game-page track 2026-06-22):** `DualFaceHero` (the PLAY-tab hero pair: THE FACE (`GameCard/grid`, 161×225 2-up) beside YOUR STATS (`GameCard/grid` + `.flipy` — the C5 step mirrored Y so TR+BL carry the diagonal; **CARD-01 back = "CARD ARTIST" + designer name only** — the adoption-count is NOT on the back; it reads in the gallery `AdoptCount` + `CardDetail` inspect, OQ-069/0024) · `PlayStats` (the dossier readout/edit: label/value rows — HOURS · % COMPLETE · STATUS (COL-02 set **minus WISHLIST**, OQ-070/0025) · PLATFORMS · OWNED SINCE · **RATING** (private-only, OQ-058/0024) · NOTES; in EDIT mode rows flip to cream-inset fields, STATUS stacks as a full-width chip set; the **pinned `edit-bar`** (DONE + CANCEL, seated above the `SectionSwitch`) appears only during edit; rides `PATCH /me/collection/:entryId`, COL-02..06) · `CardSwitcher` (COL-06/OQ-056: 3-up `GameCard/cell` grid + tap-select (a thin `scr.accent` stepped ring + gap around the selected card) + an **inline options panel** below the grid (`EquipReadout` · **SET AS MAIN** · **EDIT IN STYLER** · **DELETE** → `ConfirmSheet`); state-tags: EQUIPPED · PUBLISHED · PRIVATE · DRAFT · ADOPTED; a **DESIGN NEW** dashed tile (gold-step, F-02) launches the Styler; **DELETE semantics open (OQ-061)** — the affordance is formalized here, the rules deferred; rides `GET /me/collection/:entryId/cards` + `PATCH /me/collection/:entryId { activeCardDesignId }` + `DELETE /cards/:id` + `POST /cards/:id/unpublish`) · `SectionSwitch` (the PLAY · CARDS · ABOUT bottom rail, docked above the `NavBand` on `scr.tools`; icon inline with label; active = `scr.accent` border + `scr.accent` icon, F-09 — **a border, not a pressed keycap**; PLAY locked = inline padlock icon; the Catalog v0.5 catalogs this as `SectionSwitch`. **Reconcile with `SegmentedKeycap` (0.15) and `SectionChips` (0.13):** those prose voices say "pressed + `StateMark`" but the converged flat boards render accent-border, aligning with the 0.20 flat direction; **app-wide unification → the unified `SectionSwitch`** (one grammar, variants `/pair` · `/chips` · `/rail`; OQ-063 resolved, decision 0030 / the Device-editor set) — this PLAY · CARDS · ABOUT rail is the `/rail` variant) · `FriendContext` (the M7 friend-view module: their `GameCard/grid` face + `.flipy` stats card (their hours · status · owned-since, privacy-gated PROF-03; notes/rating stay owner-only, COL-04/05) + an opt-in **compare entry** panel (`scr.accent`-outlined, per-game YOU vs THEM columns, SOC-11); the compare **detail** is its own screen §4.6 Compare Hours; composes client-side from `GET /users/:id/collection` + `GET /me/collection/:entryId`, decision 0020 — no new endpoint).
**REUSE (no redefinition):** `CommunityGallery` · `CardDetail` · `EquipReadout` · `AdoptCount` (add-game/discover) · `ConfirmSheet` (settings/OQ-061 — one component, two uses: account-delete AUTH-07 · card-delete) · `GameCard` sizes · the §1.6 lifecycle family · `DeviceShell` + `NavBand` · `ReportSheet` · `NowTag`.
*(Game page is **non-commerce on the hub itself** — PIXELS appear only inside `CardDetail`'s `PriceChip` when an adopt decision is made in the community gallery.)*

**The Friends/Social set (converged Friends 3.3 + Find/Add 4.8 boards, friends + find-add tracks 2026-06-23):** `FeedRow`/`ActivityRow` (the SOC-06 **aggregated** activity item: actor `Avatar` + an **actor+type** predicate — *"Riko added 12 games"* as **one capped row**, never a flood — + a capped **object peek** (≤3 `GameCard/thumb`s + "+N", or a single published card, or an achievement-badge tile) + relative time; trivia excluded; quiet-vs-active are states of the one component) · `FriendRow`/`FriendTile` (the SOC-01 roster item: `Avatar` + username + an hours·games `StatPeek` + a COMPARE `chip` → 4.6 + overflow ⋮ → the actions sheet; `Row` = the ALL FRIENDS list, `Tile` = the slim landing rail. **Presence CUT** — no `PresenceDot`, no online/now-playing peek, owner 2026-06-18; the row reads hours + collection size) · `RequestRow` (SOC-08: incoming = `Avatar` + *"@x wants to connect"* + arrival + **ACCEPT**/**DECLINE**-silent; outgoing = + **CANCEL**; the **cooldown** state disables re-request with the reason, SYS-04) · `InviteHook` (the cold-start / find-more CTA: the username `SearchField/in-place` entry + **INVITE LINK** + **QR** keys → 4.8; the no-friends hero + the quiet-feed nudge are built on it) · `PersonRow`/`SearchResultRow` (the 4.8 person surface — `Avatar` + username + the **relationship-state action**, the legible spine: `none`→**ADD** · `outgoing`→**CANCEL** + PENDING · `incoming`→**ACCEPT/DECLINE** · `friends`→open profile + a `scr.accent` ✓ marker · `cooldown`→disabled ADD + reason · `blocked`→limited/dimmed → Settings; rides `GET /users/search` w/ the `relationship` enum, api 0.23) · `QrCard` (the in-person add — a `brand.cream` card QR rendered from the `POST /me/invites` token, SOC-07; QR-gen client-side, OQ-073) · `InviteLanding`/`SenderSummary` (the SOC-10 arrival — a `FlowTakeover` (✕ close top-right, **NavBand untouched**) showing the sender `Avatar` + username + a **prefilled one-tap ADD** that resolves through their Profile; no-app → store listing). **REUSES** the bottom-docked `SearchField/in-place` (username search, OQ-035), the `Avatar` monogram (PROF-08), the `Toast` (request-sent confirm), the summoned-drawer actions `.sheet`, the §1.6 lifecycle family, `DeviceShell` + `NavBand`. *(Friends is **non-commerce** — no PIXELS, **no gold** (adding a friend creates no card, F-02; the lone gold is the achievement-badge `.achv` trophy glyph in the feed — gold-as-trophy per F-02's judgment-call carve-out), **no friend SHARE** (OQ-052), no contacts-matching (SOC-07). Block is a relationship state; its LIST lives in Settings 4.15.)*

**The Compare set (converged Compare Hours 4.6 board, compare track 2026-06-24):** `CompareHeader` (the who-vs-who subject line — your `Avatar` + the friend's + names; opens the head-to-head) · `CompareTotals` (the **totals face-off** — total hours + total games side-by-side, a split **margin bar** ("by how much"), the **who's-ahead** verdict as a `NowTag`-grammar `scr.accent` status pill, + a games sub-face-off; **no PIXELS**) · `ComparePair`/`CompareRow` (the per-**shared**-game **card-vs-card matchup**: their `GameCard/cell` on the game frame **left** · the two hour counts between, **winner in `scr.accent`** + the gap · your `GameCard/cell` on the **slate+foil** frame **right** — same game art, a different card design, the foil finish marks "your" copy; each card **owner-labelled**; **tap a card → the Game page §4.2** for that game — yours or the friend's friend-view, SOC-11) · `FriendsLeaderboard`/`LeaderRow` (the friend-cohort rank by hours/games — `Avatar` + name + value + a quiet relative bar; **your row highlit** with a flat `scr.accent`-tint fill, F-09 — **never a left-edge accent rail**, OQ-078). **REUSES** `GameCard/cell` (96×134, F-01), the `Avatar` monogram (PROF-08), the `#art-*` symbol library + `fr-*`/foil frames (the card visuals), the `.return-link` `TertiaryLink` (the `‹ RETURN TO <friend>` friend-view back-seam), the §1.6 lifecycle family, `DeviceShell` + `NavBand` (FRIENDS active). *(Compare is **read-only** over the friend-visible fields (PROF-03) and **non-commerce** — no PIXELS, **no gold** (comparing creates no card, ECON-01/F-02); **completion % is OUT** (hours + games only). PROF-03 drops the hidden axis cleanly — a friend who hides hours keeps the games face-off, a `lock-well` stands in for the hidden per-game rows.)*

**The Device-editor set (converged Device editor 4.5 board, 2026-06-14; formalized decision 0030):** `SectionSwitch` (**the unified section/segment switch** — one grammar, three **variants**: **`/pair`** the 2-way page-switch (Discover UP NEXT ↔ DISCOVER; the former `SegmentedKeycap`, 0.15) · **`/chips`** the jump rail (Styler attribute sections; the former `SectionChips`, 0.13) · **`/rail`** the bottom-docked icon+label rail (Device SHELL · THEME · STICKERS · LOOKS; the Game-page PLAY · CARDS · ABOUT). Active = **flat F-09 accent-border + the orange `StateMark`**, no pip — the 0.18/OQ-067 tell, already rippled app-wide; `SegmentedKeycap` + `SectionChips` are **retained as the `/pair` + `/chips` aliases** (names append-only, the converged Discover/Styler boards untouched; OQ-063 resolved, decision 0030)) · `SectionCard` (the `/rail` cell — a `scr.tools` card, icon inline with a micro-caps label; the active card carries the accent-border + `StateMark`) · `StickerStage` (the **plastic-only placement surface** — the forehead + chin **decal zones**; the screen + 5 nav keycaps are protected no-go zones the stage refuses, DEV-03/F-04; in the in-frame model the live plastic IS the canvas) · `TransformBox` (the **place/scale/rotate handles** — an accent selection ring + 4 corner scale `.h` handles + a rotate stem + a percent readout; the device-side cousin of the Canvas `sel-ring`) · `PlacedSticker` (a sticker **rendered on the shell**, transformed in its real spot + drop-shadowed onto the plastic — the OQ-045 on-shell preview; the nav keycaps z-order **above** it, never obscured) · `StickerTray` (the library picker — free-baseline `gl-*` glyphs + premium packs (preview-then-acquire) worn as `ItemTile`s) · `SavedLook` (a re-applyable snapshot of **shell + sticker composition + screen theme**, **identified by shell·theme** — no custom name; an **ON NOW** tag when it matches the live device; DEV-05) · `LooksGrid` (the LOOKS section — a grid of `SavedLook` `MiniDevice` thumbnails + the **SAVE CURRENT** tile + a per-tile delete; cap ~12) · `KeepBar` (the **premium-commit tally** — a pinned bar accruing previewed-but-unowned shell/theme/stickers across sections → **KEEP** → the `ReconcileSheet`'s `POST /cosmetics/acquire-batch`; the device-side echo of the Styler reconcile-at-KEEP, CARD-13). **REUSES** `DeviceShell` + `NavBand` (PROFILE active — the frame is the live subject), `FlowTakeover` + `FlowHeader` (reached from Profile's MY DEVICE strip), `PreviewStrip`/`PreviewStage` (the DEV-04 whole-page theme preview), `ItemTile`/`AisleIndex`/`OwnedTag`/`LockedTag` + the `ReconcileSheet`/`PriceChip`/`CurrencyCounter`/PIXELS commerce set, the §1.6 lifecycle family. *(Device decoration is **personal-only** — never published/adopted; the editor is **commerce-adjacent** — PIXELS in play for premium packs.)*

**The Contributor-profile set (converged "My Contributions" 4.9 board, 2026-06-26; formalized decision 0032):** a **Profile-grammar recomposition** for the contributor lens (CAT-07) — it mints essentially no net-new component, reskinning the Profile page to the contribution facts. Only genuinely new piece: **`SectionEmpty`** (the **per-section** empty nudge that sits *inside* an otherwise-populated profile when one section is bare — a **gold acquisitive `DESIGN A CARD`** variant for CARDS DESIGNED (F-02, card-creating) and a **neutral `ADD A GAME`** variant for GAMES ADDED; distinct from the whole-screen `EmptyState` of the never-contributed P2). The **CAT-10 contributor standing** is **not a new component** — it is the existing **`PctPill`** (PROF-07, `brand.gold`) worn on the contributor `StatTile`s (GAMES · ADOPTIONS · GAMES-by-reach percentile vs the *contributor cohort*; **threshold-gated** SYS-04 + **privacy-gated** PROF-03 — every tile renders cleanly without its chip). **REUSES** `DeviceShell` + `NavBand` (the **viewer's own** band; reached from a Profile's Contributions gateway, CAT-07), `IdentityBlock`, the **boxless `StatTile`** row (4-up: GAMES · CARDS · ADOPTIONS · **REACHED** = collections-reached; the de-welled treatment shared with §2.2), the **PINNED-FAVOURITE hero** grammar (`GameCard/hero` + a `NowTag`-style **MOST ADOPTED** tag + `KeycapButton/secondary` VIEW CARD) for **SIGNATURE CARD**, `GameCard/cell` + `RankChip` for the **CARDS DESIGNED** grid (legibility over the Profile `/mini` TOP-5), the `Strip` row (`GameCard/thumb` + game title + "IN N COLLECTIONS" reach + chevron → Game page §4.2) for **GAMES ADDED**, `SectionHeader` + `TertiaryLink` (the **VIEW ALL ›** escaped onto the section head, opening the paginated full-list views), the **PROF-03 lock-well** (privacy-limited), and the §1.6 lifecycle family. *(Read-only, **non-commerce** but for the gold `PctPill`/`SectionEmpty` value-markers; the VIEW ALL list views reuse `GameCard/cell`+`RankChip` (all cards, adoption-sorted) and the `Strip`/reach row (all games), cursor-paginated.)*

**Overlay grammar (the "sheet" disambiguation, R5 audit).** Within the *sheet* tier there are two bottom-anchored forms: a **pulled sheet** has a **grab handle** (dragged up — the Collection sort/filter, the Store item-detail family) · a **summoned drawer** has **no handle** (raised by an action, dismissed by CANCEL/`scrim`). The `*Sheet` component suffix — `ReportSheet` · `ConfirmSheet` · `ReconcileSheet` · `OptionSheet` — names the **summoned-drawer** family; the pulled surface is the unnamed `.sheet`.
**To design (Phase C/D gaps):** card back *(when designed: printed provenance = **DESIGNER ATTRIBUTION ONLY** — "CARD ARTIST" + name; the **adoption count is NOT on the back**, it reads in the gallery `AdoptCount` + the `CardDetail` inspect — CARD-01, decision 0024 / OQ-069)* · sticker placed-on-shell preview (OQ-045) · hold-to-buy a11y alternative (OQ-046) · `motion.cardFlip`/`powerOn`/`celebration`. *(`Toggle` + the destructive **`ConfirmSheet`** closed by the Settings set; **numeric input** (the `NumPop`) + the **celebration moment** (OQ-040, the Canvas `PrintRitual`) closed by the Canvas set.)*

### 1.6 States & feedback (the §1.8 pattern, visual side)
Every screen's load/empty/error cells resolve to one family (OQ-037; behavior in design-req §1.8). All share the **dashed stepped-card silhouette** so they read as kin, split by intent:
- **Retryable** — `LoadError` "Signal Lost" (network/timeout/5xx): accent **!** + orange `KeycapButton/action-alt` **RETRY**.
- **Terminal** — `Unavailable`: muted grey person-glyph, **no retry**, GO BACK. MOD-09 non-disclosure — blocked/suspended/deleted all collapse here, indistinguishable. **Lone exception:** your *own* block shows **Unblock**.
- **Offline** — `Offline`/`OfflineStrip` (SYS-10): calm wifi-off glyph, **auto-recovers**, never an alarm; your own data stays read-only-usable, writes gated.
- **Loading** — `Skeleton`: **solid** fills (not dashes), so loading never reads as an invitation.
- **Empty** — `EmptyState/inviting`: a doorway, never a dead end (design-req §1.7).
- **Transient** failures (save/publish/purchase) → `Toast`: a **full-bleed banner sliding under the header** — what failed + a plain-words reassurance ("nothing was charged — your pixels are safe") + orange `KeycapButton/action-alt` RETRY where safe to repeat. *(Designed on the Store board; reusable verbatim by every transient write.)*
- **State-title type (F-06 conformance, decision 0024 / OQ-066):** the error/empty **state-titles** (today drawn at the off-scale 17px across ~7 boards) snap to **emphasis 15** — centered small-card titles read as 15, not display 21. Conform on the sweep (below); F-06 is not amended.

### 1.7 Inputs & keyboard (OQ-035)
Text entry uses the **system keyboard** — `keyboardAppearance` matched to the active `scr` theme (dark themes → dark keyboard), the focused field **riding just above** it. The nav band may be occluded while typing; that's accepted (typing is a focused sub-task; dismissing the keyboard returns to it). This keeps autocorrect / swipe / dictation / i18n / screen-reader support — the "stay conventional in forms" half of the art-direction split (design-req §1.1). A device-skinned **in-app keycap keyboard** (maximum metaphor) was considered and **deferred** for its autocorrect/swipe/dictation loss + i18n/a11y cost. Goes live with **Add Game**, the first heavy text surface. Field components themselves are §1.5 gaps.

---

## Part 2 — Screens (composition by name)

### 2.1 Collection (3.1)
**Compose.** `DeviceShell` + `NavBand` (Collection active). `ScreenHead`("COLLECTION", `CountKeycap`). Now-Playing hero: `GameCard/hero`(+custom) + meta (`type.micro` NPL label, `type.display` title, `type.body` sub) + `KeycapButton/primary` LOG HOURS; **unset → nudge** ("set your Now Playing" → pick from collection, WTP-03). `ToolsBar`: grip, `ToolKeycap`×4 (search · sort+`StateMark` · filter · view), spacer, `KeycapButton/primary` ADD (gold + 2/4 step, F-02).
**View modes (COL-07, decision 0012):** the `view` keycap cycles **shelf** (hero rows + per-game stats eyebrow, OQ-033) ↔ **compact grid** (`GameCard/grid`, full faces, no added labels) ↔ **dense list** (`Strip` rows: thumb + title + hours/status — the densest scan). Now-Playing hero persists in all three.
**Card peek-flip (COL-12, decision 0026 — reverses OQ-059/0025).** In **shelf + grid**, a card **flips in place to its CARD-01 stats back** (`GameCard` + `.flipy` — **game title** (header, above the rows — the flip hides the front plate) + HOURS · COMPLETE · STATUS · SINCE + "CARD ARTIST {name}" (the designer name sized to the label — a quiet, uniform credit); the game-page `DualFaceHero` back reused) — a stats peek without leaving the screen. **Tap** flips; **tap the back** returns; the back carries a **`KeycapButton/secondary` VIEW GAME ›** → the Game page (4.2); **long-press** = shortcut to the page. **Dense-list is excluded** (its rows already print hours/status; a row-tap stays open-page). The peek is **transient** (local UI; resets on leave / view-switch) via `motion.cardFlip` (reduce-motion → instant cross-fade, no rotation). **No on-face indicator** (owner); discoverability is a **first-run coachmark** (CARD-16); the flip is a **tap** (non-gesture path) with a screen-reader "flip to stats" action. **Friend-view (COL-10/11):** works, **privacy-gated** — the back shows friend-visible fields only (hours · status · owned-since + `designer`; notes/rating render the 🔒 PRIVATE row, COL-04/05/OQ-058); VIEW GAME → their friend-view Game page (M7). **Board:** the COL-12 peek-flip stage (grid · shelf · friend) lives in the canonical `collection-states.html` (the variant was folded in + retired, 2026-06-24).
**Tools-bar model (OQ-034):** keycaps *act*, the drawer *configures* — tap search → in-place `SearchField` live-filtering the view (+`MatchTag` on dev/publisher hits, COL-09); tap sort → flip ASC/DESC; tap view → cycle modes; **filter** (multi-choice) opens the pulled-up sort/filter sheet; long-press any keycap opens the sheet at that section. One shared query state between in-place search and the sheet.
**Manual order (OQ-031):** an **Arrange** mode entered from the sort sheet (long-press-drag); manual order is saved as one more sort choice.
**Friend-view (COL-10, decision 0012):** read-only; your own chrome by default + a **"view in their device"** toggle (obvious exit); reached from a friend's Top-5.
**Friend browse-tools (COL-11, decision 0021):** the friend view carries **full read-only browse-tool BAR PARITY** with your own — the `ToolsBar` keeps **search · sort + `StateMark` · filter · view** (all three view modes; one shared query state), so reading someone else's shelf isn't a poorer experience than reading your own. Two reading affordances follow: the **sorted-by-hours storytelling list** (LIST mode + HOURS↓ answers "what has she sunk time into?"; each row carries **her** hours · status · **GOT IT 'YY** owned-since, newly friend-visible per SOC-11; OWNED SINCE tells the other story) and the **read-only sort/filter drawer** — the same pulled drawer as your own (scoped search title·dev·publisher · the three views · sorts · status + genre filters) **over the friend-visible field set only** (no notes/rating/platforms sort). The **write tools stay owner-only:** **no Arrange / no MY ORDER**, **no per-entry edit**, no LOG HOURS — DONE applies to *your* view of her library only. Tapping a row opens that game's friend-view (her card + stats · opt-in compare · adopt, SOC-11).
**State matrix:** populated (3 modes) ✓ · first-run empty (inviting first-add, AUTH-06) ✓ · Now-Playing-unset nudge ✓ · loading `Skeleton` ✓ · `LoadError`+RETRY ✓ · `OfflineStrip` read-only ✓ · friend-view + chrome toggle ✓ · **friend browse-tools — bar parity + sorted-by-hours list (COL-11)** ✓ · **friend read-only sort/filter drawer — no Arrange (COL-11)** ✓ · friend unreachable (`Unavailable`/calm offline) ✓ · **card peek-flip — shelf/grid back + VIEW GAME (COL-12)** ✓ · **friend peek-flip (privacy-gated back)** ✓.

### 2.2 Profile (3.5)
**Compose.** `DeviceShell` + `NavBand` (Profile active). `ScreenHead`("PROFILE", `ToolKeycap` **settings/gear** — privacy is *not* here; it lives in Settings, decision 0011). `Well`: `IdentityBlock` (`Avatar`+`DesignBadge` PROF-08, name `type.display`, sub, bio, `GTag`s). `SectionHeader` STATS → flat `StatTile`×6 **on the screen plane** (no enclosing `Well` — the well's inner pad zeroed so the tiles sit **gutter-aligned**, sharing the screen-gutter edge with TOP 5 and the identity/strip wells rather than inset; + optional `PctPill`, PROF-07 — every tile renders cleanly without its chip). `SectionHeader` PINNED FAVOURITE → hero row (`GameCard/hero` + meta + `KeycapButton/secondary` VIEW GAME). `SectionHeader` TOP 5 (+`TertiaryLink`) → `GameCard/mini`×5 + `RankChip`s. NOW PLAYING → `Strip`. MY DEVICE → `Strip` w/ `MiniDevice`. `Well` of `ListRow`s (Achievements · Contributions gateways).
**Edit mode:** in-place (identity + curation, PROF-01/02/06/08); Top-5 re-rank via the OQ-031 drag.
**Friend-view (PROF-05, decision 0012):** adds friend count + mutual friends, `KeycapButton/action-alt` ADD FRIEND + COMPARE, overflow Report/Block; hides edit/management; your chrome default + the "view in their device" toggle. **Privacy-limited** non-friend = reduced card. **No Share** — sharing is self-view only (your invite link, SOC-07; the friend-view chip was cut, decision 0019 / OQ-052).
**State matrix:** self populated ✓ · edit mode ✓ · fresh account (inviting empties, chip-less tiles) ✓ · friend-view + chrome toggle ✓ · privacy-limited ✓ · loading `Skeleton` ✓ · `LoadError`+RETRY ✓ · terminal `Unavailable` (MOD-09; Unblock exception) ✓ · offline (own from cache read-only · friend unreachable calm) ✓.

### 2.3 Store (3.4 · 4.11 · 4.12) — converged board, store track
**Compose.** `DeviceShell` + `NavBand` (Store keycap **permanently gold**; active = pressed + `PipLight`). `ScreenHead`("STORE", `CurrencyCounter`). **Browse:** `DailyBonusBar` (claim +1 PX in-store, OQ-043) → the drop cover (ECON-08) → dense 3-up `ItemTile` grid → **THE INDEX** (`AisleIndex` rows — the full COSM-01 taxonomy incl. **DEVICE SHELLS** (OQ-042) and **PIXELS → Top Up**; COSM-02: the free baseline is not stocked). **Detail = the pulled-up sheet family** — the drop page, single items, and multi-item pack contents are all the same drawer: `PreviewStage` previews on **your own stuff** (your card · the one pocket device wearing a new shell · the **whole page** for screen themes, under `PreviewStrip`) · title row + `PriceChip` · `BuyBar` with **hold-to-buy**.
**Money path:** can't-afford → the **in-sheet bridge** (`PackTile` minis at the point of intent; the native IAP sheet runs **inline**, BUY re-arms in place) · **Top Up** landing (2×2 `PackTile` grid + `PackTile/starter` + per-tile **value math**; `↺ Restore`, OQ-041) · success = **the landed moment** (centered +N · arithmetic · one gold rule · `motion.counterTick`) · failure `Toast` · restore quiet.
**Wallet:** balance hero + `LedgerRow` history (grant · daily claim · adoption · pack · acquire · **refund reversal**) + the **ECON-09 negative variant** (alert-red hero + counter); reached everywhere via `CurrencyCounter`, which the Wallet header drops (the hero IS the counter).
**Ownership:** `OwnedTag` (price never returns) · `LockedTag` (a date, never a price — the drop-window lock) · the **earned-only trophy module** with `EarnedOnlyTag` (COSM-04, never purchasable).
**State matrix:** browse + daily claim (+claimed) ✓ · drop drawer ✓ · item sheets (finish · screen theme · device shell · sticker-pack contents) ✓ · bridge ✓ · Top Up + native IAP sheet ✓ · landed moment / `Toast` failure / restore ✓ · wallet + negative ✓ · ownership ✓ · `Skeleton` ✓ · `LoadError` + RETRY ✓ · `Offline` (browse-from-cache, **writes gated**) ✓ · sticker placed-on-shell preview **deferred** (OQ-045).
**Source:** `store-states.html` (P1–P12 + P1b/P2b). Behavior questions raised → OQ-041..OQ-044, OQ-046, OQ-047 (the inbox; spec-owner batch).

### 2.4 Add Game (4.1) — converged board, add-game track
**Compose.** `FlowTakeover` (entered from Collection's gold ADD) + `FlowHeader` (✕ · "ADD GAME" · `CountKeycap`). **Search:** `SearchField` **bottom-docked**; results = a 3-up `CardFan` in the upper screen; the **focused card's name/meta above the fan** (title `type.emphasis` + year · studio + the **CAT-09 presence line** "IN N COLLECTIONS · N FRIENDS HAVE IT"); dots/SWIPE then the NONE OF THESE hook beneath the cards; suggestions (recently-added · popular · friends) fill the pre-query state — never blank. **Tap a card → `CardDetail`:** hero card + meta ledger (release · studio · publisher · genres + the CAT-09 rows) + contributor credit (CAT-05) + gold-step **ADD TO COLLECTION** + ⋮ → `ReportSheet/drawer` (MOD-01 incl. the required details note). **Create path** (no results): a blank-plate card materializes and the form **writes onto the title plate** — `TextField` NAME + `SelectField`s GENRE(S)/STUDIO/PUBLISHER/DATE; thumb-bearing `InlineBanner` dedup (CAT-03). **Status:** the six COL-02 chips **off-card** (no stamps on art). **Card step:** `CardPicker` worn as `CardFan/pick` — `EquipReadout` above the cards; `CleanPeek`; the **never-blank be-first rail** (DEFAULT · template · SURPRISE ME — CARD-16/18); **STYLE IT** (gold step → the Styler, 0014 stage 2) + SKIP; publishing is canvas-tier (0014). **Filed:** the celebration beat — the card is the hero (lit, landed), the `CountKeycap` **+1 inset tick**, the contributor first-credit strip, **ADD & CONTINUE** the sole action (OQ-040's light tier).
**State matrix:** entry/suggestions ✓ · typing (keyboard up) ✓ · results + own-it ✓ · card detail + report ✓ · no-results → create ✓ · create + dedup ✓ · status ✓ · card step (+adopted swap · be-first) ✓ · variant detail + peek ✓ · filed celebration ✓ · loading `Skeleton` ✓ · `LoadError` + RETRY ✓ · offline **writes-gate** (SYS-10) ✓.
**Source:** `mockups/add-game/add-game-states.html` (decisions 0014–0016 · six owner passes · ReportSheet variation A ruled).

### 2.4b Game page (4.2) — adaptive hub — converged board, game-page track
**Compose.** `DeviceShell` + `NavBand` (keycap active = the entry surface — e.g. Collection keycap when reached from the collection). `ScreenHead` ("GAME"; `NowTag` if Now Playing; `ToolKeycap` ⋮ overflow → `ReportSheet/drawer`, MOD-01). `SectionSwitch` (PLAY · CARDS · ABOUT) bottom-docked above the `NavBand`.
**PLAY — owned resting (M1).** `DualFaceHero`: THE FACE (`GameCard/grid`, 161×225, `fr-*` frame, custom art) side-by-side with YOUR STATS (`GameCard/grid` + `.flipy` — mirrored Y step; **CARD-01 back prints "CARD ARTIST" + designer name only** — adoption-count lives in the gallery `AdoptCount` + `CardDetail` inspect, not on the back, OQ-069/0024). Below: game title (`type.display`) + studio · year · genre facts-line (`type.body`) + CAT-09 presence stats (`pstat` tiles: IN N COLLECTIONS · N FRIENDS HAVE IT). `PlayStats` dossier readout (label/value rows: HOURS · % COMPLETE · STATUS · PLATFORMS · OWNED SINCE · **RATING** (private-only, OQ-058/0024) · NOTES — all owner-only). **LOG HOURS** `KeycapButton/primary`.
**EDIT STATS (M2).** The `PlayStats` dossier rows flip to inline cream-inset fields; STATUS expands to a full-width stacked chip set — the COL-02 set **minus WISHLIST** (OQ-070/0025). A **pinned `edit-bar`** above the `SectionSwitch` carries `KeycapButton/primary` DONE + `KeycapButton/secondary` CANCEL. Rides `PATCH /me/collection/:entryId` (COL-02..06).
**CARDS — switcher (M3, COL-06/OQ-056).** `CardSwitcher`: 3-up `GameCard/cell` grid. Tap-select = a thin `scr.accent` stepped ring + gap around the selected card. The selected card's **inline options** surface below the grid — `EquipReadout` (CARD-22) + **SET AS MAIN** (rides `PATCH /me/collection/:entryId { activeCardDesignId }`) · **EDIT IN STYLER** → the Card editor (4.3) · **DELETE** → `ConfirmSheet` (OQ-061 semantics open). A **DESIGN NEW** dashed tile (gold-step, F-02) launches the Styler. State tags: EQUIPPED · PUBLISHED · PRIVATE · DRAFT · ADOPTED. Rides `GET /me/collection/:entryId/cards`.
**CARDS — community gallery (M4).** `CommunityGallery`: `GET /games/:gameId/cards` roster — 3-up `GameCard/cell` grid; each card carries `AdoptCount` + DESIGNED-BY credit + `PriceChip` (or FREE chip). Tap → `CardDetail` (hero card + `EquipReadout` readout + CAT-09 presence rows + contributor credit + **ADOPT** `KeycapButton/add` → adopt-confirm beat). Rides `POST /cards/:id/adopt` (ECON-03/04; CARD-22).
**ABOUT (M5).** Game facts: release · studio · publisher · genre `GTag`s + CAT-09 presence stats + contributor credit line (CAT-05).
**Neutral / unowned (M6).** No owned card or dossier — ghost-card placeholder. PLAY tab locked (inline padlock). CARDS tab = community gallery only (no switcher). Primary action: `KeycapButton/add` **ADD TO COLLECTION**.
**Friend-view (M7, SOC-11, decision 0020).** `FriendContext`: their `GameCard/grid` face + `.flipy` stats card (their hours · status · owned-since — privacy-gated PROF-03; notes/rating stay owner-only, COL-04/05). Below: the opt-in **compare entry** panel (`scr.accent`-outlined — per-game YOU vs THEM columns; the compare **detail** is §4.6 Compare Hours). Compose client-side from `GET /users/:id/collection` + `GET /me/collection/:entryId` (decision 0020 — no new endpoint). Primary: **ADOPT** their card; or **ADD TO COLLECTION** if unowned. **Privacy gate** (PROF-03): restricted view = partial + padlock note.
**Upcoming / be-first (M8).** Not-yet-released: `upcoming-badge` (gold date chip) + ghost-card placeholder + **NOTIFY** `NotifyToggle` (DISC-01). In-catalog but no community cards: **BE FIRST** dashed invite slot (ADD TO COLLECTION + DESIGN A CARD).
**State matrix:** M1 PLAY resting ✓ · M2 EDIT dossier form + pinned DONE/CANCEL bar ✓ · M3 CARDS switcher (tap-select + inline options + DESIGN NEW) ✓ · M4 community gallery → `CardDetail` + adopt-confirm ✓ · M5 ABOUT ✓ · M6 neutral (PLAY-locked, ghost-card) ✓ · M7 friend dual-face + privacy-gate + compare entry ✓ · M8 upcoming/be-first ✓ · L1 loading `Skeleton` ✓ · L2 `LoadError` "Signal Lost" + RETRY ✓ · L3 `Offline` writes-gated + error→`Toast` (SYS-10) ✓ · L4 soft-hidden/reported (MOD-02/08/09) ✓.
**Source:** `mockups/game-page/game-page-states.html` (converged 2026-06-17: candidate B, the dossier merge; 8 core artboards M1–M8 + lifecycle L1–L4; flat Scanline-Energize keycaps, F-03/0.20; `.btn.cream` secondary modifier, Bugfix #4). **API page-audited (no gap):** all drawn affordances are backed — `PATCH /me/collection/:entryId` (dossier: status/hours/%/ownedSince/rating/notes/platformIds/activeCardDesignId, COL-02..06) · `DELETE /me/collection/:entryId` · `GET /me/collection/:entryId/cards` (switcher) · `GET /games/:gameId/cards` (community gallery; each card carries `equipped` readout, CARD-22) · `POST /cards/:id/adopt` · `DELETE /cards/:id` + `POST /cards/:id/unpublish` (switcher DELETE, semantics → OQ-061) · `GET /users/:id/collection` (friend-view, decisions 0020/0021) · `GET /catalog/games/:id` (shared page + CAT-09); friend-view detail + opt-in compare compose client-side (decision 0020 — no new endpoint). **No gap — api-contract unchanged.**

### 2.5 Card editor — Styler posture (4.3, stage 2 of decision 0014) — converged board, styler track
**Compose.** In-frame (`FlowTakeover` tier; the breakout belongs to the Canvas posture, stage 3). The **card hero is persistent**; the five closed attributes ride **`AttributeSection` pages swiping beneath it**, with **`SectionChips`** to jump — FRAME · EFFECT · FINISH · **NAMEPLATE** (the plate object: SLAB · RIBBON · BEVEL · premium HOLO PLATE — the hero's plate notches live; OQ-039 ruled, decision 0018) · TITLE (**font + ink**, CARD-11). **Start-from:** `BaseRail` (DEFAULT · templates · kits · 🎲 Surprise me + the received-base variant — never blank, CARD-16/18). **Premium:** preview-then-acquire with the on-card PREVIEW flag + cost-chip stack (CARD-06/13) → **`ReconcileSheet`** at KEEP (acquire all / per-item remove · hold-to-buy · the short-by-N bridge). **Effect intensity:** `IntensitySlider` (prints into the reconcile row). **Outcomes:** KEEP (= save-private + set active, api 0.16) → **`KeepBeat`** (the light tier, 0015) · SAVE PRIVATE · the **Canvas stub door** — **no Publish** (canvas-tier, 0014).
**State matrix:** start-from ✓ · each attribute section (incl. NAMEPLATE P5b) ✓ · mid-swipe ✓ · premium preview + reconcile (+bridge) ✓ · keep beat ✓ · loading `Skeleton` ✓ · `LoadError` (draft-safe copy, CARD-14) ✓ · `Offline` **draft-safe** (free/owned work continues from cache; premium acquires + KEEP **writes-gated**, SYS-10) ✓.
**Source:** `mockups/styler/styler-states.html` (gate ruling 2026-06-12: carousel won; +NAMEPLATE, overlay cut). Open: OQ-048 (intensity scope) · OQ-049 (save-private landing).

### 2.5b Card editor — Canvas posture (4.3, stage 3 of decision 0014) — converged board, canvas track
**Compose.** The **breakout** tier (decision 0014's third spatial step, past sheet · takeover): EDIT ART crosses the posture seam and the device **swings open like a cabinet** onto the workshop — the `CanvasStage` (a grained **bench** + the registration-cornered **press bed**, the composition lying **bare** = base + vector layers; the diegetic shell-swing breakout resolves **OQ-007**). Three buckets ride the bench: **ADD** raises the `AssetShelf`/`ElementTray` (the app's one summoned drawer — categories · the all-free vector roster · search + ★ · the BASE solid/gradient row, CARD-02/17 + 0017) → a picked glyph lands as a new pulled slip. **EDIT** works the `LayerRack`: each layer is a physical **slip** you **pull to isolate** (the rest ghost to ~28%, CARD-08); long-press → the ops row (RENAME · LOCK · HIDE · DUPLICATE · GROUP · DELETE); **Z-order = rack order** (drag L↔R); the `cap-meter` holds the line at **30** (OQ-008). The pulled slip raises the **EDIT slip-sheet** — opacity (`IntensitySlider`) · fill/eyedropper (SOLID/GRADIENT) · stroke/glow/blend · flip/radius/dup/delete (CARD-10); **text slips** add font · ARC · spacing/align/case · screening (CARD-11/MOD-07). The relocated **`editbar`** (undo/redo + scoped **RESET SLIP**, CARD-09) docks just above the rack. **PROOF** is the hold-to-preview size-ladder (the true print — flattened, the Styler's closed attributes live — at GRID · MINI · THUMB, verifying the thumbnail safe-area at proof time, CARD-07/15). **PRESS** raises the finish-up sheet (the CARD-19 checklist — complexity · hash-dedup · premium-owned → `ReconcileSheet` + acquire-batch — then **PUBLISH · SAVE PRIVATE · TO THE STYLER**; published prints immutable, CARD-20; private → switcher + `/me/cards`, OQ-049). Publish fires `PrintRitual` (the OQ-040 three-beat, full tier).
**Two ruled changes (baked into the board).** **(1)** Undo/Redo (+ RESET SLIP) **relocated to the `editbar` above the rack** (thumb reach, off the far top corner). **(2)** **PROOF renders the card at its set sizes** (GRID · MINI · THUMB) — the legibility check is part of the proof, not a post-publish surprise.
**State matrix:** entry/breakout (P1, OQ-007 diegetic yield) ✓ · slip isolation + relocated undo/redo (P2) ✓ · ADD sheet (P3) ✓ · EDIT slip-sheet incl. the text-slip fragment (P4) ✓ · the rack in full — ops + precision + the cap (P5) ✓ · PROOF + the size-ladder (P6) ✓ · PRESS finish-up + reconcile (P7) ✓ · the first-print ritual (P8, OQ-040) ✓ · loading `Skeleton` ✓ · `LoadError` (draft-safe, CARD-14) ✓ · `Offline` — editing + PROOF from cache (client-side flatten), PRESS/PUBLISH + premium acquires **writes-gated** (SYS-10) ✓.
**Source:** `mockups/canvas/canvas-states.html` (gate ruling 2026-06-13: **C "print shop" won** + the two ruled changes; P1–P8 + lifecycle P9–P11). **API page-audited (no gap):** every write already exists — `POST /cards` draft · `PATCH` autosave · publish · save-private · `POST /cosmetics/acquire-batch` · `GET /cards/assets` · `GET /cards/:id/share-image` (api 0.16/0.18). The cap-30 (OQ-008) and save-private (OQ-049) are **product-spec-only** (resolved 0019) — no contract surface. **Resolved here:** OQ-007 (breakout) + OQ-040 (first-print ritual), design-side.

### 2.6 Report (4.16) — converged board, report track
**Compose.** A summoned `ReportSheet/drawer` (OptionSheet family — the ⋮ overflow pressed beneath a dim `scrim`, **no grab handle**) riding any host screen; **one grammar across three targets** — published `GameCard` · catalog entry (`CardDetail`) · user (`IdentityBlock`): flat F-09 reason rows (`scr.accent` border + the `StateMark`) · the required-details `TextField/area` disclosing **inline beneath a reason that needs specifics** (MOD-01; moderator-only, **outside MOD-07**) · red `KeycapButton/destructive` **SUBMIT REPORT** + cream CANCEL (destructive-adjacent → §1.8 confirm). The **catalog** target carries the **report-duplicate** reason (no note — merge to the original); the **user** target adds the **BLOCK strip** past an OR divider — an outlined alert strip (block glyph), *not* a keycap and *not* a reason: instant · silent · severs · mutually invisible · managed in Settings (SOC-09).
**Lifecycle.** dormant SUBMIT (no reason · or required note empty) → **in-flight** (SUBMIT held pressed, kin `motion.holdToBuy`; CANCEL muted) → **`ReportConfirm`** (the calm in-sheet seal — `scr.accent` check, "we'll take a look"; reflects MOD-02 **soft-hide without naming a threshold**) · the post-block **`ReportConfirm/block`** (red ⊘ seal — severed · invisible · silent · MANAGE IN SETTINGS) · **offline** writes-gate (`OfflineStrip` + gated SUBMIT — never an alarm, SYS-10) · submit-failure → `Toast` + RETRY (§1.6 transient).
**State matrix:** launch/picker (card) ✓ · required-details empty → dormant (catalog, MOD-01) ✓ · report-duplicate armed (catalog) ✓ · in-flight ✓ · filed `ReportConfirm` (card, MOD-02) ✓ · user report + BLOCK alongside (SOC-09) ✓ · post-block `ReportConfirm/block` ✓ · submit-error `Toast` + RETRY (§1.8) ✓ · `Offline` writes-gate (SYS-10) ✓.
**Source:** `mockups/report-sheet/report-states.html` (Variation A ruled; `report-sheet-drafts.html` B/C + Add Game P3b kept for history). **API page-audited** (no gap): `POST /reports {targetType: card|game|user, reason, details?}` incl. "duplicate" + `/me/blocks` (SOC-09).

### 2.7 Discover (3.2) — converged board, discover track
**Compose.** `DeviceShell` + `NavBand` (Discover keycap active = pressed + `PipLight`). **Two rooms** under a `SegmentedKeycap/pair` (UP NEXT ↔ DISCOVER) — **bottom-docked** in a smaller footer below the scroll, above the `NavBand`; a standard `ScreenHead` names the **active page** (UP NEXT / DISCOVER), **no `CountKeycap`** (owner ruling 2026-06-13 — the header names the sub-page, the toggle switches, the plastic keycap is the tab; this resolved the tab/segment "Discover" echo).
**Up Next (WTP-01/02/03).** The single **Now-Playing pin** (a `GameCard`-led hero + `NowTag` + `KeycapButton/primary` LOG HOURS, `PUT /me/now-playing`) atop the ranked **`QueueRow`** queue — owned **+ unowned** (unowned = `WISHLIST`, COL-02), **drag-to-reorder** (drop-line, `PATCH /me/queue/reorder`); rows source-tagged (IN COLLECTION · WISHLIST · **REC'D BY** a friend, SOC-05). The primary build control is `KeycapButton/add` **ADD FROM COLLECTION** (owner: "you control it by adding games from your collection"); unowned ride along as wishlists / friend-recs.
**Discover.** Three sections — **Upcoming** (`ReleaseRow`s + `NotifyToggle`, DISC-01/CAT-08 → NOTIF-01) · **Friend recommendations** (`RecRow`s, SOC-05) · **Trending** (`GameCard+custom` + `RankChip`(/first) + `AdoptCount` + DESIGNED-BY credit, DISC-04/CARD-05 — **non-commerce: adoption counts, never prices; no PIXELS mark**). **Search** (DISC-03): the `ScreenHead` magnifier `ToolKeycap` opens the bottom-docked `SearchField/in-place` over `ResultRow`s (+`MatchTag` dev/publisher hit · in-collection ✓ · ⋮ → `ReportSheet/drawer`). **Tap-through → `CardDetail`** (CAT-09 presence rows + contributor credit (CAT-05) + rec credit + `KeycapButton/add` ADD TO COLLECTION + UP NEXT; an **upcoming** entry swaps ADD → **NOTIFY**; routes → Game page 4.2). **Cold-start:** the thin catalog → the **Contributor hook** (`EmptyState/inviting` + `RankChip/first` "BE FIRST" → Add Game, CAT-02).
**Browse-By parked.** Genre/studio browse (DISC-02) is **not surfaced** on Discover for now (owner ruling 2026-06-13) — disposition (park · relocate to the Game page 4.2 tappable genre/studio · cut) is **OQ-057**.
**State matrix:** Up Next populated ✓ · reorder (drop-line) ✓ · empty (inviting first-run) ✓ · Discover landing (3 sections) ✓ · `CardDetail` ✓ · search active (bottom-docked) ✓ · cold-start Contributor hook ✓ · loading `Skeleton` ✓ · `LoadError` + RETRY ✓ · `Offline` (Upcoming/Trending **browse-from-cache**; the writes — NOTIFY · add-to-queue · reorder · now-playing — **gated**, SYS-10) ✓.
**Source:** `mockups/discover/discover-states.html` (gate ruling 2026-06-13: **A "Two rooms" won** + the seven owner edits — bottom/smaller toggle · standard header · real games · Browse-By cut · simplified pages; drafts a/b/c + the card-fan variant `discover-states-fan.html` kept for history). **API 🔶 page-audit owed:** OQ-053 (notify-me endpoint) · OQ-054 (`GET /me/queue` read-shape: source + recommender) · OQ-055 (`GET /discover/trending-cards` item shape) · OQ-057 (DISC-02 disposition).

### 2.8 Settings (4.15) — converged board, settings track
**Compose.** A `FlowTakeover` from Profile's `ScreenHead` gear (`FlowHeader` ◂ "SETTINGS"; frame + `NavBand` persist, **PROFILE keycap active**; **non-commerce — no PIXELS, no `CountKeycap`**). The page is a **lean list** — `Well`s of `ListRow`s under `SectionHeader`s — where **each heavier feature opens its own focused page** (the row→dedicated-page grammar; gate ruling 2026-06-14). A right-edge **scroll-rail** marks position on the one long scroll.
**The list.** **ACCOUNT** (email + verify tag AUTH-08 · username + the PROF-06 cooldown microcopy · password / `KeycapButton/secondary` SIGN OUT AUTH-05 · alert-red DELETE ACCOUNT → the `ConfirmSheet`, AUTH-07) · **PRIVACY & SAFETY** (the `Toggle` for PROF-03 + BLOCKED USERS → its page, SOC-09) · **PREFERENCES** → the **NOTIFICATIONS** row ("N ON" summary → its page) · **FEEDBACK & SUPPORT** (FEEDBACK & BUG REPORTING → the triage · HELP & CONTACT, SYS-09) · **ABOUT & LEGAL** (ToS + Privacy AUTH-10 + version; the "screen theme isn't here → Device editor" footnote, DEV-04).
**Feature pages.** **Notifications** (NOTIF-02): a dedicated page (`FlowHeader` ◂ NOTIFICATIONS) of grouped per-type `Toggle`s — Social · Your Games & Cards · Store — + the **NOTIF-04 OS-permission-declined recovery** (`InlineBanner` OPEN PHONE SETTINGS, toggles gated). **Blocked users** (SOC-09): list + cream UNBLOCK (the MOD-09 terminal's lone exception). **Delete account** (AUTH-07): the `ConfirmSheet`.
**Feedback & bug reporting (SYS-11 · type-first triage).** The row opens a triage of three `TriageCard`s (feedback · suggestion · bug); each routes to a tailored form. **Feedback/suggestion** = a minimal form (one `TextField/area` + the mid-width `KeycapButton/primary` SEND — no logs, no tax). **Bug** = a purpose-built form foregrounding two diagnostic tiers: auto **non-PII context chips** (app version · platform) *and* the **`LogAttach`** opt-in (the consent-gated device logs, OQ-060 room). Submit → in-flight (held pressed, kin `motion.holdToBuy`) → `FeedbackConfirm`; `Toast` on failure (§1.8). Rides `POST /feedback` (+ `POST /feedback/:id/logs` — opaque bundle, OQ-060); support-facing — outside MOD-07, rate-limited SYS-05.
**State matrix:** list loaded ✓ · unverified-email (AUTH-08) ✓ · notifications page + OS-declined recovery ✓ · blocked list + unblock ✓ · delete `ConfirmSheet` ✓ · feedback triage → minimal form · bug form (chips + `LogAttach`) · submitting · `FeedbackConfirm` · error `Toast` ✓ · loading `Skeleton` ✓ · `Offline` writes-gated (SYS-10) ✓ · `LoadError` + RETRY ✓.
**Source:** `mockups/settings/settings-states.html` (16 devices; gate ruling 2026-06-14 — **C "type-first triage" + notifications as its own page** + owner refinements: mid-width `/primary` SUBMIT · `ConfirmSheet` bottom drawer; `settings-page.html` + feedback drafts a/b/c kept for history). **API ✅** (no gap — feedback `POST /feedback`+`/feedback/:id/logs` 0.22; username/verify/blocks/notification-prefs/account-delete exist). **Owed:** **OQ-060** (log-bundle shape, reserved in UI) · the **`ConfirmSheet` unification** with the game-page track (OQ-061) · Catalog HTML sync (lagging at v0.4, as for Report/Discover).

### 2.10 Friends (3.3) — converged board, friends track
**Compose.** `DeviceShell` + `NavBand` (**FRIENDS keycap active** = pressed + `PipLight`; 5th/rightmost). The **SOC-06 feed IS the landing** (gate ruling 2026-06-23: **A "Feed-first" won**; B "Roster-first" / C "Connect-first" retired) — a standard `ScreenHead`("FRIENDS") + a **persistent find/add header key** (`ToolKeycap` person-plus, badged with the pending-request count → the **4.8 ADD FRIENDS hub**); content is **one low-noise scroll**, no sub-tabs.
**The surfaces.** A **requests banner** (the SOC-08 pending fast-path → the 4.8 inbox) · a slim **friends rail** (`FriendTile`s → ALL FRIENDS, SOC-01) · the **`FeedRow` stream** (the five aggregated SOC-06 types — added_games (capped + peek) · beat/completed · published_card (foil peek) · unlocked_achievement (the gold trophy tile); trivia excluded, the import never floods). **ALL FRIENDS** = the `FriendRow` roster (hours·games `StatPeek` + COMPARE `chip` → 4.6 + ⋮). **A friend's ⋮ → the actions sheet** (a summoned drawer): VIEW PROFILE · COMPARE HOURS (→4.6, SOC-03) · **RECOMMEND A GAME** (SOC-05 → their What-to-Play — *the compose surface is undrawn, OQ-075*) · UNFRIEND (silent) · a divided SAFETY section REPORT (→4.16, MOD-01) + BLOCK (SOC-09, silent). **No SHARE anywhere** (OQ-052).
**Find/add lives in 4.8** (§2.11) — the header key + banner jump there; the tab hosts the feed/roster/requests-preview/actions, not the full hub (the IA seam, reconciled at converge).
**State matrix:** feed landing ✓ · active feed (all 5 types) ✓ · roster (`FriendRow`) ✓ · ADD FRIENDS hub preview ✓ · cold-start invite hook (SOC-07/10) ✓ · actions sheet ✓ · **quiet feed / empty-requests** (thin window → digest + `InviteHook` nudge; no pending = no banner) ✓ · loading `Skeleton` ✓ · `LoadError` "Signal Lost"+RETRY ✓ · `Offline` (feed+roster from cache, the writes — accept/decline/cancel/recommend/block — **gated**, SYS-10) ✓.
**Source:** `mockups/friends/friends-states.html` (gate ruling 2026-06-23: **A "Feed-first" won**; B/C retired to history; flat buttons rippled to **Scanline Energize**; F-06 clean). **Presence + mutual-friend counts CUT** (owner 2026-06-18). **API ✅** (page-audit 0.23 — `GET /me/feed` item shape, OQ-071). **Owed:** the SOC-05 **recommend-compose** surface (OQ-075).

### 2.11 Find / Add Friends (4.8) — converged board, find-add track
**Compose.** Reached from the Friends tab's find/add key (renders in-frame; **FRIENDS keycap active**). A `ScreenHead`("ADD FRIENDS") + the collection-style **`‹ RETURN TO …` `TertiaryLink`** under the header (hub → FRIENDS · sub-screens → ADD FRIENDS). The **relationship-state machine** is the spine — every `PersonRow` shows one state → the single right action.
**The surfaces.** The **hub landing** (the username `SearchField` entry + INVITE LINK + QR + a requests preview) · **username SEARCH** (the bottom-docked `SearchField/in-place`, OQ-035; results = `PersonRow`s across the states; no-results → the invite bridge; **exact username, no contacts-matching**, SOC-07) · **your INVITE** (the `POST /me/invites` link, copy/share + the `QrCard`) · the **REQUESTS inbox** (`RequestRow` incoming/outgoing + the **cooldown** re-request, SOC-08) · the **SOC-10 invite-landing** (`SenderSummary` as a `FlowTakeover` — ✕ close top-right, NavBand untouched — + the prefilled one-tap ADD, resolving through the Profile; no-app → store) · the **`PersonRow` state spine** + the request-sent `Toast`.
**State matrix:** hub landing ✓ · search results (the live spine) ✓ · no-results (invite bridge) ✓ · invite link + QR ✓ · requests inbox (incoming/sent/cooldown) ✓ · invite-landing (SOC-10) ✓ · the six relationship states + sent-`Toast` ✓ · loading `Skeleton` ✓ · `LoadError`+RETRY ✓ · `Offline` writes-gated (SYS-10) ✓.
**Source:** `mockups/find-add-friends/find-add-friends-states.html` (built off Friends draft A; owner-iterated — RETURN links · P5 `FlowTakeover` · normalized `Toast`; Burt-clean). **Non-commerce** (no gold, no SHARE, no contacts-matching). **API ✅** (page-audit 0.23 — `GET /users/search` PersonRow + `relationship` enum, OQ-072 · `GET /invites/:token` resolve shape + client-side QR, OQ-073).

### 2.12 Compare Hours (4.6) — converged board, compare track
**Compose.** `DeviceShell` + `NavBand` (**FRIENDS keycap active** — Compare lives in the social cluster, reached from a friend's Profile). A `ScreenHead`("COMPARE") + the **`‹ RETURN TO <friend>` `TertiaryLink`** under the header (the collection-style friend-view back-seam). Entered from the friend Profile's **Compare hours** action (SOC-02/PROF-05).
**Has-overlap (the model).** The `CompareHeader` who-vs-who → the `CompareTotals` **face-off** (total hours + total games side-by-side · a split **margin bar** · the `scr.accent` **who's-ahead** pill · a games sub-face-off) → the **`ComparePair` card-vs-card matchups** (per shared game: their `GameCard/cell` on the game frame · the two hour counts, **winner in `scr.accent`** + the gap · your `GameCard/cell` on the slate+foil frame; **card-tap → Game page §4.2**, yours or the friend's friend-view, SOC-11) → the `FriendsLeaderboard` slice (your row highlit, flat `scr.accent`-tint, F-09). Rides `GET /me/compare/:friendId` (totals + shared-game pairs + leaderboard slice; api 0.25, OQ-074).
**No-shared-games.** A graceful empty — the **totals still compare** with zero overlap — then a nudge to **browse their collection** / **recommend a game** (orange, **no gold** — comparing creates no card).
**Privacy-limited (PROF-03).** A friend who hides an axis limits the compare; the hidden axis **drops out cleanly, never leaks** — hours hidden → the totals cell reads a quiet HIDDEN lock, the per-game block becomes a `lock-well`, the friend leaves the hours leaderboard, **the games axis still compares**; a block severs the compare entirely (SOC-09). The endpoint **omits** the hidden fields (api 0.25).
**State matrix:** P1 has-overlap (face-off + card-vs-card matchups + leaderboard) ✓ · P2 no-shared-games ✓ · P3 friends leaderboard ✓ · P4 loading `Skeleton` ✓ · P5 privacy-limited (PROF-03) ✓ · L1 `LoadError` "Signal Lost"+RETRY ✓ · L2 `Offline` (read-from-cache; **read-only, so no writes to gate** — refresh/browse/recommend wait for reconnect, SYS-10) ✓.
**Source:** `mockups/compare-hours/compare-states.html` (gate ruling 2026-06-24: owner picked **B "Versus / head-to-head"**; A "Side-by-side ledger" + C "Diff-first / overlap" retired to history; matchup reworked to the card-vs-card layout, cards `/cell` 96×134 with real `#art-*`; left-edge accent rail dropped, OQ-078; Burt-clean). **Non-commerce** (no gold, completion % out, read-only). **API ✅** (page-audit 0.25 — `/me/compare/:friendId` payload shape + PROF-03 gating, OQ-074).

### 2.13 Welcome & Auth (4.13) — converged board, welcome-auth track
**Compose.** The **logged-out root** — `DeviceShell` + a **`NavBand/locked`** (grayed/non-interactive, present from step one, F-04). **Games-forward landing:** an `INGAME` wordmark → a **`CardFan`** of community `GameCard`s (full faces, F-01) with a gold all-caps **card-artist credit** beneath (the one sanctioned on-screen gold — gold = card authorship, F-02) → two **system-stat** readouts (GAMES CATALOGED · HOURS LOGGED, `SYS-12` — informational, **not gold**) → a **contribution tagline** → the **sign-in form (Draft A — owner-picked declunk, 2026-06-27):** cream-inset `TextField`s (email · password with **FORGOT? inline** on the password label) · one orange `KeycapButton/primary` **SIGN IN** (the lone full-width primary) · a **compact `Sign in with Apple`** (iOS, token-exempt Apple lockup) under an OR CONTINUE WITH divider · **CREATE ACCOUNT** demoted to a "New to InGame?" `TertiaryLink` footer. Android (W1b) drops Apple **and** the OR row (AUTH-03). On-screen chrome is square (F-07 — the lifecycle `seal`/`LoadError` badges are **full squares, no pixel-step**; the step belongs to `GameCard`s). The device-hero direction is kept for history (`welcome-auth-device-hero.html`).
**State matrix — 18 states, every case the client handles:**
- **Landing / entry:** **W1** landing (iOS) · **W1b** landing (Android, no SIWA — AUTH-03) · **W13** `Skeleton` / first-paint cold open (the `SYS-12` stats load async; the hero + form silhouette is reserved so nothing shifts when data lands; **stats degrade-hide, never block** the form, §1.6).
- **Create account (AUTH-01 / AUTH-10 / MOD-07):** **W2** form + age-13/ToS gate (CREATE ACCOUNT disabled-until-checked) · **W3** username availability checking→available (AUTH-11, `GET /auth/username-available`) · **W4** field validation (email-taken · rejected-username MOD-07 · weak-password · terms-unchecked).
- **Sign in (AUTH-02 / 03 / 09):** **W5** wrong-credentials (`AUTH_FAILED` 401 — calm inline strip, deliberately not field-level) · **W5b** rate-limited cooldown (`RATE_LIMITED` 429, SYS-05 — calm orange `InlineBanner`, SIGN IN gated, FORGOT? stays live) · **W5c** suspended / catch-all submit error (`ACCOUNT_SUSPENDED` MOD-09, or a post-validation `SERVER_ERROR` — red `authfail` strip, reason-first, CONTACT SUPPORT) · **W12** submitting / in-flight (request open — `KeycapButton/busy` spinner, all CTAs gated → **no double-submit**) · **W6** SIWA username-completion + account-linking (AUTH-09, `POST /auth/apple` → `usernamePending`).
- **Password reset (AUTH-04):** **W7** request → send link · **W8a** link-sent confirm · **W8b** set-new-password from the emailed link (`POST /auth/password-reset/confirm`) · **W8c** link expired / already used (single-use, 1-hour — invalid-token → REQUEST A NEW LINK loops back to W7).
- **Post-signup + lifecycle (§1.6):** **W9** soft verify-email notice + resend (AUTH-08 — non-blocking; the seam into Onboarding §2.14) · **W10** `LoadError` Signal-Lost + RETRY (a **retryable transport** failure — the request never landed) · **W11** `Offline` writes-gated (SYS-10 — auth is a write, can't read from cache; every CTA gates calmly).
- **Error routing (so build isn't guessing):** *got an answer we can't act on* → an inline beat on the same form (401 wrong-creds → W5 · 429 → W5b · suspended / 500-after-validation → W5c); *couldn't get an answer* → the retryable lifecycle (transport/timeout → W10 Signal Lost · offline → W11). In-flight is always W12; a submit resolves to the signed-in app or back to the matching beat.
**Source:** `mockups/welcome-auth/welcome-auth-states.html` (decision 0029, games-forward; **sign-in declunked to Draft A** + the **5 lifecycle/error states W5b · W5c · W8c · W12 · W13 added** 2026-06-27; Burt PASS). **API ✅** — every state maps to an existing endpoint or the standard error model: the auth endpoints + `GET /auth/username-available` (AUTH-11) + `GET /stats/public` (SYS-12) + the `RATE_LIMITED` / `ACCOUNT_SUSPENDED` / `SERVER_ERROR` codes (api 0.26 endpoints + the **W8c invalid-token** & **W4 register-rejection** error responses pinned at api 0.32; **no new endpoints required**).

### 2.14 Onboarding (4.14) — converged board, onboarding track
**Compose.** A bounded, **fully-skippable** `FlowTakeover` (`DeviceShell` + `NavBand` persist, F-04) that **grows the collection underneath the steps and reveals it populated** (the defining constraint — never an empty shelf). One consistent **skip = the header `SKIP` keycap on every step**; steps advance via their own `KeycapButton` (START/CONTINUE). A `step-rail` (STEP n/4) marks brevity.
**Steps.** O1 welcome (START) · O2 add-a-few via the **CAT-09 popular rail** (`CardFan` + `SearchField`, COL-01; header SKIP + a rail `count` badge) · O3 status (optional COL-02, defaulted BACKLOG, `StateMark`) · O4 pick-genre (controlled multi-select `GTag`, PROF-01/CAT-04) · O5 card teaser (the lone **gold** ADD & DESIGN, F-02) + invite nudge (orange) · O6 **NOTIF-04 push pre-prompt** (soft in-app ask; decline never fires the OS one-shot) → O7 ENABLE (OS prompt) / O8 NOT-NOW (lands directly) · O9 **finale = the faithful populated Collection** (real tools-bar + C5 grid + Now-Playing hero) · O10 zero-add → **friendly add-nudge, no auto-seed** (OQ-B, decision 0029).
**State matrix:** O1–O10 + L1 `Skeleton` · L2 Signal-Lost+RETRY (+ a distinct FINISH-SETUP recovery escape) · L3 `Offline` writes-gated (SYS-10). **Not resumable** + **no push re-offer** (decision 0029).
**Source:** `mockups/onboarding/onboarding-states.html` (decision 0029; Burt PASS). **API ✅** — existing auth/collection/catalog/queue endpoints + the CAT-09 popular rail.

### 2.15 Device editor (4.5) — converged board, in-frame · device track
**Compose.** `DeviceShell` + `NavBand` (**PROFILE active** — the frame is the live subject). Reached from Profile's MY DEVICE strip as a **`FlowTakeover`** (`FlowHeader` ◂ "DEVICE" + the gold `CurrencyCounter`/`CountKeycap` → Wallet; commerce-adjacent). **In-frame model:** controls dock on the device's own screen; the bottom **`SectionSwitch/rail`** (SHELL · THEME · STICKERS · LOOKS as `SectionCard`s; active = accent-border + `StateMark`, no pip) is the switcher. **SHELL** = the 5 §1.1 colourways via `ItemTile`s + the before→after `MiniDevice` pair (own/switch, DEV-02). **THEME** = the 6 §1.1 screen themes under a `PreviewStrip` whole-page live preview (DEV-04, legibility floor honored). **STICKERS** = the `StickerTray` → `StickerStage` place/scale/rotate (`TransformBox`) on the **forehead + chin plastic only** (DEV-01/03). **LOOKS** = the `LooksGrid` of `SavedLook`s + SAVE CURRENT (DEV-05). Premium shell/theme/stickers preview live and accrue on the `KeepBar` → KEEP → `ReconcileSheet` (`POST /cosmetics/acquire-batch`, OQ-065).
**State matrix:** D1 editing landing · D2 shell-switch (before→after) · D3 theme whole-page preview (DEV-04) · D4 sticker place/scale/rotate `TransformBox` (plastic only) · D5 the OQ-045 on-shell preview (`PlacedSticker`, handles hidden, nav z-ordered above) · D6 LOOKS (`SavedLook` combos · ON NOW · SAVE CURRENT — the per-tile **delete** affordance is owed to the board, DEV-05 follow-up / decision 0030) · D7 premium previewed (`KeepBar`) · D7b the cart carries across sections · D7c reconcile (`ReconcileSheet` + short-by-N bridge) · D8 `Skeleton` · D9 `Offline` writes-gated (SYS-10) · D10 `LoadError` Signal-Lost+RETRY.
**Hard rules proven:** nav keycaps render **above** every sticker (DEV-03/F-04); the theme preview keeps the contrast floor (DEV-04); **personal-only** — no share/adopt verbs. Owner refinements baked in: stickers on **plastic only**; looks **identified by shell·theme** (no name); **SAVE CURRENT** the only save.
**Source:** `mockups/device/device-states.html` (gate ruling 2026-06-14: owner picked **A "in-frame live edit"**; the `SectionSwitch` rail won, 4 sections incl. LOOKS; drafts a/b/c + `switcher-takes`/`switcher-rail`/`draft-a-looks` retired to history). Burt PASS. **Design-spec ✅** (this §2.15 + §1.5 Device-editor set, decision 0030) · **API ✅** (page-audit 0.27 — `stickerComposition` shape + `/me/device/looks`; OQ-062/064). Closes OQ-045/062/063/064/065/076.

### 2.16 Contributor profile (4.9) — converged board, Profile-grammar · catalog track
**Compose.** `DeviceShell` + `NavBand` (the **viewer's own** band — reached from any Profile's **Contributions** `ListRow` gateway, CAT-07; self from your Profile, friend from theirs). A `ScreenHead`("CONTRIBUTIONS") over an `IdentityBlock` (whose record it is). **STATS** = the **boxless** `StatTile` 4-up **GAMES · CARDS · ADOPTIONS · REACHED** (de-welled, gutter-aligned — the §2.2 treatment), each eligible tile wearing its **CAT-10 `PctPill`** standing (GAMES / ADOPTIONS / GAMES-by-reach vs the contributor cohort; SYS-04 threshold + PROF-03 privacy gates — the chip is **omitted, never faked**, when ungated). **SIGNATURE CARD** = the PINNED-FAVOURITE `GameCard/hero` (the most-adopted card) + **MOST ADOPTED** tag + adoptions sub + VIEW CARD. **CARDS DESIGNED** = a `GameCard/cell` grid + `RankChip` (top-N by adoptions, **each card captioned with its adoption count** — the `.adopt` caption shared with the VIEW ALL grid) under a **VIEW ALL ›** `TertiaryLink`. **GAMES ADDED** = `Strip` rows (thumb + game title + "IN N COLLECTIONS" reach + chevron → Game page §4.2) under its own **VIEW ALL ›**. Read-only; the only gold is the `PctPill`/`SectionEmpty` value-markers.
**State matrix:** P1 self (full) · P2 never-contributed `EmptyState` (gold be-first hooks) · **P2b self-partial** (one section bare → the `SectionEmpty` nudge: gold **DESIGN A CARD** / neutral **ADD A GAME**; doubles as the PROF-07 below-threshold case — stats render chip-less) · P3 friend-view · **P3b friend-empty** · P4 **PROF-03 privacy-limited** (`lock-well`; restricted detail withheld, honest aggregate totals still readable per CAT-09) · **V1/V2 VIEW ALL** (the cursor-paginated all-cards `/cell` grid by adoption + all-games reach rows; header echoes the section + a `‹ CONTRIBUTIONS` back-seam) · L1 `Skeleton` · L2 Signal-Lost+RETRY · L3 `Offline` (cached, read-only, SYS-10). The profile reads as a **top-N summary**; VIEW ALL reveals the rest.
**Source format.** `GET /users/:id/contributions` (friend/full vs non-friend/limited: `{ user, stats{ gamesAdded, cardsDesigned, totalAdoptions, totalReached }, standing{ byAdoptions?, byGames?, byReach? } | null, signatureCard, topCards[], topGames[] }`) + the paginated **`GET /users/:id/contributions/cards?cursor=`** and **`/games?cursor=`** behind VIEW ALL. API page-audited → contract **0.28**.
**Source:** `mockups/contributor-profile/contributor-states.html` (gate ruling 2026-06-26: owner picked **B · The Trophy Wall**, then iterated to the Profile-like layout — badges dropped for gold `PctPill` standing, CAT-06 field-edits dropped, card/game VIEW-ALL parity; drafts A/C + the badge-bar / `-profilelike` lineage retired to history). Burt PASS. **Design-spec ✅** (this §2.16 + §1.5 Contributor-profile set, decision 0032) · **API ✅** (page-audit 0.28). Resolves the CAT-07 design debt; the standing is the new **CAT-10** (product-spec 0.28). Closes **OQ-079**.

### 2.9 OQ traceability
**Resolved here (decision 0013):** OQ-006 → F-03 + the Keycap system (`KeycapButton`/`ToolKeycap`) · OQ-014 → F-01 · OQ-036 → F-02 (colour-disambiguated step) · OQ-037 → §1.6 · OQ-035 → §1.7 · OQ-031 (Arrange) · OQ-033 (shelf stats) · OQ-034 (tools-bar model) → §2.1 · OQ-012 → resolved (decision 0012; friend-chrome toggle).
**Still open** (owed before the screens that touch them): OQ-005 (egg presentation → Achievements) · OQ-046 (hold-to-buy a11y alternative). *(OQ-045 sticker-on-shell preview → resolved at the Device-editor formalization, §2.15 / decision 0030.)* *(OQ-007 break-out → resolved design-side at the Canvas converge, `canvas/canvas-states.html` — see open-questions.md.)*
**Marker token rename (on-screen marker audit 2026-06-14; owner Draft A, boards re-skinned 2026-06-15):** the on-screen `ChipPip`/`PipLight` is renamed **`StateMark`** — a flat `scr.accent` pixel-square with one-pixel-notched corners (the on-screen selection/active marker, F-09); the **pink** pip stays the **SHELL LED only** (`NavKeycap`/`NavBand`/`DeviceShell` LEDs, F-05) and keeps the name `PipLight`. Token `motion.pipLight` → `motion.stateMark`. **Resolves OQ-067** (on-screen selection marker colour/shape) **+ OQ-063's marker sub-part** (the `SectionSwitch` tell is the `StateMark`, not a pink pip; OQ-063's app-wide-consolidation question stays open). *(Recorded here + in the changelog per the spec-owner protocol; OQ-067 moved to Resolved in the inbox.)*
**Canvas (§2.5b) + COL-11 (§2.1) formalized:** the Canvas converge set (`CanvasStage`/`AssetShelf`/`ElementTray`/`LayerRack`+slips/`editbar`/the EDIT slip-sheet/PROOF size-ladder/PRESS finish-up/`PrintRitual`) + §2.5b composition; the COL-11 friend browse-tools into §2.1 (decision 0021). OQ-007 + OQ-040 carried as Canvas resolutions.
**Game page (§2.4b) formalized (0.21):** the Game-page set (`DualFaceHero` · `PlayStats` · `CardSwitcher` · `SectionSwitch` · `FriendContext`) + §2.4b composition (M1–M8 + L1–L4); API page-audited, no gap. Decisions 0020/0021/0024/0025 encoded (SOC-11 · CARD-01/22 · COL-02..06 · MOD-02/08/09). OQ-056 (switcher style-presets — editor concern, not a game-page gap; DESIGN NEW just launches the Styler) · OQ-061 (`ConfirmSheet` affordance formalized, card-delete semantics still open) · OQ-063 (`SectionSwitch` app-wide unification — deferred) cross-referenced. `SectionSwitch` active = accent-border (flat, no pip) per the 0.20 flat direction; Catalog v0.5 uses this form; `SegmentedKeycap`/`SectionChips` prose reconcile stays OQ-063.
**Welcome & Auth (§2.13) + Onboarding (§2.14) formalized (0.27):** the games-forward landing + the **`NavBand/locked`** pattern + the sign-in-first form; the guided-seed onboarding (one header SKIP · CAT-09 rail · NOTIF-04 soft pre-prompt · faithful populated finale · zero-add friendly nudge). Behaviour rulings (zero-add no-seed · NOTIF-04 no re-offer · not-resumable) + the new public endpoints **AUTH-11** (username-availability) + **SYS-12** (public stats) in decision 0029 (product-spec 0.25 · api 0.26).
**Friends (§2.10) + Find/Add (§2.11) formalized (0.23):** the **Friends/Social set** (`FeedRow`/`ActivityRow` · `FriendRow`/`FriendTile` · `RequestRow` · `InviteHook` · `PersonRow`/`SearchResultRow` · `QrCard` · `InviteLanding`/`SenderSummary`) + §2.10/§2.11 compositions; the **relationship-state machine** is the 4.8 spine (none→ADD · outgoing→CANCEL · incoming→ACCEPT/DECLINE · friends→profile · cooldown→disabled · blocked→limited). **`PresenceDot`/`StatPeek` CUT** (owner 2026-06-18 — never formalized). API page-audited (0.23): `GET /me/feed` item shape (**OQ-071**) · `/users/search` PersonRow + `relationship` enum (**OQ-072**) · `/invites/:token` resolve shape + client-side QR (**OQ-073**) — all **moved to Resolved**. **OQ-075** (the SOC-05 recommend-compose surface) stays **open** — a design-surface gap, the `POST /recommendations` endpoint already exists. Gate ruling: **A "Feed-first" won**; B/C retired. Source: `friends/friends-states.html` + `find-add-friends/find-add-friends-states.html`.
**Compare Hours (§2.12) formalized (0.26):** the **Compare set** (`CompareHeader` · `CompareTotals` · `ComparePair`/`CompareRow` · `FriendsLeaderboard`/`LeaderRow`) + §2.12 composition (the card-vs-card **Versus** board: P1 has-overlap · P2 no-shared-games · P3 leaderboard · P4 Skeleton · P5 privacy-limited · L1 Signal-Lost · L2 Offline). API page-audited → contract **0.25** (`/me/compare/:friendId` payload shape + PROF-03 gating, **OQ-074** — moved to Resolved). Gate ruling: owner picked **B "Versus / head-to-head"**, A/C retired. **Non-commerce** (no gold), **completion % OUT**, read-only; card-tap → Game page §4.2 (SOC-11); the highlighted leaderboard row is a flat accent-tint, **no left-edge rail** (OQ-078). The convergence/formalization debt **OQ-077** is closed. Source: `compare-hours/compare-states.html`.
**Device editor (§2.15) formalized (0.29):** the **Device-editor set** (`SectionSwitch` unified with `/pair`·`/chips`·`/rail` variants · `SectionCard` · `StickerStage` · `TransformBox` · `PlacedSticker` · `StickerTray` · `SavedLook` · `LooksGrid` · `KeepBar`) + §2.15 composition (the in-frame board: D1–D6 edit/looks + D7/D7b/D7c premium-cart + D8–D10 lifecycle). Four rulings in decision 0030: **OQ-062** (`stickerComposition` shape + plastic-only zones, server-validated) · **OQ-063** (`SectionSwitch` app-wide → one grammar + variants, `SegmentedKeycap`/`SectionChips` retained as aliases, converged boards untouched) · **OQ-064** (saved looks = `device_looks` snapshots, no name, computed ON NOW, delete + cap, **DEV-05**) · **OQ-065** (premium live try-on persists → atomic KEEP-batch, owned-only device). **OQ-045** closed design-side; **OQ-076** (the formalization-owed capture) closed. API page-audited → contract **0.27** (`stickerComposition` shape + `GET/POST /me/device/looks` + `DELETE /me/device/looks/:id`). Gate ruling: owner picked **A "in-frame live edit"**; b/c + switcher takes retired. Source: `device/device-states.html`.
**Contributor profile (§2.16) formalized (0.31):** the **Contributor-profile set** — a Profile-grammar recomposition (one new component, **`SectionEmpty`**, the per-section nudge; the **CAT-10 standing** is the existing `PctPill` worn on contributor `StatTile`s, cohort-relative + SYS-04/PROF-03-gated) + §2.16 composition (P1 self · P2 empty · **P2b self-partial** · P3 friend · **P3b friend-empty** · P4 PROF-03 limited · **V1/V2 VIEW ALL** + L1–L3 lifecycle). Spec ripple in decision 0032: product-spec **CAT-07** revised (drop CAT-06 field-edits + achievement badges, add the standing + collections-reached + VIEW ALL) + new **CAT-10** (contributor standing — `PctPill` percentiles vs the contributor cohort, threshold + privacy gated). **API page-audited → contract 0.28** (`GET /users/:id/contributions` friend/full vs non-friend/limited shape + the paginated `…/cards?cursor=` · `…/games?cursor=` VIEW-ALL endpoints). §2.2 Profile STATS de-well (0.29) is the shared treatment. Gate ruling: owner picked **B · The Trophy Wall**, iterated to the Profile-like layout; A/C retired. **OQ-079 → Resolved.** Source: `contributor-profile/contributor-states.html`.

---

## Changelog
| Date | Version | Change |
|---|---|---|
| 2026-06-11 | 0.1 | Phase B draft: foundation rules F-01..07, full token set (shell/screen/brand/type/space/motion), named component catalog incl. the OQ-006 "Mix" button system, Collection + Profile recomposed by name, Phase C/D gap list. |
| 2026-06-11 | 0.2 | **Card plates → `type.ui` (Chakra Petch 700).** Silkscreen (`type.print`) rescoped to sticker print: PlateButton labels, tags, tertiary links. Catalog + H2 mockup updated to match. |
| 2026-06-11 | 0.3 | **F-08: one font per surface.** Chakra Petch = the screen font (PlateButton, tags, links now `type.ui`); Paytone One = the plastic font. **Silkscreen retired.** Catalog + H2 mockup updated. |
| 2026-06-11 | 0.4 | **Buttons → Keycap system** (owner picked Take A; supersedes the Mix). F-03 revised: keycap physics on both surfaces. `PlateButton`/`ToolChip` → `KeycapButton`/`ToolKeycap`; `shadow.plate` → `shadow.key`; `motion.platePress` retired. H2 mockup retrofitted (theme-aware keycap edges via color-mix). |
| 2026-06-11 | 0.5 | **`brand.storeYellow` → `brand.gold`** (generic value marker). `PctPill` now gold-filled (was dashed accent outline). |
| 2026-06-11 | 0.6 | **Brand constants pruned:** `brand.hazard` retired (was Printed-Plate destructive only). **Shell palettes:** Bone removed; **Pink** + **Carbon** added (5 colourways; Carbon flips `shell.silk` light). Tweaks + catalog updated. |
| 2026-06-11 | 0.7 | **ADD keycap → `brand.gold`** (gold = acquire/value actions). Applied in both H2 mockups + catalog. |
| 2026-06-11 | 0.8 | **Synced to the Collection-states + Profile-states passes** (decision 0013): added the **States & feedback** family + **§1.6**, **§1.7 Inputs & keyboard** (OQ-035 system keyboard), `KeycapButton/action-alt` (orange non-card step) + **F-02 colour-disambiguation** (OQ-036), in-place `SearchField`+`MatchTag`, `Avatar`+`DesignBadge` (PROF-08). Recomposed Collection (3 view modes, tools-bar model, Arrange, friend-chrome) + Profile (Settings gear not privacy; friend-view; state matrices). Fixed stale OQ-012 ("open"→resolved) + header version. | OQ-006/012/014/031/033/034/035/036/037 |
| 2026-06-12 | 0.9 | **F-09: no sunken containers** — flat planes; selection = accent border + pip; pressed keycaps + text inputs the named exceptions. Formalizes the 2026-06-10 flat-panels ruling + the Add Game pass-4 report-drawer ruling. |
| 2026-06-12 | 0.10 | **Store + PIXELS formalized (store track, owner-directed):** §1.5 gains the **Currency & Commerce set** — the **PIXELS mark** (pixel-gem v3; replaces the ◈ placeholder app-wide) · `CurrencyCounter` (+negative) · `PriceChip` · `BuyBar` w/ **hold-to-buy** (OQ-046) · `PackTile` (+`/starter`, value math, $ on `/secondary`) · `LedgerRow` · `Toast` (§1.6 transient slot filled) · `OwnedTag`/`LockedTag`/`EarnedOnlyTag` · `ItemTile` · `DailyBonusBar` (OQ-043) · `AisleIndex` · `PreviewStrip` · `PreviewStage`. §1.4 +`motion.holdToBuy`/`counterTick`. New **§2.3 Store** composition + state matrix (lifecycle incl.; sticker-on-shell preview deferred OQ-045); old §2.3 → §2.4. Catalog HTML → **v0.3**. Gaps pruned (toasts · currency counter). Source: `store-states.html` (5 owner rulings, 2026-06-12). |
| 2026-06-12 | 0.11 | **Owner pass-5 library rulings (Add Game track):** `KeycapButton/primary` fill → **`scr.accent`** (the "bronze" **`scr.btn` token retired** — action buttons read orange/on-theme; `/action-alt` now differs by the step only) · **F-09 selection borders = `scr.accent`** (pink stays the pip voice) · report **SUBMIT = `/destructive`** (red). |
| 2026-06-12 | 0.12 | **Add Game formalized (add-game track):** §1.5 gains the **Forms & Flow set** — `FlowTakeover`/`FlowHeader` · `TextField`(+`/area`/`/error`) · `SelectField` · `ResultRow` · `InlineBanner` · `CardFan`(+`/pick`) · `CardDetail` · `EquipReadout` · `CleanPeek` · **`ReportSheet`** (`/drawer` ruled app-wide; `/modal`+`/takeover` history) · §1.2 gains the **plate legibility floor** (≥10/9px in fan/picker contexts + the editor min plate-band note) · new **§2.4 Add Game** composition + full state matrix (old §2.4 → §2.5) · gaps pruned (multi-line input, dropdown/picker, report sheet). Catalog HTML → **v0.4**. Source: `add-game/add-game-states.html`. |
| 2026-06-12 | 0.13 | **Styler formalized (styler track):** §1.5 gains the **editor set** — `AttributeSection` (5 attributes incl. **NAMEPLATE**, OQ-039/0018) · `SectionChips` · `BaseRail` · **`IntensitySlider`** (the slider gap closed) · `ReconcileSheet` · `KeepBeat` · new **§2.5 Styler** composition + state matrix (draft-safe lifecycle; old §2.5 → §2.6). Catalog v0.4 carries both sets. Source: `styler/styler-states.html` (carousel ruling). |
| 2026-06-13 | 0.14 | **Report formalized (report track):** §1.5 `ReportSheet` gains its **three targets** (card · catalog +duplicate · user +BLOCK) + the **lifecycle states** (dormant → in-flight → submitted; offline writes-gate; `Toast` failure) and a new named sub-surface **`ReportConfirm`** (+`/block`); new **§2.6 Report** composition + full state matrix (old §2.6 OQ-traceability → §2.7). Reflects MOD-01 (required details, mod-only/outside MOD-07) · MOD-02 (soft-hide, no threshold) · SOC-09 (block handoff) · SYS-10 (writes-gate). API page-audited, no gap (`POST /reports` {card\|game\|user + reason + details} incl. duplicate + `/me/blocks`). Source: `report-sheet/report-states.html`. | MOD-01/02 · SOC-09 · SYS-10 |
| 2026-06-13 | 0.15 | **Discover formalized (discover track):** §1.5 gains the **Discover set** — `SegmentedKeycap` (the owed **segmented control**, `/pair`, bottom-docked + smaller, active pressed + pink `PipLight`) · `QueueRow` · `ReleaseRow` · `RecRow` · `AdoptCount` · `NotifyToggle`; the **"segmented control (Discover needs it)" gap is closed** (gaps line trimmed; "toggle" rescoped to the switch). New **§2.7 Discover** composition + full state matrix incl. lifecycle (old §2.7 OQ-traceability → **§2.8**). Discover is **non-commerce** (no PIXELS — adoption counts, not prices). Records the gate ruling (**A "Two rooms" won** + the 7 owner edits — bottom/smaller toggle · standard header · real games · **Browse-By cut → OQ-057** · simplified pages; card-fan variant `discover-states-fan.html` retired). **API page-audit owed:** OQ-053/054/055/057. Source: `discover/discover-states.html`. | WTP-01/02/03 · DISC-01/03/04 · CAT-08/09 · COL-02 · SOC-05 |
| 2026-06-13 | 0.15 | **Hygiene (R1 audit — doc-presentation lane):** Source-mockups header cite corrected — `Design System Catalog` **v0.3 → v0.4** (the header had lagged its own 0.12/0.13 changelog, which already took the Catalog HTML to v0.4 with the Forms & Flow + Editor sets). No spec/behaviour change; patch note under the current version. |
| 2026-06-14 | 0.16 | **Settings formalized (settings track):** §1.5 gains the **Settings set** — `Toggle` (the square keycap-knob switch) · **`ConfirmSheet`** (the destructive-confirm **bottom drawer** — **closes the "confirm dialog" gap**; shared with the game-page track's `ConfirmSheet`, OQ-061) · `LogAttach` (the SYS-11 bug device-logs opt-in, OQ-060 room) · `FeedbackConfirm` · `TriageCard`; the **`Toggle` + confirm-dialog gaps closed**. New **§2.8 Settings** composition + full state matrix (old §2.8 OQ-traceability → **§2.9**). Records the gate ruling (**C "type-first triage" + notifications as its own page**) + owner refinements (**mid-width `/primary` feedback SUBMIT** · **`ConfirmSheet` bottom drawer**). Non-commerce (no PIXELS). **API ✅** no gap (feedback `POST /feedback`+`/feedback/:id/logs` 0.22). **Catalog HTML sync owed** (lagging at v0.4, as for Report/Discover). Source: `settings/settings-states.html`. | SYS-11 · AUTH-05/07/08/10 · PROF-03/06 · SOC-09 · NOTIF-02/04 · SYS-09/10 |
| 2026-06-14 | 0.15 | **Hygiene (R3 audit — doc-correction lane):** §2.2 Profile **friend-view drops Share** — sharing is **self-view only** (your invite link, SOC-07; the friend-view chip was cut by **decision 0019 / OQ-052**). Brings design-spec into line with product-spec PROF-05, api-contract, and ui-design-requirements §3.5 (which already reads "no Share"). Also closed the OQ-007/OQ-040 resolved-OQ leaks (§2.8 + open-questions). No spec/behaviour change; patch note under the current version. |
| 2026-06-14 | 0.17 | **`brand.success` token (R4 audit — token-gap close):** §1.1 gains **`brand.success`** (`#7fcf9b`) — the positive/affirmative status colour (verified · on · attached · ledger credits), the green counterpart to `brand.alert`. Closes the gap R4 found where Settings, Discover, and the Store ledger each improvised an off-palette green. **Rippled:** store-ledger earn `#7fe0c3` → `brand.success`; Settings already used the canonical value. Theme-invariant (light-theme variant parked). **Catalog swatch sync owed** (as for Report/Discover/Settings). Lime FOIL `#d3e95e` left per owner. Source: decision 0023. |
| 2026-06-15 | 0.17 | **Hygiene (R5 audit — doc-clarity lane):** §1.5 gains an **overlay-grammar disambiguation** note — the `*Sheet` suffix names the **summoned-drawer** family (no grab handle: Report/Confirm/Reconcile/Option), distinct from a **pulled** `.sheet` (grab handle). Pairs with completing the `ConfirmDialog → ConfirmSheet` rename in the Settings board (mockup hygiene). No new component; patch note under the current version. |
| 2026-06-17 | 0.18 | **Canvas formalized (canvas track):** §1.5 gains the **Canvas set** (open-composition / print-shop) — `CanvasStage` (workshop + press-bed + the diegetic shell-swing **breakout**, OQ-007) · `AssetShelf`/`ElementTray` (the ADD glyph-roster drawer — **reuses** the one summoned-drawer grammar) · `LayerRack` + `slip`s (pull-to-isolate · `cap-meter` at **30**, OQ-008 · Z = rack-order · long-press ops) · `editbar` (relocated undo/redo + scoped RESET SLIP, CARD-09) · the **EDIT slip-sheet** (CARD-10/11/MOD-07) · the **PROOF** size-ladder (GRID/MINI/THUMB, CARD-07/15) · the **PRESS** finish-up sheet (CARD-19) · `PrintRitual` (the OQ-040 first-print three-beat). New **§2.5b Canvas** composition + full state matrix (the two ruled changes recorded: undo/redo→editbar · PROOF renders set sizes). **API page-audited, no gap** — all writes exist 0.16/0.18 (draft · autosave · publish · save-private · acquire-batch · assets · share-image); cap-30 (OQ-008) + save-private (OQ-049) product-spec-only, resolved 0019. **OQ-007 + OQ-040 resolved** design-side. | CARD-07/08/09/10/11/13/15/19/20/21 · OQ-007/008/040/049 |
| 2026-06-17 | 0.18 | **F-09 marker rename (`ChipPip`/`PipLight` → `StateMark`):** the on-screen selection/active marker is the **orange pixel-square** (`scr.accent`, flat, square, one-pixel-notched corners), **not** the pink pip; F-09 reworded (the line's "pink" rescoped to the **SHELL LED only**, F-05). Renamed across §1.5 (`ToolKeycap` · the `StateMark` component entry · `SectionChips` · `SegmentedKeycap` · §2.1 sort · §2.6 Report reason rows) + token `motion.pipLight` → `motion.stateMark`; shell `NavKeycap`/`NavBand` LEDs keep `PipLight` (pink). §2.9 note added. Boards were re-skinned 2026-06-15; Catalog label "active + ChipPip" → "active + `StateMark`". Source: `audit/2026-06-14/onscreen-pip-marker-audit.md` (owner Draft A). |
| 2026-06-17 | 0.18 | **COL-11 friend browse-tools (§2.1, decision 0021):** the friend-view gains **read-only browse-tool bar parity** (search · sort + `StateMark` · filter · view), the **sorted-by-hours storytelling list** (her hours · status · GOT IT 'YY owned-since, SOC-11), and the **read-only sort/filter drawer** (over the friend-visible field set only — no notes/rating/platforms) with **no Arrange / no per-entry edit**. +2 state-matrix rows. Board + API already done (api widened 0.20). Source: `collection/collection-states.html`. | COL-11 · SOC-11 |
| 2026-06-17 | 0.18 | **Hygiene (verifier follow-up to the trio formalization):** completed the `StateMark` rename — `SectionChips` (§1.5) "pressed + pip" → "pressed + `StateMark`" (the one on-screen pip the rename pass missed; the styler board already renders it orange) + added it to the 0.18 rename list. Added **`NumPop`** (the Canvas numeric X/Y coordinate popover, CARD-09) to the §1.5 Canvas set; pruned **numeric input** + the **celebration moment (OQ-040)** from the Phase-C/D gap line (closed by `NumPop` / the Canvas `PrintRitual`). No behaviour change. |
| 2026-06-18 | 0.19 | **F-06 type-scale conformance ruling (decision 0024 / OQ-066) — F-06 is NOT amended.** §1.2 gains the conformance note (off-scale on-screen type 17/13/12/10.5/10/9 snaps to the nearest of **21/15/11/9** — no `state-title`/`mini-button` role carved); §1.6 gains the **state-titles → emphasis 15** sub-call. **Ripple debt:** the actual ~7-board conformance SWEEP (`*-states.html` — worst Settings; cleanest Store/Collection/Report-sheet) is a **separate follow-up task, OWED — not done here** (no board edited this pass). Also: §1.5 **card-back gap annotated** with the OQ-069 ruling — printed provenance = **designer-attribution only**, adoption count drops to the gallery `AdoptCount` + `CardDetail` inspect (CARD-01). No new component, no token change. | OQ-066/069 (F-06 unchanged) |
| 2026-06-18 | 0.20 | **Flat buttons + Scanline Energize (owner-locked).** On-screen action buttons + tool keycaps **flattened** — `shadow.key` (the `0 3px 0`/`0 2px 0` raised edge + inset bevel + the clipped `drop-shadow`) **deprecated** for screen; **pressed/active = `motion.scanlineEnergize`** (fine CRT scanlines + a hairline-darkened fill, **no motion, no pip**). F-03 rescoped (keycap travel = shell nav only), F-09 exception list trimmed (on-screen press no longer travels/sinks), `motion.keyPress` → shell-only + new **`motion.scanlineEnergize`**, `KeycapButton`/`ToolKeycap`/`CountKeycap` defs flattened. **Rippled** across all `*-states.html` boards + the Catalog (specimens **v0.5 → v0.6**). CountKeycaps flatten-only (not pressable); the clip-path corner-notches, the physical `NavBand` keys (3D), and the segmented/tab **accent-border** active are all kept (segmented consolidation stays OQ-063). Verified per board (headless render + **Burt** DS-audit). Source recipe: `collection-flat-buttons-drafts-2.html` (draft F). | F-03 |
| 2026-06-22 | 0.21 | **Game page formalized (game-page track, §4.2).** §1.5 gains the **Game-page set** — `DualFaceHero` (PLAY-tab hero pair: THE FACE + YOUR STATS, `/grid` 2-up 161×225, `.flipy` mirrored step; CARD-01 back = designer attribution only, OQ-069/0024) · `PlayStats` (dossier readout/edit + pinned `edit-bar`, COL-02..06 minus WISHLIST OQ-070/0025, rating private-only OQ-058/0024) · `CardSwitcher` (COL-06/OQ-056: 3-up tap-select + inline options + state-tags + DESIGN NEW; DELETE → `ConfirmSheet`, OQ-061 semantics open) · `SectionSwitch` (PLAY·CARDS·ABOUT bottom rail, accent-border active F-09 flat; app-wide unification deferred OQ-063) · `FriendContext` (M7: dual-face friend + opt-in compare entry, SOC-11; client-side compose, decision 0020). **`KeycapButton/secondary` HTML modifier = `.btn.cream`** (split from `.sec`, Bugfix #4). New **§2.4b Game page** composition + full state matrix (M1–M8 + L1–L4). **API page-audited, no gap** — all affordances backed (`PATCH/DELETE /me/collection/:entryId` · `GET /me/collection/:entryId/cards` · `GET /games/:gameId/cards` + CARD-22 `equipped` · `POST /cards/:id/adopt` · `DELETE /cards/:id` + `/unpublish` · `GET /users/:id/collection` 0020/0021 · `GET /catalog/games/:id`; compare client-side, decision 0020). **Decisions 0020/0021/0024/0025 encoded; OQ-056/061/063 cross-referenced.** REUSE: `CommunityGallery`/`CardDetail`/`EquipReadout`/`AdoptCount`/`ConfirmSheet`/`GameCard`/§1.6 lifecycle/shell. §2.9 updated. Source: `game-page/game-page-states.html`. | CARD-01/22, COL-02..06, SOC-11, MOD-02/08/09 |
| 2026-06-18 | 0.22 | **DS conformance sweep across all `*-states` boards (owner-directed).** **F-06 ✅ (resolves the 0.19 OQ-066 ripple debt):** every off-scale on-screen Chakra-Petch size snapped to **21/15/11/9** on all 13 boards (exempt: card plates · artboard chrome · the simulated OS keyboard); `game-page-states` was the conformant reference — verified 0 non-exempt off-scale remain. **F-05 ✅:** the text-input caret → `scr.accent` (was pink `brand.accent`; shell LEDs stay pink). **F-03/F-07 ✅ (source hygiene):** folded the flat-button override into the base CSS — deleted the dead `shadow.key` 3D + the C5-overridden base `border-radius` (render byte-unchanged); shell 3D, clip-path, scanline press, screen-well radius, text-input insets all kept. **F-01 (in progress):** ratified **`GameCard/cell` 96×134** (the 3-up grid / picker cell) here; the owner-decided board resizes — mid cluster 84–94→`/cell` · large 108–150→`/hero` 138×193 · small 60/78→`/mini` 64×89 — are **OWED** (the agent sweep hit the session usage cap; apply on reset). Left/noted: sliders + toggle knob + the `.schips` segmented control (OQ-063) + the retired `discover-states-fan` (still 3D). | OQ-066 resolved · F-01 |
| 2026-06-23 | 0.23 | **Friends (3.3) + Find/Add (4.8) formalized (friends + find-add tracks).** §1.5 gains the **Friends/Social set** (`FeedRow`/`ActivityRow` · `FriendRow`/`FriendTile` · `RequestRow` · `InviteHook` · `PersonRow`/`SearchResultRow` · `QrCard` · `InviteLanding`/`SenderSummary`; **`PresenceDot`/`StatPeek` CUT**, owner 2026-06-18) + **§2.10 Friends** / **§2.11 Find-Add** compositions (the SOC-06 feed-first tab + the relationship-state-machine find/add; lifecycle + quiet/empty drawn; flat buttons Scanline; F-06 clean). API page-audited → contract **0.23** (`/me/feed` item shape OQ-071 · `/users/search` PersonRow+`relationship` OQ-072 · `/invites/:token` resolve+QR OQ-073 — moved to Resolved). Gate ruling: **A "Feed-first" won**, B/C retired. **OQ-075** (recommend-compose surface) stays open. | SOC-01/06/07/08/09/10 |
| 2026-06-23 | 0.24 | **F-02 gold clarification (owner-ratified — Burt conformance audit).** Gold's intent widened from *card-creating only* to **card-creating OR the PIXELS economy**: gold legitimately marks the currency mark, `PriceChip`, `PackTile`, and `BuyBar`/CLAIM keycaps on commerce surfaces (Store · the Discover/Add commerce hooks · device-editor acquire) — **gold is the PIXELS economy's colour** (ECON-01). System-orange stays the **prominent non-card, non-commerce** action (RETRY · ADD FRIEND · SHARE). Gold stays **off** secondary / incidental controls (SHARE · nav) — fix applied: `canvas` SHARE-THE-CARD keycap (publish already fired) demoted gold → orange. No token change; the F-02 prose + the Catalog Foundation-Rules card + the Burt F-02 check updated in tandem. Source: the 13-board Burt conformance audit (2026-06-23). | F-02 · ECON-01 |
| 2026-06-24 | 0.25 | **Collection card peek-flip (§2.1, decision 0026 — reverses OQ-059/0025).** +COL-12: a **shelf+grid** card **flips in place to the CARD-01 stats back** — **tap flips · tap-back returns · `KeycapButton/secondary` VIEW GAME → Game page (4.2) · long-press shortcut**; dense-list excluded (row-tap stays open-page); transient peek via `motion.cardFlip` (reduce-motion fallback); **no on-face indicator** (owner) — discoverability via first-run coachmark, the flip is a tap (non-gesture, CARD-16); friend-view parity with the privacy-gated back (COL-10/11; notes/rating → 🔒 PRIVATE). +2 state-matrix rows. The back also prints the **game title** as a header above the stat rows (the flip hides the front plate, so the back re-states the game; existing CAT-02 identity data — presentation only). **Board:** the COL-12 peek-flip stage (grid · shelf · friend) was folded into the canonical `collection-states.html` (variant retired, 2026-06-24). REUSE: the game-page `DualFaceHero` back (`.flipy` + `cb-*`) + `GameCard`. | COL-12, CARD-01/16 |
| 2026-06-24 | 0.26 | **Compare Hours (4.6) formalized (compare track).** §1.5 gains the **Compare set** — `CompareHeader` (who-vs-who) · `CompareTotals` (the totals **face-off** + margin bar + `scr.accent` who's-ahead pill + games sub-face-off) · `ComparePair`/`CompareRow` (the per-shared-game **card-vs-card** matchup — their `GameCard/cell` on the game frame, yours on the slate+foil frame, the hours between, **winner in `scr.accent`**; card-tap → Game page §4.2, SOC-11) · `FriendsLeaderboard`/`LeaderRow` (friend-cohort rank, your row a flat accent-tint — **no left-edge rail**, OQ-078). + **§2.12 Compare Hours** composition + full matrix (P1 has-overlap · P2 no-shared-games · P3 leaderboard · P4 Skeleton · P5 privacy-limited PROF-03 · L1 Signal-Lost · L2 Offline SYS-10). **API page-audited → contract 0.25** (`/me/compare/:friendId` payload shape + PROF-03 gating, OQ-074). Gate: owner picked **B "Versus / head-to-head"**; A/C retired. **Non-commerce** (no gold), **completion % OUT**, read-only. §2.9 updated; **OQ-074 + OQ-077 moved to Resolved**. REUSE: `GameCard/cell` · `#art-*`/`fr-*` frames · `Avatar` · the §1.6 lifecycle · shell. Source: `compare-hours/compare-states.html`. | SOC-03 · PROF-03 · ECON-01 |
| 2026-06-25 | 0.27 | **Welcome & Auth (4.13) + Onboarding (4.14) formalized (decision 0029).** §1.5 gains the **`NavBand/locked`** pattern (logged-out grayed/non-interactive). New **§2.13 Welcome & Auth** (games-forward landing — showcase + `SYS-12` system stats + contribution tagline; sign-in-first form; W1–W11) + **§2.14 Onboarding** (guided-seed: one header SKIP · CAT-09 popular rail · NOTIF-04 soft pre-prompt · faithful populated finale · zero-add friendly nudge OQ-B; O1–O10 + lifecycle). Behaviour rulings (zero-add no-seed · NOTIF-04 no re-offer · not-resumable) + new public endpoints **AUTH-11** / **SYS-12** in decision 0029 (product-spec 0.25 · api 0.26). Games-forward chosen over device-hero (kept for history). §2.9 updated. Sources: `welcome-auth/welcome-auth-states.html`, `onboarding/onboarding-states.html`. | AUTH-11 · SYS-12 · NavBand/locked |
| 2026-06-25 | 0.27 | **Card-back label reworded "CARD ART DESIGNED BY" → "CARD ARTIST"** (owner directive; product-spec CARD-01 0.26) + the **designer name sized to the label** (a quiet, uniform credit — print, F-06-exempt). Synced §1.5 `DualFaceHero` · §2.1 Collection peek-flip · §2.4b PLAY M1 · the Phase-C/D card-back note. Rippled to the canonical boards (collection-states + game-page-states; `.cb-prov .p-name` 12→7.5px); retired lineage boards left as history. Presentation only — no token/component/API change. | CARD-01, COL-12 |
| 2026-06-27 | 0.29 | **§2.2 Profile STATS de-welled (owner directive).** The STATS section drops its enclosing `Well` — the six `StatTile`s now sit **flat on the screen plane**. The well's old 12px inner pad was **zeroed** so the tiles share the screen-gutter edge with TOP 5 and the identity/strip wells (verified flush 21→355px), rather than reading as a phantom 12px inset once the box is gone. Matches the just-converged **Contributor profile (4.9)**, which de-welled its stats. **Rippled** to the canonical board `profile/profile-states.html` (all 6 STATS states). Presentation only — no token/component/API change; `StatTile`/`PctPill` unchanged (PROF-07). |
| 2026-06-26 | 0.28 | **F-05 on-screen position pips squared (owner directive).** F-05 reworded: the **shell LEDs** (the `NavBand` active pip + the power LED) stay **round** lit pips, but **on-screen position pips** (CardFan / carousel swipe dots) become **square, pixel-stepped, flat** — the `StateMark` notch geometry (`scr.accent` lit / `scr.grip` off, 6px, no glow), on-screen chrome per F-07 — completing the on-screen-chrome logic the 2026-06-15 marker pass began (markers went square then; the round-orange position pips were the remaining holdout). **Rippled:** the Catalog F-05 card + its 3 carousel specimens (HTML → v0.10) + the `.fdot`/`.sdots` rule on add-game · onboarding · styler · styler-flat-chips. **Not swept:** on-screen status/notify dots (`er-dot`/`kb-dot`) — a separate role, left round lit pending owner. No token change (reuses `StateMark` geometry + `scr.accent`/`scr.grip`). | F-05 · F-07 |
| 2026-06-27 | 0.31 | **Contributor profile (4.9) formalized (catalog track; decision 0032).** §1.5 gains the **Contributor-profile set** — a Profile-grammar recomposition with one net-new component, **`SectionEmpty`** (the per-section empty nudge: gold acquisitive `DESIGN A CARD` / neutral `ADD A GAME`); the **CAT-10 contributor standing** is the existing **`PctPill`** (PROF-07) worn on the contributor `StatTile`s (percentile vs the *contributor cohort*, SYS-04 threshold + PROF-03 privacy gated). New **§2.16 Contributor profile** composition + state matrix (P1 self · P2 empty · P2b self-partial · P3 friend · P3b friend-empty · P4 PROF-03 limited · V1/V2 VIEW ALL · L1–L3 lifecycle). Spec ripple (decision 0032): product-spec **CAT-07** revised (drop CAT-06 field-edits + achievement badges, add standing + collections-reached + VIEW ALL) + new **CAT-10**. **API page-audited → contract 0.28** (`GET /users/:id/contributions` friend/full vs non-friend/limited shape + paginated `…/cards?cursor=` · `…/games?cursor=`). Reuses the §2.2 Profile STATS de-well (0.29). §2.9 updated; **OQ-079 → Resolved**. Gate: owner picked **B · The Trophy Wall**, iterated to the Profile-like layout; A/C retired. Source: `contributor-profile/contributor-states.html`. | CAT-07 · CAT-10 · PROF-03/07 |
| 2026-06-27 | 0.32 | **Welcome & Auth (4.13) — sign-in declunked to Draft A + the state matrix completed (owner-picked, this session).** §2.13 updated: the **sign-in form** converged to **Draft A** across W1/W1b/W5/W11 (one orange SIGN IN primary · **FORGOT? inline** on the password label · a **compact `Sign in with Apple`** under OR CONTINUE WITH, iOS · **CREATE ACCOUNT** demoted to a `TertiaryLink` footer; Android drops Apple + the OR row, AUTH-03). **State matrix expanded 13 → 18 cases** so the engineer has every beat: +**W5b** rate-limited cooldown (`RATE_LIMITED` 429, SYS-05) · +**W5c** suspended / catch-all submit error (`ACCOUNT_SUSPENDED` MOD-09 / post-validation `SERVER_ERROR`) · +**W8c** reset link expired/used (invalid-token → loops to W7) · +**W12** submitting / in-flight (`KeycapButton/busy` → no double-submit) · +**W13** `Skeleton`/first-paint (SYS-12 stats degrade-hide), plus explicit **error-routing** (answerable error → inline beat W5/W5b/W5c; unanswerable → W10 Signal-Lost / W11 Offline). Polish: lifecycle **`seal`/`LoadError` badges squared** (full squares, F-07); banner `<use>` glyphs recoloured (filled Apple mark → soft, line-art → accent — no black icons); card-artist credit gold all-caps (F-02). **No new product-spec IDs / endpoints** — every state maps to existing AUTH/SYS/MOD IDs + the standard error model (api 0.26). Burt PASS. Source: `welcome-auth/welcome-auth-states.html`. | AUTH-02/04 · SYS-05/12 · MOD-09 |
| 2026-06-27 | 0.30 | **Device editor (4.5) formalized (device track; decision 0030).** §1.5 gains the **Device-editor set** — `SectionSwitch` **unified** (`/pair`·`/chips`·`/rail` variants; `SegmentedKeycap`/`SectionChips` retained as aliases, OQ-063) · `SectionCard` · `StickerStage` · `TransformBox` · `PlacedSticker` · `StickerTray` · `SavedLook` · `LooksGrid` · `KeepBar` — + new **§2.15 Device editor** composition + state matrix (in-frame board: D1–D6 edit/looks · D7/D7b/D7c premium-cart · D8–D10 lifecycle). Four rulings (decision 0030): OQ-062 (`stickerComposition` shape + plastic-only zones, server-validated) · OQ-063 (`SectionSwitch` app-wide → one grammar + variants; converged Discover/Styler untouched) · OQ-064 (saved looks = `device_looks` snapshots, no name, delete + cap; **DEV-05**) · OQ-065 (premium live try-on → atomic KEEP-batch, owned-only device). **API page-audited → contract 0.27** (`stickerComposition` shape + `GET/POST /me/device/looks` + `DELETE`). §2.9 updated; **OQ-045/062/063/064/065/076 → Resolved**. Gate: owner picked **A "in-frame live edit"**; b/c + switcher takes retired. Source: `device/device-states.html`. | DEV-01/03/05 · COSM-03 · ECON-07 |
