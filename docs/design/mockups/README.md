# Design mockups (working artboards)

Standalone HTML artboards from the Claude Design iteration, checked in for review + git history.
**Requirements truth stays in [`../ui-design-requirements.md`](../ui-design-requirements.md)** —
these are drafts feeding the eventual `design-spec.md`, not a source of truth.

**Layout:** one folder per page/screen (matching the rows in [`../SCREEN-STATUS.md`](../SCREEN-STATUS.md));
`design-system/` holds cross-app system drafts. The **Design System Catalog stays at this top level**
(owner directive). Within a folder, the live file is the one **not** marked "superseded" — drafts are
kept for history.

| File | What it is |
|---|---|
| `InGame Design System Catalog.dc.html` | **The component catalog (visual)** — the design-spec §1.5's twin, **v0.4**. v0.1 Phase B extraction (foundation rules F-01..08, tokens, Keycap buttons, GameCard sizes, screen furniture) · v0.2 the §1.8 States & Feedback family + in-place search + offline + avatar badge · **v0.3 (store track, 2026-06-12): the PIXELS currency mark + the Currency & Commerce set** — `CurrencyCounter` (+negative/tick) · `PriceChip` + adopt chip · `BuyBar` **hold-to-buy** (idle/holding) · `PackTile` (+`/starter`, value math, $ on `/secondary`) · `LedgerRow` (earn/spend/reversal) · `Toast` banner · `OwnedTag`/`LockedTag`/`EarnedOnlyTag` · `DailyBonusBar` · `AisleIndex` · `PreviewStrip` — and the gap list pruned to match · **v0.4 (add-game + styler tracks, 2026-06-12): the Forms & Flow set** (Add Game) — `FlowTakeover`/`FlowHeader` · `TextField` · `SelectField` · `ResultRow` · `InlineBanner` · `CardFan` · `CardDetail` · `EquipReadout` · `CleanPeek` · `ReportSheet` — **plus the Editor Set** (Styler) — `AttributeSection` · `SectionChips` · `BaseRail` · `IntensitySlider` · `ReconcileSheet` · `KeepBeat`. *Claude Design export shell (`support.js` not checked in); renders fine standalone.* |
| `design-system/button-system-drafts.dc.html` | Three in-app button languages (Keycap / HUD Hairline / Printed Plate) on the Midnight theme, each with the full vocabulary + the Collection tools bar rebuilt in that language. *Claude Design export — references a `support.js` runtime that isn't checked in; renders fine standalone except Take B's decorative corner brackets.* |
| `collection/h2-underlay-v2-c5-hybrid-ds-enforced.html` | **Collection + Profile** artboards, corner system C5 hybrid (square chrome, stepped-diagonal cards; Destiny = All-Time Pick, 210 hrs everywhere). **Updated 2026-06-10 per decisions 0011/0012:** privacy chip removed from the Profile header (`PROF-03` lives in Settings) and the theme-changer slot is now the **Settings gear** (screen theming moved to the Device editor, `DEV-04`). Has the tweaks-panel runtime (shell colors + screen themes). |
| `collection/collection-states.html` | **Collection states + view modes** (standalone, built-in SVG art): the first-run empty (`AUTH-06`), the pulled-up sort/filter drawer (`COL-07`/`COL-09` + the OQ-031 manual-order and OQ-034 keycap-interaction proposals), **the in-place search state** (keyboard up — `COL-09` live-filter; OQ-035 keyboard treatment), and **the three COL-07 view modes made explicit** — shelf (hero rows w/ stats — pending formalization, OQ-033) / grid (2-wide faces, the h2-underlay layout) / list (strip rows, hours/status scan) — with the Now-Playing hero persistent in all three. **Now at Profile parity (2026-06-11):** **friend's Collection** ×2 (`COL-10` — your chrome + the "view in their device" toggle, decision 0012) · **Now-Playing-unset nudge** (§3.1) · the §1.8 **lifecycle** (skeleton · load-error+retry) · **terminal "unavailable"** (`MOD-09` non-disclosure) · **offline** ×2 (own from cache, read-only · a friend you can't reach — `SYS-10`). |
| `collection/ratchet-art.png` | Low-poly card-art asset used by the h2-underlay artboards (the Marathon PNG is still missing from the export — Marathon falls back to its built-in SVG symbol). |
| `profile/profile-states.html` | **Profile state matrix** (standalone, built-in SVG art) — the §3.5 set: **edit mode** (in-place; identity + curation per `PROF-01/02/06/08`, Top-5 re-rank via the OQ-031 gesture), **fresh account** (inviting empties + `PROF-07` chip-less tiles), **friend view** ×2 (your chrome default + the "view in their device" toggle, decision 0012), **privacy-limited** (`PROF-03` non-friend card), the §1.8 lifecycle pair (skeleton loading · load-error + retry), and the **terminal "unavailable"** states (blocked/suspended/deleted → one generic mask + the Unblock exception, `MOD-09`), and the **offline** states (own profile from cache, read-only + lightweight indicator · a friend you can't reach gets a calm notice — `SYS-10`). Maverick = self, Riko = friend, Vanta = non-friend (2026-06-11). |
| `add-game/add-game-draft-a-wizard.html` | **Add Game — Draft A "Stepped wizard"** (standalone, built-in SVG): the 4.1 flow as discrete pages — SEARCH → DETAILS → CARD, `FlowHeader` step dots + BACK/NEXT keycaps, one decision per screen. Panels P1–P8 per the stage-1 plan (P9–P10 deferred to the converged board); `CardPicker` junction (decision 0014), shelf-slot + contributor first-credit beats (OQ-040). **Pre-gate draft.** |
| `add-game/add-game-draft-b-inline.html` | **Add Game — Draft B "Single-surface disclosure"** (standalone, built-in SVG): one anchored screen — permanent top `SearchField`, results / create-form / card-step all expand **inline** beneath the chosen game (accordion); no step indicators; multi-add = collapse → tick → field refocus (P8 is the thesis). Panels P1–P8 (P9–P10 deferred). **Pre-gate draft.** |
| `add-game/add-game-draft-c-cardled.html` | **Add Game — Draft C "Card-led"** (standalone, built-in SVG): the card object is the protagonist — fanned **full-face** results (F-01), the picked card held in hand while details attach around it, the create-form **writes onto the title plate**, status as stamps, and the climax: **filing the card into its shelf slot**. Panels P1–P8 (P9–P10 deferred). **Pre-gate draft — superseded by `add-game-draft-c2-cardled.html` (kept for history).** |
| `add-game/add-game-draft-c2-cardled.html` | **Add Game — C-v2** (standalone, built-in SVG): Draft C with the **gate ruling applied** (plan Task 4, 2026-06-12) — `CardFan` (forefront full face+plate, swipe affordance, off-card forefront meta line) · **no status stamp on art** (off-card chips + eyebrow) · `CardDetail` (tap-from-search detail + ADD TO COLLECTION + open report menu, MOD-01) · `EquipReadout` (equipped cosmetics afforded) · `CleanPeek` (hold to view bare art) · thumb-bearing dedup banner. Panels P1–P8 + P3b/P7b (P9–P10 deferred). **Superseded by `add-game-draft-c3-cardled.html` (kept for history).** |
| `add-game/add-game-draft-c3-cardled.html` | **Add Game — C-v3** (standalone, built-in SVG): C-v2 with the **pass-2 notes applied** (plan, 2026-06-12) — **3-up `CardFan`s, all three plates legible** (zero plate occlusion) + the **plate type floor raised** (≥10px forefront / ≥9px neighbors; P8 shelf-minis the captioned exemption) · P1 drops the premature be-first hook · fan↔dots breathing room · **P3b report = bottom drawer** (OptionSheet family, summoned by ⋮, no handle, the page's only report access — MOD-01) · P7b peek label simplified ("CLEAN VIEW") · **"DESIGNED BY …"** credits · **meta-above-the-card** on swipeable views (P3/P7/P7b — readout/meta up top, cards in the thumb zone; P6 the deliberate exception) · ✓ IN COLLECTION meta state now drawn (P3 inset). **Superseded by `add-game-draft-c4-cardled.html` (kept for history).** |
| `add-game/add-game-draft-c4-cardled.html` | **Add Game — C-v4** (standalone, built-in SVG): C-v3 with the **pass-3 notes applied** (plan, 2026-06-12) — **bottom-docked SearchField** in all search panels (the Collection in-place pattern; results above, the **focused card's meta just above the field**; OQ-035 satisfied by construction; supersedes meta-above on P3 only) · **report drawer = selectable reasons + SUBMIT/CANCEL** (MOD-01, §1.8 confirm) · **P7 enlarged** (150×210 forefront, meta high) + per-face **adoption counts** (`CARD-05`) + **"1 / 7" position count** · **`CAT-09` presence stats** ("IN 214 COLLECTIONS · 3 FRIENDS HAVE IT" in P3 meta + P3b ledger — decision 0016) · **"+1" inset tick inside the gold CountKeycap** (P8). Panels P1–P8 + P3b/P7b (P9–P10 deferred). *Currency chips still wear the ◈ placeholder — swaps to the PIXELS mark on the converged board per the currency ruling.* **Superseded by `add-game-draft-c5-cardled.html` (kept for history).** |
| `add-game/add-game-draft-c5-cardled.html` | **Add Game — C-v5** (standalone, built-in SVG): C-v4 with the **pass-4 notes applied** (plan, 2026-06-12) — **P8: DONE cut** (one earned action — ADD & CONTINUE; ✕ + nav are the exits) · **P2/P3 restacked**: focused card's **name/meta above the cards**, the **cards just above the search bar**, dots/SWIPE then the NONE OF THESE hook beneath, field at bottom · **P3b selection = accent border + pip on a flat row** — first application of **F-09 "no sunken containers"** (design-spec 0.9; pressed keycaps + text inputs the named exceptions). Panels P1–P8 + P3b/P7b (P9–P10 deferred; ◈ → PIXELS at converge). **Superseded by `add-game-draft-c6-cardled.html` (kept for history).** |
| `add-game/add-game-draft-c6-cardled.html` | **Add Game — C-v6** (standalone, built-in SVG): C-v5 with the **pass-5 notes applied** (plan, 2026-06-12) — **"bronze" buttons → orange** (`scr.btn` retired, design-spec 0.11: VIEW MATCH / NEXT / ADOPT now `scr.accent`) · **P3b report baseline**: orange selection border (F-09) + **red SUBMIT** (`/destructive`) + **INCORRECT INFO's required details field** (`TextField/area`; MOD-01 0.16) · **P7/P7b readout in ONE position** (high, both panels; CleanPeek dims the top readout) · **P8 pure celebration** (shelf strip cut — the filed card is the hero) · **never-blank be-first rail** (TEMPLATE · DEFAULT · SURPRISE ME — CARD-16/18 finally drawn). Panels P1–P8 + P3b/P7b (P9–P10 deferred; ◈ → PIXELS at converge). **Converged into `add-game-states.html` (kept for history).** |
| `add-game/add-game-states.html` | **Add Game — CONVERGED board** (standalone, built-in SVG; decisions 0014–0016 + six owner passes): the full **4.1 state matrix** — P1 entry fans · P2 typing (bottom-docked field riding the keyboard — OQ-035 by construction) · P3 results (name/meta above the cards, cards above the bar, `CAT-09` presence meta) + own-it inset · P3b `CardDetail` + **`ReportSheet/drawer`** (variation A ruled: "SUBMIT REPORT" red `/destructive`, required details `TextField/area`, orange F-09 selection) · P4 no-results blank · P5 create + thumb dedup banner · P6 status off-card · P7 `CardPicker` (`EquipReadout` high, `CARD-05` adoption counts, **never-blank be-first rail** — CARD-16/18) + insets · P7b variant + `CleanPeek` · P8 filed **celebration** (the card is the hero) · **P9 lifecycle** (`Skeleton` · SIGNAL LOST + RETRY) · **P10 offline writes-gate** (`SYS-10`) — **PIXELS mark** in every currency slot (◈ retired); picker folded into `.cfan.pick`; one shared plate-band recipe. 13 devices + 3 insets. **Stage 1 complete — awaiting design-spec formalization (spec-owner batch).** |
| `report-sheet/report-sheet-drafts.html` | **ReportSheet — 3 variations ×2 contexts** (standalone, built-in SVG; owner pass-5 ask): the app-wide report pop-up as **A `/drawer`** (bottom sheet over scrim, inline details disclosure) · **B `/modal`** (centered dialog, the conventional pole) · **C `/takeover`** (FlowHeader flow, one decision per zone) — each on a **catalog entry** (DUPLICATE · INCORRECT INFO + **required details**, MOD-01) and a **user** (reason set + **BLOCK USER** distinct from filing — outlined alert strip past an OR divider, instant/silent, SOC-09). Debuts **`TextField/area`**; shows **dormant vs armed red SUBMIT**; orange selection + pink pip throughout (F-09/v0.11). **RULED: A (`/drawer`) won — first normalized inside `add-game/add-game-states.html` P3b, then converged into its own board `report-sheet/report-states.html`; this file stays the historical draft artifact (B/C losing history).** |
| `report-sheet/report-states.html` | **ReportSheet — CONVERGED board** (standalone, built-in SVG; A `/drawer` ruled): the §4.16 report pop-up lifted out of the drafts into its own state matrix, faithful to Add Game **P3b** + design-spec **§1.5** — **3 targets** (published **card** · **catalog entry** incl. report-**duplicate** · **user** with **BLOCK** offered alongside, SOC-09) across the full lifecycle (9 devices): launch from the ⋮ overflow · reason picker · the required-details note (MOD-01; moderator-only, **outside MOD-07**) · dormant SUBMIT (no reason · details-required-but-empty) · **in-flight** (keycap held pressed, hold-to-buy lineage) · **filed confirmation** (calm in-sheet beat; reflects MOD-02 soft-hide **without exposing thresholds**) · **post-block confirmation** (severed · mutually invisible · silent · managed in Settings) · **offline writes-gate** (`OfflineStrip`, SYS-10) · submit-error **`Toast`** + orange RETRY (§1.8). One grammar everywhere: flat F-09 reason rows (orange select border + pink pip) · `TextField/area` cream inset · red `KeycapButton/destructive` SUBMIT. **Report-track converge complete — API ✅ page-audited (`POST /reports` {card\|game\|user + reason + `details`} incl. duplicate + `/me/blocks`, SOC-09; all drawn affordances backed, no OQ); Design-spec formalization of the lifecycle cells (in-flight · filed/post-block confirmation · offline-gated → §1.5/§1.6) owed (spec-owner batch).** |
| `store/store-draft-a-shelf.html` | **Store — Draft A "Arcade shelf"** (standalone, built-in SVG): the Store as a **stocked shop interior** — lit **marquee drop case** (`ECON-08`) · category **shelf planks** + ALL-AISLES directory (full `COSM-01` taxonomy incl. screen themes + currency; `COSM-02` free-baseline shop sign) · detail = **counter-inspection overlay** with `PreviewStage` on YOUR card / YOUR device (F-01 full faces) · short-by-N **bridge** with `PackTile`s at the point of intent · Wallet as **the till** (gold readout + cream paper-receipt `LedgerRow`s, incl. the `ECON-09` **negative variant**) · `Toast` = service-bell strip + orange RETRY · **EARNED-ONLY glass trophy case** (`COSM-04`). Panels S1–S8 (lifecycle deferred to converge). **Pre-gate draft (store track).** |
| `store/store-draft-b-drops.html` | **Store — Draft B "Drops-led editorial"** (standalone, built-in SVG): the Store as a **magazine** — the seasonal drop is the **cover story** (`ECON-08` leads), curated **editor's-pick modules**, full `COSM-01` taxonomy as **THE INDEX** at the back · item detail = **takeover spread** (FlowHeader ◂, `PreviewStage` hero on YOUR card, standfirst + spec chips, **`BuyBar` pinned** as a checkout rail) · **screen-theme detail re-themes the whole page** (Deep Sea live preview + gold PREVIEWING strip, `DEV-04`) · bridge grows **in the spread** (`PackTile` = price-list rows) · S5 shows the **native IAP sheet** half-raised (`ECON-06`) · Wallet = **bank statement** (TODAY/EARLIER groups + `ECON-09` negative variant) · `Toast` = full-bleed under-header banner + RETRY · **THE TROPHY SHELF** earned-only module (`COSM-04`). Panels S1–S8 (lifecycle deferred). **Pre-gate draft (store track).** |
| `store/store-draft-c-rack.html` | **Store — Draft C "Category rack"** (standalone, built-in SVG): the Store as a **record-store rack** — every `COSM-01` aisle is a **divider chip on a persistent rail** (whole taxonomy always visible; DROPS gold + a slim pinned **drop bar**, `ECON-08`) · dense uniform **ItemTile grid** · detail = a **bottom sheet over the rack** (same pulled-up family as Collection's sort/filter drawer; `PreviewStage` on YOUR card / before→after device pair, `BuyBar` as the sheet's bottom row) · bridge grows **inside the sheet** (`PackTile`s at intent) · currency is **just another rack** (+ ↺ Restore, `ECON-06`) · Wallet = **balance plate + dense utility `LedgerRow`s** (+ `ECON-09` negative variant) · `Toast` = classic floating bottom toast + RETRY · **EARNED-ONLY gold sub-rack** (`COSM-04`). The conventional-commerce pole of §1.1. Panels S1–S8 (lifecycle deferred). **Pre-gate draft (store track).** |
| `store/currency-identity-drafts.html` | **Customizer currency — identity drafts ×3** (standalone, built-in SVG): three name+mark directions for the in-app face of the `ECON-01` currency (spec term "Customizer currency" unchanged; the winner replaces the ◈ placeholder app-wide incl. the Add Game adopt chip) — **A "CREDITS"** (the cabinet's own word; milled gold token punched with the card-step square; ticker CR) · **B "INK"** (the print-shop material cards are pressed in — ties the OQ-040 "first print" ritual; gold drop; mass noun, packs become REFILLS; *FOIL rejected — collides with the foil finish type*) · **C "VOLTS"** (the device's charge; gold cell + dark bolt, F-07 square + F-05 glow kin; login bonus = trickle charge). Each shown in chrome context: `CurrencyCounter` (+ ECON-09 negative) · `PriceChip` · adopt chip · `PackTile` · `LedgerRow` (draft C's form, per the store gate ruling) + grammar/risk notes. **Superseded by `currency-identity-drafts-v2.html` (kept for history).** |
| `store/currency-identity-drafts-v2.html` | **Customizer currency — identity drafts v2 ×3** (standalone, built-in SVG): per the owner's currency ruling #1 (2026-06-12) — the pixel-stepped shape comes **out of the coin**: each mark IS a stepped form, **gold with blue streak emphasis** (blue = mark-internal cyan accent, never chrome; gold stays the value marker, F-02), with a new name — **A "BITS"** (the GameCard's stepped silhouette struck as the unit itself, blue foil slash on the step diagonal; "costs 8 BITS" names the aesthetic; optional powers-of-two pack flavour) · **B "PIXELS"** (the ◈ placeholder rebuilt as a pixel-cut gem — stepped facets, blue glint pixels + blue-lit stair edge; ticker PX, the tightest chip) · **C "SPARKS"** (a gold spark climbing its own staircase, blue motion trails; the kinetic one). Same chrome-context battery as v1 (`CurrencyCounter` + negative · `PriceChip` · adopt chip · `PackTile` · `LedgerRow`/C) + grammar/risk notes. **RULED: PIXELS won (ruling #2) — carried into `store-states.html`.** |
| `store/store-states.html` | **Store — CONVERGED board** (standalone, built-in SVG; owner rulings #1–#3 applied, 2026-06-12): **B-led hybrid** — **DAILY BONUS claim bar** (+1 PX picked up ON the Store, OQ-043; claimed-state fragment) · tightened **cover-story drop** + **denser 3-up browse grid** (6 categories of `ItemTile`s) + **THE INDEX** (incl. **DEVICE SHELLS**, OQ-042, and **PIXELS → TOP UP**) · detail = **C's bottom sheet** (`PreviewStage` on YOUR card; gold-edged source tile) · **P1b: the drop page as a drawer** (issue header + countdown + the drop's ItemTiles, same sheet grammar) · **P2b: multi-item pack contents** (glyph packs are **SHELL STICKER PACKS**, not card assets — the card canvas already draws, ruling #5/OQ-047; the sheet leads with the full WHAT'S-INSIDE grid of all 12 stickers; placement in the Device editor, on-shell preview deferred OQ-045; debuts **press-and-hold BUY**, the spend-confirm model for all instant-PX purchases — OQ-046) · **whole-page theme PREVIEWING** composed with the sheet (Deep Sea live) · **device shells on the ONE pocket handheld** (no alternate bodies) · **in-sheet bridge** with inline IAP (`PackTile`s at intent; BUY re-arms in place) · **Top Up landing**: **2×2 PackTile grid** + a real one-time **STARTER PACK** ($0.99 → 12 PX, OQ-044) + **per-tile value math** (PX/$ + %-better) + dollar prices on **KeycapButton/secondary** + native sheet + ↺ Restore · success = **the landed moment** (draft B's S6 page verbatim: centered +30 · 5→35 · one gold rule; pack options stay on Top Up) · `Toast` = B's under-header banner + RETRY · Wallet = **B hero + C `LedgerRow`s** + `ECON-09` negative · ownership rows + **TROPHY SHELF** (`COSM-04`; STEP CROWN renamed to dodge the currency) · **currency = PIXELS mark v3** (flat pixel-art facets, opaque ink outline, solid blue glints — reads on gold) replacing ◈ in every slot · **+ the deferred lifecycle cells**: `Skeleton` · `LoadError` "Signal Lost"+RETRY · `Offline` browse-from-cache with **writes gated** (`SYS-10`). P1–P12 + P1b/P2b. **Store-track stage 1 complete — formalized into design-spec 0.10 + catalog v0.3 (owner-directed, 2026-06-12); behavior OQs 041–047 remain with the spec owner.** |
| `styler/styler-draft-a-carousel.html` | **Styler — Draft A "Carousel"** (standalone, built-in SVG; the Card editor's in-frame posture, 0014 stage 2): the mandated carousel treatment — **persistent card hero**, the four closed attributes as **`AttributeSection` pages swiping beneath it** (`SectionChips` jump; P2 drawn mid-swipe) · `BaseRail` start-from (templates · preset kits · 🎲 Surprise me + the **received-base** adopt-then-edit variant, CARD-16; the adopt door stays in Add Game) · F-09 selection tiles · **`IntensitySlider`** (the catalog's missing slider, flagged) · premium = preview-then-acquire with an on-card PREVIEW flag + cost-chip stack (CARD-06/13) → **`ReconcileSheet`** at KEEP (acquire all / per-item remove · hold-to-buy · short-by-12 **bridge** with PackTile minis) · **`KeepBeat`** light beat (0015 tier) + SAVE PRIVATE + the **Canvas stub door** (NO Publish — canvas-tier, 0014) · P8 = the **OQ-039 taxonomy exhibit** (recommendation: adopt NAMEPLATE · defer OVERLAY). Panels P1–P8; lifecycle deferred to converge. PIXELS mark + commerce set reused from `store-states.html`. **RULED: carousel won (gate, 2026-06-12) — converged into `styler/styler-states.html`; kept for history.** |
| `styler/styler-draft-b-drawers.html` | **Styler — Draft B "Drawer rack"** (standalone, built-in SVG; styler track, 0014 stage 2): the sheet-family treatment — the **hero stays maximal** (184×257, the app's biggest card) and the closed attributes are **drawers pulled from a docked rack**: `SectionChips` worn as **drawer pulls carrying live swatches** of their current value (the rack = a composition readout), the drawer rising FROM the rack (pulls stay reachable; pulled → keeps its grab handle) with **NO SCRIM over the hero** (the live preview stays lit — flagged judgment call) · one drawer at a time = CARD-12 made spatial · `BaseRail` = the START drawer (grid form; DEFAULT/template/kit/🎲 + the received-base variant) · 6-up F-09 tile grids · `IntensitySlider` docked in the EFFECT drawer · the **`ReconcileSheet` is the one drawer that DOES scrim** (commerce = conventional, §1.1; same CARD-13 anatomy: acquire all / remove · hold-to-buy · the PackTile bridge) · `KeepBeat` at hero scale + SAVE PRIVATE + the Canvas stub door (no Publish) · P8 exhibit notes the rack's **5-pull density ceiling**. Panels P1–P8; lifecycle deferred. **Retired at the gate (carousel won, 2026-06-12) — kept for history.** |
| `styler/styler-draft-c-workbench.html` | **Styler — Draft C "Workbench"** (standalone, built-in SVG; styler track, 0014 stage 2): the zero-modality treatment — a **persistent cockpit**: hero (150×210) · **c6's `EquipReadout` reused** as the always-on composition readout (premium chips burn gold; EMBER carries its **70%** — intensity prints into the readout) · a fixed **tray** swapping with the active tool (**8-up thumb-swatch FRAME tray** — the densest browse of the three; `IntensitySlider` beside what it tunes) · a **ToolsBar bench**: grip + `SectionChips` worn as **icon ToolKeycaps** (BASE/FRAME/EFFECT/FINISH/TITLE; active = pressed + pip, Collection's grammar) + KEEP — **one tap swaps sections, nothing slides** · `BaseRail` = the BASE tool's tray (+ received-base variant) · quiet outcomes under the readout (SAVE PRIVATE · the Canvas stub door; no Publish) · the **model-invariant `ReconcileSheet`** (identical across A/B/C — deliberate) · `KeepBeat` light tier · P8 exhibit shows a **sixth tool still fits** (the bench scales furthest). Panels P1–P8; lifecycle deferred. **Retired at the gate (carousel won, 2026-06-12) — kept for history.** |
| `styler/styler-states.html` | **Styler — CONVERGED board** (standalone, built-in SVG; gate ruling 2026-06-12: *"I'll take carousel, only note is add nameplate remove overlay"*): **Draft A's carousel grammar** — persistent hero, `AttributeSection` pages swiping beneath, `SectionChips` to jump — now with **FIVE closed attributes: FRAME · EFFECT · FINISH · NAMEPLATE · TITLE** — **P5b NAMEPLATE is the ruled 5th type** (the plate *object*: SLAB · RIBBON · BEVEL · premium HOLO PLATE; the hero's plate notches live; TITLE re-scoped to **font + ink**) and **OVERLAY is cut** (P8 records both: NAMEPLATE cell ADOPTED, OVERLAY cell CUT) · `BaseRail` start-from + the received-base variant (CARD-16) · `IntensitySlider` (the new catalog slider; the value prints into the reconcile row) · preview-then-acquire with on-card PREVIEW flag + cost-chip stack → `ReconcileSheet` at KEEP (acquire all / per-item remove · hold-to-buy · the short-by-12 PackTile bridge; rides 0017's `POST /cosmetics/:id/acquire`) · light `KeepBeat` (0015) + SAVE PRIVATE + the Canvas stub door (**no Publish** — canvas-tier) · **+ the deferred lifecycle cells**: `Skeleton` (solid fills) · `LoadError` "Signal Lost"+RETRY (draft-safe copy, CARD-14) · `Offline` **draft-safe** (free/owned from cache; premium acquires + KEEP **writes-gated**, SYS-10). Panels P1–P8 + P5b + P9–P11. **Styler-track stage 2 complete — formalization (design-spec §2.x · catalog · the OQ-039/COSM-01 ripple) = spec-owner batch; OQ-048/049/050 in the inbox.** **Owner edit 2026-06-13: the EDIT ART → Canvas door is now a persistent ORANGE `CANVAS` chip in the SectionChips row** (a posture switch coloured the action-orange, present on every editing panel — EDIT ART one tap from anywhere); the under-hero dashed door retired. *(design-spec §2.5 re-sync owed — spec-owner.)* |
| `canvas/canvas-draft-a-hud.html` | **Canvas — Draft A "Floating HUD"** (standalone, built-in SVG; the Card editor's breakout posture, 0014 stage 3): the **TOTAL-breakout** OQ-007 proposal — the frame **fully yields** (P1 drawn mid-yield: dashed ghost shell receding past the glass), edge-to-edge void stage; tools are **floating HUD clusters** and the thesis is the **contextual pill that rides the selected element** (P2: ring + handles + rotate stem + FILL/STROKE/FX pill) · `AssetShelf` (free-baseline glyphs + the colour/gradient BASE row, CARD-02/17) · the pill expands into the **`ElementTray`** (opacity on the IntensitySlider form · fill/eyedropper/gradient · stroke/glow/blend/flip/radius; text variant w/ ARC curve, CARD-10/11) · floating **`LayerRack`** (eye/lock/grip rows · GROUP/DUP/RENAME/DEL · the **9/30 cap meter** + the **30/30 at-cap state**, OQ-008 ruling) + smart guides + numeric popover (CARD-08/09) · **hold-to-preview** = the true-to-life flattened card with the Styler's closed layers live (CARD-15/0015) · unsaved-exit guard + autosave (CARD-14) · **P7 publish sheet**: CARD-19 checks as a visible checklist + gold PUBLISH (reconcile routes to the Styler's `ReconcileSheet` if premiums unowned) · **P8 = the FIRST-PRINT ritual** (OQ-040 full tier: printing scan-line · assembly replay · gallery staging + SHARE/NOTIF-04 routing). Panels P1–P8; lifecycle deferred. **Retired at the gate (C "print shop" won, 2026-06-13) — kept for history.** |
| `canvas/canvas-draft-b-rails.html` | **Canvas — Draft B "Cockpit rails"** (standalone, built-in SVG; canvas track, 0014 stage 3): the **PARTIAL-breakout** OQ-007 proposal — the shell **slides off the bench** (P1 mid-slide) but **the bezel persists** as a thin workbench frame: still the device, opened for work · **zero modality** (the Styler-cockpit lineage): a persistent **left tool rail** (ADD · LAYERS · COLOR · the 👁 HOLD; NEXT waits gold at the rail's foot) + a persistent **bottom tray that RE-AIMS** with the active tool/selection — nothing floats, nothing summons · ADD lives **in-tray** (categories + free glyphs + the BASE gradient row) · the full `ElementTray` (CARD-10/11 + the text/ARC fragment) · the **`LayerRack` as a horizontal FILMSTRIP** of layer thumbnails (z-order = sideways drag; eye/lock/dup badges; 9/30 + the 30/30 at-cap fragment, OQ-008) · hold-to-preview = the chrome **sleeps** (dims, never moves) while the true card shows (CARD-15) · P7 finish-up **in-tray** (CARD-19 checklist + safe-area-on preview + PUBLISH gold; reconcile routes to the Styler's sheet) · P8 ritual = **the bench goes dark** (the cockpit's one theatrical moment). Panels P1–P8; lifecycle deferred. **Retired at the gate (C "print shop" won, 2026-06-13) — kept for history.** |
| `canvas/canvas-draft-c-press.html` | **Canvas — Draft C "Print shop"** (standalone, built-in SVG; canvas track, 0014 stage 3): the **DIEGETIC breakout** OQ-007 proposal — the shell **swings open like a cabinet** (P1 mid-swing on its hinge) onto a **workshop bench**; the composition lies on a **press bed** with registration corners · the thesis is **layers-first**: layers are **physical SLIPS** (printing plates) fanned in a rack at the bench's foot — **pull a slip to edit that layer in ISOLATION** (everything else ghosts to 28%; CARD-08's stacked-tap problem solved spatially) · z-order = the rack's left-right order (drag to refan); long-press ops row (RENAME/LOCK/HIDE/DUP/GROUP/DELETE) · tools arrive as the **app's one bottom-sheet grammar** (pulled → handle): the ADD sheet (a new glyph joins the rack pulled) + the EDIT sheet (CARD-10/11; no scrim-dim on the work) · **hold-to-preview = PROOF** — a **persistent `👁 PROOF` key on the bench row** (press-and-hold; P2/P5), the press stamps the true print (CARD-15, diegetically named) · the cap is physical: **the rack fills at 30** (OQ-008) · P7 = the PRESS finish-up sheet (CARD-19 checklist; canonical labels, workshop eyebrows only, §1.1) · P8 = **the press RUNS** (OQ-040's ritual native to the set: platen sweep · the slips fly in · the print lifted off, glowing). Panels P1–P8; lifecycle deferred. **RULED: C won (gate, 2026-06-13) — converged into `canvas/canvas-states.html`; kept for history.** |
| `canvas/canvas-states.html` | **Canvas — CONVERGED board** (standalone, built-in SVG; gate ruling 2026-06-13: *"Let's go with C"* + two changes): **Draft C's print-shop grammar** — the shell swings open onto the workshop; the composition lies on a **press bed**; layers = **physical slips** pulled to edit in **isolation**; tools ride the one **bottom-sheet** grammar (ADD/EDIT sheets); the persistent **`👁 PROOF`** key (hold-to-preview, diegetic, CARD-15); **PRESS** = run-the-press publish (CARD-19 checklist → gold PUBLISH; reconcile routes to the Styler's sheet + `POST /cosmetics/acquire-batch`); **P8 the press RUNS** (OQ-040 full ritual). **Two ruled changes baked in: (1)** Undo/Redo + scoped RESET relocated to a persistent **`editbar` docked just above the slip rack** (thumb reach, CARD-09); **(2)** **PROOF renders the GameCard size ladder** — GRID·96 · MINI·64 · THUMB·44 (design-spec §1.5) so **thumbnail safe-area legibility is verifiable at proof time** (CARD-07/15). **+ the deferred lifecycle cells**: `Skeleton` · `LoadError` "Signal Lost"+RETRY (draft-safe, CARD-14) · `Offline` — **editing + PROOF work from cache** (client-side flatten), **PRESS/publish + premium acquires writes-gated** (SYS-10). Panels P1–P8 + P9–P11. Element cap 30 (OQ-008). **Canvas-track stage 3 complete — OQ-007 + OQ-040 resolved design-side; formalization (design-spec §2.x + the editor's open-composition set) = spec-owner batch.** |
| `discover/discover-draft-a-rooms.html` | **Discover — Draft A "Two rooms"** (standalone, built-in SVG; the §3.2 tab — the segmented-control gap-closer, DISCOVER nav-keycap active): the **clarity/separation** pole — the `UP NEXT ↔ DISCOVER` toggle is a **hard binary**, two purpose-built rooms that share nothing but the control. **Up Next** = a focused ranked management list (Now-Playing pin · drag-reorder `QueueRow`s · source/status tags `WISHLIST`/`REC'D BY`, WTP-01/02/03 · COL-02 · SOC-05); **Discover** = a **sectioned hub** (Upcoming rail w/ NOTIFY-ME · genre/studio chips · Trending peek · search). New **`SegmentedKeycap/pair`** — two keycaps, hard seam, active **pressed + pink PipLight**, *worn as the screen header (no h2; the plastic DISCOVER keycap names the tab)*. P4 browse drill-down (DISC-02, CAT-09 presence) · P5 games search (DISC-03, bottom-docked `ResultRow`+`MatchTag`, in-collection ✓, ⋮ report) · P6 `CardDetail` (presence + credit + ADD-TO-COLLECTION/UP-NEXT) · P7 cold-start (empty queue `EmptyState/inviting` + the Contributor hook, `RankChip`/first, CAT-02) · P8 Trending (DISC-04, `RankChip` + `AdoptCount`, **non-commerce — no PIXELS**). Flagged new: `SegmentedKeycap` · `QueueRow` · `ReleaseRow` · `AdoptCount`. Panels P1–P8; lifecycle (Skeleton · Signal-Lost · offline writes-gated) deferred to converge. **RULED: A won (gate 2026-06-13) — converged into `discover-states.html` with the owner's edits (bottom/smaller toggle · standard header · real games · Browse-By cut · simplified pages); kept for history.** |
| `discover/discover-draft-b-feed.html` | **Discover — Draft B "One feed, lensed"** (standalone, built-in SVG; the §3.2 tab, DISCOVER nav-keycap active): the **continuity** pole — Up Next and Discover are **facets of one continuous scroll** on a left **flow-rail** (one river, node dots per band); the toggle is a **LENS, not a room** — `UP NEXT` narrows the feed to your queue, `DISCOVER` widens the *same* feed to the catalog, bands **blending** (queue → friend recs → upcoming → trending). New **`SegmentedKeycap/lens`** — ONE seated keycap (pressed, F-03) sliding in a track, bearing the **aperture glyph + pink PipLight + an accent under-fill**, with a lens cue (◐ queue / ◯ catalog). Under the wide lens the queue collapses to a one-line **peek**; **browse = a filter on the river** (P4); search docks conventional (P5, DISC-03); P6 `CardDetail` (primary verb ADD-TO-UP-NEXT drops into the feed); P7 cold-start = **one continuous empty→invite** (dry queue flows straight into the Contributor hook, CAT-02); P8 Trending = a **band** in the river (DISC-04, `RankChip`+`AdoptCount`, non-commerce). Panels P1–P8; lifecycle deferred to converge. **Retired at the gate (A "Two rooms" won, 2026-06-13); kept for history.** |
| `discover/discover-draft-c-arcade.html` | **Discover — Draft C "Arcade aisles"** (standalone, built-in SVG; the §3.2 tab, DISCOVER nav-keycap active; the C-line diegetic lineage from styler/canvas): the **metaphor** pole — Up Next = a physical **cartridge magazine** (cards as cartridges in numbered slots, ridged grip spines, the Now-Playing pin = the **LOADED** slot, drag-reorder = **slide to reslot** with a slot-gap); Discover = walking the **arcade floor** — an Upcoming **marquee** (lit bulbs + NOTIFY-ME) · genre **aisles** as numbered signs you step into · a Trending **cabinet**. New **`SegmentedKeycap/lever`** — a diegetic two-position hardware selector worn on-screen (recessed throw-track + a ridged knob seated in the active detent, pressed + pink PipLight), **PULL QUEUE ⇄ ARCADE** (F-04 untouched). P5 the **directory** terminal (DISC-03, bottom-docked `ResultRow`+`MatchTag`); P6 `CardDetail` under an inspection light (ADD-TO-SHELF / LOAD-INTO-QUEUE); P7 cold-start = empty magazine + a bare aisle (Contributor hook, CAT-02, `RankChip`/first); P8 the **cabinet** = the adoption **high-score board** (DISC-04, `RankChip`+`AdoptCount`, **non-commerce — scores not prices, no PIXELS**). Panels P1–P8; lifecycle deferred to converge. **Retired at the gate (A "Two rooms" won, 2026-06-13); kept for history.** |
| `discover/discover-states.html` | **Discover — CONVERGED board** (standalone, built-in SVG; gate ruling 2026-06-13: Two rooms + seven owner edits): **Draft A's two-rooms grammar** with the ruled changes baked in — **(1)** the **`SegmentedKeycap` moved to a smaller bottom-docked footer** (above the plastic NavBand; active segment pressed + pink PipLight); **(2)** a **standard `ScreenHead`** names the active page (UP NEXT / DISCOVER) — *resolves the labeling nuance (header names the sub-page, toggle switches, plastic keycap is the tab)*; **(3)** **real games** (Elden Ring · Hades · Celeste · Hollow Knight · Stardew Valley · Outer Wilds · Disco Elysium; upcoming GTA VI · Fable · The Witcher IV; FromSoftware search set); **(4)** **Browse-By removed** (DISC-02 parked → OQ-057); **(5)** Discover pared to three sections — **Upcoming** (`ReleaseRow` browse + NOTIFY-ME) · **Friend recommendations** (SOC-05 rows + ADD-TO-QUEUE) · **Trending** (DISC-04, `RankChip`+`AdoptCount`); **(6)** games-only **search kept** as a header magnifier (DISC-03, bottom-docked `ResultRow`+`MatchTag`, in-collection ✓, ⋮ report); **(7)** Up Next = the Now-Playing pin + ranked `QueueRow`s + a primary **ADD FROM COLLECTION** (one unowned wishlist/friend-rec retained for WTP-01). **Panels P1–P10**: Up Next populated · reorder · empty · Discover landing · `CardDetail` · search · Contributor hook (CAT-02, `RankChip`/first) · **+ lifecycle** Skeleton · "Signal Lost"+RETRY · Offline (browse-from-cache, **writes gated**, SYS-10). No PIXELS (non-commerce). **Discover-track converge complete — Design-spec formalization (`SegmentedKeycap`/`QueueRow`/`ReleaseRow`/`AdoptCount` → §1.5 + §2.x) + API page-audit (OQ-053/054/055/057) = spec-owner batch.** |
| `discover/discover-states-fan.html` | **Discover — card-fan variant** (standalone, built-in SVG; owner request 2026-06-13: *"make another like this one but with the Add Game card-fan aesthetic"*): the converged Two-rooms board (`discover-states.html`) **kept verbatim** — standard `ScreenHead`, the smaller bottom-docked `SegmentedKeycap`, real games, the three Discover sections, lifecycle — but every game is now shown as the Add Game **`CardFan`** (a swipeable **3-up** fan: forefront full-face + peeking neighbours, **zero plate occlusion**, the **meta above** + dots/SWIPE/position below). The fan replaces the rails/rows/grids across **Up Next** (a *playlist fan* — Now-Playing leads; reorder = lift the forefront + a position scale), **Upcoming**, **Friend recs**, **Trending**, and **Search** (literally the Add Game search pattern). `CardDetail` + the Contributor hook carry over unchanged. Panels P1–P10 incl. lifecycle (Skeleton fan · Signal-Lost · Offline writes-gated). Same data + IDs as the row board. **RULED: rows/rails (`discover-states.html`) won the aesthetic call (2026-06-13) — this card-fan variant is retired, kept for history.** |
| `game-page/game-page-draft-a-cardled.html` | **Game page — Draft A "Card-led scroll"** (standalone, built-in SVG; §4.2 the adaptive hub, COLLECTION/FRIENDS nav active per entry): the **InGame-native** pole — the **card object is the hero** (full-face, **tap-flips front↔back**, P3 = the standardized back: auto-stats + provenance, CARD-01), and the page is **one prioritized scroll** beneath it whose **adaptive band changes by entry-context**: P1 **neutral** (facts + tappable studio/genre chips + contributor credit + `PresenceStats` CAT-09 + ADD-TO-COLLECTION + the `CommunityGallery`) · P2 **owned** (the band = inline-edit **`PlayStats`** — hours/%/status/owned-since/rating/notes, per-field pencils + NOW-PLAYING + the `CardSwitcher` rail) · P6 **friend-view** (SOC-11: their card + `EquipReadout` CARD-22 + `FriendContext` privacy-gated + **atomic ADOPT** + the **opt-in `CompareStrip`** you-vs-them + the don't-own variant). P4 = the **card switcher / customizations view** (OQ-056 · COL-06: equipped + its readout, then mine·adopted·draft·private·published state-tagged + DESIGN NEW → Styler; SAVE-AS-NEW note); P5 = the **gallery adopt** (inspect readout → adopt the whole card, FREE/1 PX, no "adopt just the canvas", CARD-15/SOC-11); P7 = **upcoming → NOTIFY ME** (DISC-01/NOTIF-01) + **no-cards-yet be-first** (CAT-05). Panels P1–P7; lifecycle (soft-hidden · loading · offline) deferred. **Rev 2 (owner notes 2026-06-13):** button scale/weight tightened (gold = one primary per band; emoji→SVG) · **P5 re-cut** into the gallery grid + a rising `CardDetail` inspect sheet · **rating** de-emphasized + PENDING (OQ-058), pulled off the card-back (no card rating) · duplicate "design new" removed · **in-between UIs** added (⋮ overflow menu · switch→equip · adopt-confirm). **Rev 3 (owner round-2 notes 2026-06-13):** P2 `PlayStats` → a **pull-up drawer** (the page keeps only a slim collapsed peek — both the open editing sheet *and* the resting peek are documented; the card stays the hero) · the **card-back drops the "made in InGame" line** (provenance = designer + adoption + cosmetics only, CARD-01) · **buttons re-aligned to the Add Game catalog verbatim** (mini = 6px·8px / 9px keeping the base `shadow.key` 3px edge — the rev-2 lighter 2px was the miss). **Rev 4 (owner gate ruling — A WINS, 2026-06-13):** *"moving forward with A"* + 3 changes — (1) a dedicated **owned-landing panel** (new **P2** = what you see the instant you tap an owned game: card + collapsed `YOUR PLAY` peek + the switcher, drawer closed; the stats panel becomes **P2b**, drawer open) · (2) the **⋮ overflow is now a pull-up drawer** (`GAME OPTIONS` bottom-sheet, was a dropdown) · (3) **COMPARE now opens a drawer** (P6 `CompareStrip` rises over the dimmed friend page, was inline). Every heavy surface now shares the one bottom-sheet grammar. **Chosen draft → converge to `game-page-states.html` next (+ the deferred lifecycle states). B/C retired (kept for history).** |
| `game-page/game-page-draft-b-dossier.html` | **Game page — Draft B "Tabbed dossier"** (standalone, built-in SVG; §4.2 the adaptive hub): the **conventional-legible** pole — a seated **section switcher** (PLAY · CARDS · ABOUT; aligns with the forthcoming `SegmentedKeycap`, flagged not depended-on) chunks the dense page so each screen does one job. **PLAY** = `CardHero` + a clean **`PlayStats` dossier** (label/value rows, HOURS shown mid-edit, EDIT STATS flips the block to fields) + EDIT-THIS-CARD/SWITCH/SHARE; **CARDS** = the `CardSwitcher` (state-tagged equipped/published/private/draft/**adopted** + DESIGN NEW) scrolling into the `CommunityGallery` + the rising `CardDetail` adopt sheet; **ABOUT** = canonical facts + chips + credit + `PresenceStats` + friends-who-own + suggest-edit. The **state sets the default tab**: P1 neutral → ABOUT (PLAY locked) · P2 owned → PLAY · P3 flip (in place, inside PLAY) · P4 CARDS/switcher (OQ-056) · P5 CARDS/gallery+atomic-adopt · P6 friend-view → PLAY (their card + `FriendContext` privacy-gated + atomic ADOPT + opt-in `CompareStrip` + don't-own variant, SOC-11) · P7 upcoming→NOTIFY ME + be-first. Carries rev-3 (catalog button scale · rating PENDING OQ-058 · card-back no "made in InGame" · adopt-confirm in-between). Panels P1–P7; lifecycle deferred. **Retired at the gate — A won (2026-06-13); kept for history.** |
| `game-page/game-page-draft-c-pinned.html` | **Game page — Draft C "Pinned card + drawer depth"** (standalone, built-in SVG; §4.2 the adaptive hub): the **sheet-family / open-space** pole (the owner's *"more open space, fewer containers"* take) — the `CardHero` **pins compact at the top** with its primary action + an adaptive **stat strip** always present; the body is a **lean** set of hairline **drawer-launcher rows** (no boxes, lots of air); all heavy content opens as **bottom-sheet drawers**: **YOUR PLAY** (`PlayStats` inline-edit) · **STATS & PROVENANCE** (the card-back — **C reads the flip as a drawer**: the front never leaves the pin, CARD-01) · **YOUR CARDS** (`CardSwitcher`, OQ-056) · **COMMUNITY CARDS** (the `CommunityGallery` as a drawer — tap a face → it selects + an atomic adopt bar appears) · **COMPARE** (`CompareStrip`, opt-in). Adaptive pin: P1 neutral → ADD (the open landing = the thesis) · P2 owned → EDIT + stat strip + YOUR PLAY drawer · P3 flip-as-drawer · P4 switcher drawer · P5 gallery drawer + adopt · P6 friend-view (their pinned card + privacy-gated strip + COMPARE drawer + atomic ADOPT + don't-own variant, SOC-11) · P7 upcoming→NOTIFY ME + be-first. Carries rev-3 (catalog buttons · rating PENDING OQ-058 · card-back no "made in InGame" · adopt-confirm). Panels P1–P7; lifecycle deferred. **Retired at the gate — A won (2026-06-13); kept for history.** |
| `game-page/game-page-mix-takes.html` | **Game page — the A×B mix (large card + a bottom dossier switch) · 3 takes** (standalone, built-in SVG; owner gate notes on A, 2026-06-14): *"mix A and B — embrace B's dossier but keep the card as large as A; add a delete affordance; make the dossier fit the design system; bring the dossier tab to the bottom."* The dossier section-switch **is the `SegmentedKeycap`** (the converged Discover component, design-spec §1.5) — docked at the bottom, widened pair→**trio (PLAY · CARDS · ABOUT)**; the dossier body is the app's **standard list-row grammar** (nothing newly invented). Adds the missing **delete affordance** — a `MANAGE` mode reveals a **trash badge** per saved card (equipped = **locked**, switch first) → a **destructive ConfirmSheet** (red rail · danger DELETE · safe KEEP; reconcile w/ Settings `ConfirmDialog`; semantics → **OQ-061**). Three takes differ only in how the **large card relates to the bottom-tabbed dossier**: **Take 1** pinned hero + the dossier swaps inline beneath (card constant) · **Take 2** card-led, the card itself morphs per tab (front+stats / collection-head / flipped-back) · **Take 3** hero + the dossier rides up as a sheet over the dimmed card (rev-4 drawer grammar). Each take = PLAY · CARDS · ABOUT (the owned dossier; neutral/friend/upcoming carry from A rev 4, not re-drawn). **Pre-gate — owner picks a take → fold into A → converge.** |
| `game-page/game-page-trophy-takes.html` | **Game page — "Card as trophy" · 3 fresh takes** (standalone, built-in SVG; **re-brainstorm from first principles**, brainstorming skill, 2026-06-14): owner asked for the best mobile UI/UX for showing *all* the §4.2 info, unbound by A/B/C/mix. **Brainstorm ruling: card-as-trophy** — on the owned state the equipped card is the unquestioned star; everything else gets a calm, clearly-subordinate home (the cure for the "incoherence" = too many co-equal elements). **3 distinct interaction models** (owned state shown; same design system — `GameCard` · `SegmentedKeycap` · sheet grammar · `KeycapButton` · C5; delete flow incl. trash · equipped-locked · destructive `ConfirmSheet`, OQ-061): **A "Collapsing Trophy"** *(recommended)* — card opens large/alive then **collapses to a sticky header** on scroll (tap-flip · swipe-riffle-your-cards · long-press-peek); calm prioritized sections below (Your Play · Your Cards+delete · Community · Friends/About); the iOS large-title / Wallet pattern · **B "Card in Hand"** — near full-bleed gesture-operated card + one pull-up dossier sheet (bottom `SegmentedKeycap`) · **C "Trophy Dashboard"** — card at the heart of a glanceable module-tile board, each tile → a sheet. **Pre-gate — owner picks a take (or blend) → becomes the converge basis.** Brief + ruling: `plans/2026-06-13-game-page-drafts-brief.md` (Re-brainstorm §). **Owner picked A → built high-fidelity in `game-page-trophy-collapsing.html`.** |
| `game-page/game-page-trophy-collapsing.html` | **Game page — Take A "Collapsing Trophy" · HIGH-FIDELITY** (standalone, built-in SVG; owner picked A + asked for a real, convention-enforced draft, 2026-06-14): the equipped card is a **trophy** that opens large/alive then **collapses to a sticky header** on scroll; full §4.2 inventory + adaptive states. **Owner notes applied:** "your card" tag **removed** · full owned field set incl. **platforms** + **date acquired** (`ownedSince`) · **Your Cards larger + unobstructed** — state reads *below* the card, the **MAIN card carries the F-09 selection** (accent outline + pink `ChipPip`), **hold → options drawer** (SET AS MAIN · EDIT · DUPLICATE · SHARE · DELETE→`ConfirmSheet`, OQ-061) · **Community = a link when owned, a listed gallery when not**. **Conventions enforced:** `KeycapButton/primary` = **orange `scr.accent`** (gold+2/4-step reserved for card-creating ADD/DESIGN, F-02) · `shadow.key` 0 3px 0 · C5 card steps · F-01 (never crop/obstruct) · F-09 (flat planes, accent-border + pip selection) · F-06 type scale · `ListRow`/`Strip`/`StatTile`/`NowTag`/`EquipReadout`. **Panels:** A1 trophy · A2 collapsed-header + inventory · A3 flip · A4 card options drawer + delete confirm · A5 full PlayStats editor (all fields) · A6 neutral (community listed) · A7 friend-view (SOC-11 — adopt + opt-in compare). **The converge candidate** → `game-page-states.html` next. Trophy-takes A/B/C kept for history. **Iterated → `game-page-trophy-dualface.html`.** |
| `game-page/game-page-trophy-dualface.html` | **Game page — Take A iteration: dual-face hero + on-card stat edit** (standalone, built-in SVG; owner notes on the high-fi draft, 2026-06-14): the **hero shows both sides of the card at once** — the **face + the stats back side-by-side** (no flip needed), **game facts as a plain meta line** (studio · year · genre), with **EDIT STATS** under the stats side. **EDIT enlarges the stats card and the values become fields directly on the card back** (on-card editing: hours/%/status/platforms/acquired/rating; the face shrinks to a thumb; provenance stays auto; long NOTES edits just beneath). **Your Cards: no hold-drawer** — **tap to select** (orange outline + pip, F-09) and the options (**SET AS MAIN · EDIT · DELETE**) render **inline on the screen**; DELETE → the destructive `ConfirmSheet`. **Share Card Image removed from the per-card options** — the **only share spot is a docked footer at the bottom of the screen** (owned only; self-share, decision 0019). Conventions held (orange `KeycapButton`; gold = card-creating only; `shadow.key`; C5; F-01/F-06/F-09). **Panels:** A1 dual-face hero · A2 on-card stat edit · A3 scrolled (select + inline options + bottom share + delete confirm) · A4 friend dual-face (privacy-gated back). Neutral/don't-own carry from `trophy-collapsing`. **Refined (owner 2026-06-14):** screen title → **"GAME"** · **facts as a plain meta line under the game title** (boxed chips reverted) · the **stats card frame mirrored on the Y axis** (C5 step on the opposite corners — as if the card was turned over) · **EDIT is inline** (same GAME screen, no separate mode) showing the **stats card as the only card** while editing · **Your Cards selection = a thin orange border** (pip dropped). **Converge candidate (iterating on `trophy-collapsing`).** |
| `settings/settings-page.html` | **Settings — the conventional page + page states** (standalone, built-in SVG; §4.15 the well-trodden pattern, built **once** from the §1.5 catalog; settings track): a **`FlowTakeover`** from Profile's gear (`FlowHeader` ◂ SETTINGS; frame + `NavBand` persist, **PROFILE keycap active**; **non-commerce — no PIXELS / no `CountKeycap`**). Sections = `Well`s of `ListRow`s under `SectionHeader`s — **ACCOUNT** (EMAIL +verify tag · USERNAME @aiden + **PROF-06 cooldown** "NEXT CHANGE OK NOW" · PASSWORD · SIGN OUT `/secondary` AUTH-05 · DELETE ACCOUNT alert-red AUTH-07) · **PRIVACY** (PROF-03) · **SAFETY → BLOCKED USERS** (SOC-09) · **NOTIFICATIONS** (NOTIF-02 per-type) · **FEEDBACK & SUPPORT** (SYS-11 row + HELP/Contact SYS-09) · **ABOUT & LEGAL** (ToS+Privacy AUTH-10 + version; the DEV-04 "screen theme isn't here" footnote). **Two new components flagged for the gate: `Toggle`** (the square two-position keycap-knob switch — ON = `scr.accent` track+knob-right, OFF = `scr.grip`+knob-left; debuted on PRIVACY + the NOTIFICATIONS prefs) and **`ConfirmDialog`** (the centered destructive delete-account confirm — red seal + plain-words AUTH-07 ripple + red `/destructive` + cream CANCEL; flagged as a new spatial frame vs the one-drawer grammar). **9 devices / the full §1.6 state matrix:** A1 loaded (account/privacy/blocked) · A2 loaded (notifications/feedback/about) · B1 **unverified-email** (AUTH-08 `InlineBanner` + RESEND) · B2 **notifications OS-declined** (NOTIF-04 recovery banner + gated toggles) · B3 **blocked-users list + UNBLOCK** (SOC-09, the MOD-09 exception) · C1 **delete `ConfirmDialog`** (AUTH-07) · D1 `Skeleton` · D2 `Offline` writes-gated (SYS-10) · D3 `LoadError` "Signal Lost"+RETRY + the write-hiccup `Toast` (§1.8). **Superseded by `settings-states.html`** (the converged board folds in the C-triage feedback + moves notifications to its own page); kept for history. |
| `settings/feedback-draft-a-inline.html` | **Feedback (SYS-11) — Draft A "Inline expanding row"** (standalone, built-in SVG; settings track; one of 3 distinct feedback-surface models). **Distinctness axis = how you get from the Settings row to a filed report.** A's answer: **zero navigation** — the FEEDBACK & BUG REPORTING row **expands in place** inside the FEEDBACK & SUPPORT `Well`; you pick a type (`TypePick` — 3 inline segmented keycaps, active = pressed + pink pip), type a message, and SEND **without leaving Settings**. **5 states:** A1 idle (expanded · type FEEDBACK · dormant SEND) · **A2 bug + logs attached — THE PROOF** (the bug→`LogAttach` control fits the cramped inline footprint with the **consent line full-width, never hidden** + a compact reserved OQ-060 payload chip) · A3 submitting (SEND held pressed) · A4 the inline `FeedbackConfirm` (collapses in place, still in Settings) · A5 error → §1.8 `Toast`, form intact. Feedback SUBMIT = red `/destructive` per the brief (flagged judgment call — see the brief). **Retired at the gate — C "type-first triage" won (2026-06-14); kept for history.** |
| `settings/feedback-draft-b-sheet.html` | **Feedback (SYS-11) — Draft B "Dedicated sheet"** (standalone, built-in SVG; settings track; 1 of 3). B's answer: **you go somewhere** — the row opens a focused **`FlowTakeover`** (`FlowHeader` ◂ SEND FEEDBACK; frame + NavBand persist) that does one job with room to do it well. **6 panels:** B1 idle takeover (`SelectField` type picker + tall `TextField/area` + dormant SEND) · B1b the type **OptionSheet** (the SelectField → the app's one drawer grammar; F-09 selection) · **B2 bug + logs — THE PROOF** (the navigation earns the bug affordances room: the **roomy `LogAttach` `Well`** — toggle + a 2-line consent + the reserved OQ-060 payload panel with explicit size/preview room) · B3 submitting · B4 the full-screen `FeedbackConfirm` + DONE · B5 error → `Toast`. Feedback SUBMIT = red `/destructive` per the brief (flagged). **Retired at the gate — C "type-first triage" won (2026-06-14); kept for history.** |
| `settings/feedback-draft-c-triage.html` | **Feedback (SYS-11) — Draft C "Type-first triage"** (standalone, built-in SVG; settings track; 1 of 3). C's answer: **answer one question first** — a triage screen of three `TriageCard`s (FEEDBACK · SUGGESTION · BUG, the BUG card flagged "+ ATTACH LOGS" ahead of the tap) routes you to a **form tailored to that type**. **6 panels:** C1 the triage (idle) · **C2 minimal feedback form — the NO-TAX PROOF** (FEEDBACK/SUGGESTION route to one field + SEND, no logs/no consent/no diagnostics — the common path got *lighter*) · **C3 the bug form — THE HERO** (purpose-built: auto **non-PII diagnostic-context chips** — INGAME 2.1.0 · iOS 18.2 · iPhone 14 — *plus* the `LogAttach` foregrounded as the hero, its consent explicitly contrasting the logs vs the basics) · C4 submitting · C5 `FeedbackConfirm` + DONE · C6 error → `Toast`. **Proves triaging first makes the bug path better without taxing the common path.** Feedback SUBMIT = red `/destructive` per the brief (flagged). **WON the gate (2026-06-14) — converged into `settings-states.html` (+ the owner's notifications-as-its-own-page ruling); kept for history.** |
| `settings/settings-states.html` | **Settings — CONVERGED board** (standalone, built-in SVG; §4.15; gate ruling 2026-06-14 — **C "type-first triage" feedback + notifications as its own page**; settings track): the board of record. Settings settles into **a lean list of `ListRow`s where each heavier feature opens its own focused page** (`FlowTakeover` ◂ SETTINGS; frame + NavBand persist, PROFILE active; non-commerce — no PIXELS). **16 devices / 4 stages:** S1–S2 the **lean list** (ACCOUNT · PRIVACY & SAFETY [the PROF-03 `Toggle` + BLOCKED USERS →] · PREFERENCES → the **NOTIFICATIONS row "4 ON"** · FEEDBACK & SUPPORT · ABOUT/LEGAL; one scroll, scroll-rail-marked top/bottom) · S3 unverified-email (AUTH-08) · **S4 the NEW NOTIFICATIONS page** (NOTIF-02 — grouped `Toggle`s: Social / Your Games & Cards / Store) · S5 its **OS-permission-declined recovery** (NOTIF-04 banner + gated toggles) · S6 **BLOCKED USERS** list + unblock (SOC-09, the MOD-09 exception) · S7 delete **`ConfirmSheet`** (AUTH-07, bottom drawer) · **S8–S13 the C-triage feedback** (the `TriageCard` triage → S9 the minimal feedback form [no-tax] · S10 the bug form [auto non-PII **diagnostic-context chips** + the **`LogAttach` hero**, OQ-060 reserved room] · S11 submitting · S12 `FeedbackConfirm` · S13 error `Toast`) · S14 `Skeleton` · S15 `Offline` writes-gated (SYS-10) · S16 `LoadError` "Signal Lost"+RETRY (§1.8). **New components (design-spec formalization owed — spec-owner): `Toggle` · `ConfirmSheet` · `LogAttach` · `FeedbackConfirm` · `TriageCard`.** **Owner refinements 2026-06-14 (both former residuals resolved):** feedback SUBMIT = a **mid-width `KeycapButton/primary`** (centered, on-theme orange — not red) · the destructive confirm = a **bottom `ConfirmSheet`** (renamed from `ConfirmDialog`; aligns with the game-page track's destructive `ConfirmSheet`, OQ-061 — spec-owner to unify). **API ✅** (no gap — feedback `POST /feedback`+`/feedback/:id/logs` 0.22; the rest exist). **Settings-track converge complete.** Brief + ruling `plans/2026-06-13-settings-drafts-brief.md`. |
| `device/device-draft-a-inframe.html` | **Device editor — Draft A "In-frame live edit"** (standalone, built-in SVG; §4.5 `DEV-01..04`; device track; **1 of 3 distinct interaction models**). **Distinctness axis = how you edit the frame + place stickers.** A's answer: **the device frame IS the canvas, edited in place** — a `FlowTakeover` (◂ DEVICE, **PROFILE keycap active**, frame + NavBand persist) where the controls **dock on the screen** (`SectionChips`: SHELL · THEME · STICKERS) while **stickers drop directly onto the visible plastic**; the **OQ-045 on-shell preview is continuous** (you decorate the frame you're standing in, live). **6 panels:** A1 editing (the in-frame landing; SHELL = the 5 §1.1 colourways) · A2 **shell-switch** (CARBON re-wraps the live frame, stickers+theme ride along — one body, OQ-042/DEV-02/03; the `PreviewStage` before→after) · A3 **theme-preview** (DEEP SEA whole-page `PreviewStrip`, **legibility floor held + shell stays plastic**, DEV-04) · A4 **sticker place/scale/rotate** (the `StickerTray` → a placed glyph wearing the `TransformBox` — accent ring + 4 corner handles + rotate stem + SCALE/ROT readout, kin to Canvas `sel-ring`; **the DEV-03/F-04 proof** — a sticker dragged at the keys is **refused**, the nav band shows its protected dashed boundary) · A5 **on-shell preview** (OQ-045 closed — 4 stickers render on the real shell, handles off, nav fully legible; personal-only `stickerComposition`) · A6 **premium-reconcile** (`ReconcileSheet` for a NEON ZOO pack + DEEP SEA theme — per-item ✕ · TOTAL · short-by-N strip · `PackTile` bridge · HOLD TO ACQUIRE ALL, COSM-03 → `POST /cosmetics/acquire-batch`). New components flagged for the gate: **`StickerStage`** (the decoratable plastic + the nav no-go) · **`TransformBox`** · **`PlacedSticker`** · **`StickerTray`**. Tokens verbatim (5 shell colourways · 6 screen themes); PIXELS + `ReconcileSheet`/`PreviewStrip`/`PreviewStage` reused from store/styler. Lifecycle (Skeleton · Offline · Error) deferred to converge. **Pre-gate draft — brief `plans/2026-06-14-device-editor-drafts-brief.md`.** |
| `device/device-draft-b-workbench.html` | **Device editor — Draft B "Workbench breakout"** (standalone, built-in SVG; §4.5 `DEV-01..04`; device track; **2 of 3**). **Distinctness axis = how you edit the frame + place stickers.** B's answer: **the device pulls forward onto a workbench** (the breakout tier — kin to the Canvas print-shop `.shop`) — the editor's tools surround the device on a dark bench *instead of crowding its screen*, and the fiddly place/scale/rotate gets **real room via a zoom loupe**, then the device **snaps back into the frame** (the proof). **6 panels:** B1 **breakout entry** (the device pulls onto the bench; tools on the rail not the screen; SHELL = 5 colourways) · B2 **shell-switch** (CARBON on the bench; the before→after `MiniDevice` pair; one body OQ-042) · B3 **theme-preview** (the device's whole screen re-themes to DEEP SEA on the bench under `PreviewStrip`; **floor held + shell stays plastic**, DEV-04) · B4 **sticker place/scale/rotate** (the **zoom loupe** magnifies the work zone 240% so the `TransformBox` has generous room — the breakout's payoff; a `locator` shows position + **the LOCKED nav zone**, DEV-03/F-04) · B5 **snap-back on-shell preview** (OQ-045 — the device reassembles in-frame, the full composition on the real shell, **nav re-proven legible**; personal-only) · B6 **premium-reconcile** (`ReconcileSheet` over the dimmed bench; short-by-N `PackTile` bridge; COSM-03 → `acquire-batch`). New components flagged: the **workbench bench** (reuses Canvas `.shop` grammar) · the **zoom `loupe`** + `locator` · `TransformBox`/`PlacedSticker`/`StickerTray`. The breakout legitimately yields the in-frame chrome (decision 0014, tier 3); nav legibility is re-proven on snap-back. Tokens verbatim; PIXELS/`ReconcileSheet`/`PreviewStrip` reused. Lifecycle deferred to converge. **Pre-gate draft — brief `plans/2026-06-14-device-editor-drafts-brief.md`.** |
| `device/device-draft-c-dressing.html` | **Device editor — Draft C "Dressing room"** (standalone, built-in SVG; §4.5 `DEV-01..04`; device track; **3 of 3**). **Distinctness axis = how you edit the frame + place stickers.** C's answer: a **diegetic** take (the C-line lineage: styler-workbench · canvas-press · discover-arcade) — your device **stands on a turntable under a spotlight** and you **dress it**: spin the stand to reach a face, pull **wraps · moods · decals** from the wardrobe, watch the screen light up. The metaphor makes the hard rule physical: **the 5 nav keys are raised hardware — decals slide off them** (DEV-03/F-04). **6 panels:** C1 **dressing-room entry** (device on the turntable; the rotate control; the wardrobe = SHELL wraps) · C2 **try on a wrap** (CARBON; before→after `MiniDevice`; one body OQ-042) · C3 **mood** (the screen lights up DEEP SEA under `PreviewStrip`; **floor held + shell stays plastic**, DEV-04; the spotlight warms to match) · C4 **dress it** (a decal wears the `TransformBox`; **spin to reach a face**; the **keys-repel** proof in a legible **detail inset** — a decal slides off a raised key, DEV-03/F-04) · C5 **step back & admire** (OQ-045 — the fully-dressed device on its stand, every decal on the real shell, nav legible; personal-only) · C6 **premium-reconcile** (`ReconcileSheet` dims the room; short-by-N `PackTile` bridge; COSM-03 → `acquire-batch`). New components flagged: the **dressing-room/turntable stand** (the one genuinely new diegetic frame) + the **detail inset** (keys-repel proof) · `TransformBox`/`PlacedSticker`/`StickerTray` (worn as a wardrobe). Tokens verbatim (5 colourways · 6 themes); PIXELS/`ReconcileSheet`/`PreviewStrip` reused. Lifecycle deferred to converge. **Pre-gate draft — brief `plans/2026-06-14-device-editor-drafts-brief.md`.** |
| `device/device-switcher-takes.html` | **SectionSwitch — 3 takes for a new mode-switcher** (standalone, built-in SVG; device track; owner ask 2026-06-14, having picked Device Draft A). **Replaces the "Segmented Toolbar" convention** — Draft A's `SectionChips` (SHELL/THEME/STICKERS), kin to Discover's `SegmentedKeycap` (UP NEXT↔DISCOVER) + the Styler's `SectionChips` (5 carousel sections) — with a component modeled on **Settings S8's `TriageCard` options** (icon tile + title + one-line description). **Core tension:** S8 cards *pick-once-and-route*; a tool switcher is *always present* and must coexist with the active section's controls. **Selection moves to the F-09 language** (scr.accent border + pink `ChipPip`), off the old F-03 pressed-keycap tell. Shown in the Device-A context, 2 frames each (2 active sections): **T1 Section Stack** (the literal S8 adaptation — a vertical list of `SectionCard`s; the active one **expands in place**, accordion, its tray nested inside; most legible, weakest at 5-way) · **T2 Section Rail** (the compact upgrade — `SectionCard`s shrink to a horizontal **icon-card rail**, all visible, active = border+pip, the tray **docks beneath**; lowest-friction, no reflow; scales cleanly to 2-way/5-way — the convention-respecting choice) · **T3 Section Hero** (one **rich card at a time** — big icon+title+subtitle+tray inline, neighbours **peeking**, swipe/‹›-page with an elongated accent dot; kin to the Styler carousel; richest active state, but one section visible at a time). Each caption notes the **2-way (Discover) / 5-way (Styler) generalization**. Working names **`SectionSwitch`/`SectionCard`** (flagged; catalog only at converge). **Cross-app adoption on the converged Discover/Styler boards = OQ-063** (not applied here). **RULED: T2 "Section Rail" won (owner, 2026-06-14: *"work in Take 2… put the rail at the bottom, and the symbol in-line with the label"*) → refined in `device-switcher-rail.html`; T1/T3 kept for history.** |
| `device/device-switcher-rail.html` | **SectionSwitch — Take 2 refined (the chosen mode-switcher)** (standalone, built-in SVG; device track; owner ruling 2026-06-14). T2 "Section Rail" with the two owner changes applied: **(1)** the rail is now a **pinned footer at the bottom of the screen** (below the scroll, above the plastic `NavBand` — the same bottom-docked posture as Discover's switcher), the active section's content filling the screen **above** it; **(2)** each `SectionCard` wears its **icon in-line with the label** (one centered row). The descriptive **title + one-line subtitle** (the S8 DNA) lead the content area so the compact rail stays a clean switch; selection = **scr.accent border + pink `ChipPip`** (F-09). **3 frames** show the switch with no layout reflow — R1 STICKERS · R2 SHELL · R3 THEME (each the section's tray above, the rail pinned at the foot). Working names `SectionSwitch`/`SectionCard`. **Pre-converge — confirm, then fold into Device A's full state matrix (`device-states.html`); cross-app adoption = OQ-063.** |

View: open in a browser, or render headless:
`msedge --headless=new --screenshot=out.png --window-size=1140,1280 <file>`
