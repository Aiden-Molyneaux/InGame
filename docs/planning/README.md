# Planning

This folder will hold **`implementation-plan.md`** — the build sequence and tasks — authored by
Claude Code once the product-spec and design-spec are settled.

Two gates come before implementation planning:
1. The **design-spec** is in place (UI fully designed).
2. A **dev-tooling plan** exists for consistent testing on **Chrome (web)** and **iPhone (device)**
   from the single Expo codebase, including a seed/mock data layer.

Until then, this folder is intentionally empty.
