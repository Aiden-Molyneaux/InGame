# M1 Foundation Review — LEDGER (cumulative) · ✅ CONVERGED & CLOSED (6 rounds)

**Verdict: the foundation is fundamentally sound** — the boring stack, executable-zod monorepo, the
SYS-01/07 authz guard, RevenueCat, the deferred render spike, the front-loaded gate scheme, and the
staged-release plan all survived steelman as KEEP. **44 findings** (F01–F44); **6 ship-blockers** (F03,
F06, F21, F22, F29, F30 — all cheap, all about making guards *sound*, none re-litigating a core choice);
the rest split must-now / fast-follow / defer (see **Priority stamp**). The review did **not** over-correct
(it explicitly rejected Turborepo/Nx, blanket runtime validation, broader tagging, per-PR dep gates, a new
pre-scaffold gate, and HTML/SQLi theater). Loop closed at round 6 (marginal find-rate fell to doc-anchors).

Adversarial review of the build foundation **before engineering starts**, yardstick **ship fast AND
safely**. Reviewing: decision **0046** (10 arch decisions) · **CONVENTIONS.md** · decision **0045**
(gates) · product-spec **§9** (stack). Verdicts: KEEP · CUT · MODIFY · ADD · DEFER. `★` = agreed across
≥2 areas or ≥2 rounds. Steelman-vetted (no churn). **No plan docs edited during the loop.**

## ✅ Confirmed KEEP (survived steelman — lock at G-A, do NOT churn)
Stack + monorepo/`packages/shared` zod-as-executable-contract · SYS-01 scoped-helper + CONVENTIONS rules
2 & 4 + G-D core (the original-sin guard, **write-path** — read-path is F06) · render pipeline deferred to the M4 budget-capped spike (G-H) ·
redux-persist scoped to local drafts (but see F14/F20) · RevenueCat (free correctness for 2-store IAP) + Drizzle (SQL-transparent,
testable) · selective [LINT] tagging (rules 7/8 stay human-judged) · overall gate volume (front-loaded, tapering) ·
backup sequencing (G-F/G-C).
**Round 2 re-confirmed:** the front-loaded gate sequencing + the G-J/G-L/G-M ladder + the MOD-09 generic-unavailable collapse + the
asymmetric tier disclosure (`/me` exposes own adminTier, `/users/:id` exposes none) · SYS-12/AUTH-11 public reads (aggregate-cached /
disclosed-but-throttled, already 0043-adjudicated) · the input boundary (zod + Drizzle parameterization + RN's no-DOM XSS profile) —
**do NOT add an HTML sanitizer or a standalone SQLi rule** (velocity-draining theater).

## Findings
| ID | target | verdict | recommendation | speed↔safety | sev | rounds |
|---|---|---|---|---|---|---|
| F01 | rule 5 event-per-mutation [LINT] + Decision 6 "emit/**outbox**" + G-E | MODIFY | **Seam (MUST-NOW):** keep the cheap in-process `emitOnCommit` call-site + a **typed append-only `DomainEventType` registry** (unregistered type = compile error, not a no-op the presence-grep can fake); wrapper covers **both** ACH-08 **and** the MOD-10 audit row, transactionally (F16). **CUT** the outbox/delivery machinery (no consumer till M7). **DEFER** the FAIL-the-PR lint *teeth* + G-E "completeness" to a review-checklist now → lint at M7 (**pull to M3 if discipline slips** — R5's closest call). Reframe G-E → the provable "**single emit seam is the only mutation path, un-bypassable**". **Ripple the DoD "event emitted" line in the same edit.** | keeps the un-retrofittable seam at near-zero cost; defers friction no consumer uses till M7; the registry is the only mechanically-honest form | major | 1→5 |
| F02 | G-B "floor is real" verifies load-bearing lints on an **empty M1 repo** (no repos/mutations/authz routes yet) | MODIFY | Split lint-proof: at **G-B** demo only the lints with substrate (raw-DB / un-zod'd / untagged); fold the **authz/scoping/emission** lint planted-PR proof into the **M2 G-D** batch on the first real endpoint; re-fire the RED-on-purpose demo against a **real** SYS-07 test there (the M1 one is a toy). One-word CONVENTIONS header fix. | proof the net has teeth on real code, not an empty repo | major | 1 |
| F03 ★ | destructive DB runners "by config" → disposable (Decision 9 / G-C) — config-only is defeated by `export DATABASE_URL=…` (the most plausible agent action), and G-C is verified-once | MODIFY | **SHIP-BLOCKER (R4-elevated).** Every destructive runner (migrate-down, seed-reset, truncate, drizzle push/reset) **fails CLOSED at runtime** — refuses unless the target DSN carries a disposable marker (DB name `^(local|staging|test)_` **OR** `INGAME_ALLOW_DESTRUCTIVE=1`) that **prod creds structurally lack**; a test asserts prod config carries no such flag. **G-C verifies the guard FIRES** (point a runner at a prod-shaped URL, watch it abort), not just that 3 DB instances exist. ~10 lines, before the first migration. | the one unrecoverable un-gated mistake; the env-var path config can't see is the clincher; a scratch-DB false-positive is a 5-sec rename | **ship-blocker** | 1→4 ★ |
| F04 ★ | client-bundle secret exposure — gitleaks scans the git tree, not the built JS bundle; `EXPO_PUBLIC_*` ships to every device by design | MODIFY | **3-bucket key taxonomy** in CONVENTIONS: (1) **server-only** (DB, RevenueCat SECRET, JWT-signing, the 0043 deep-link HMAC, SMTP) — asserted **absent from the built bundle**; (2) **intentional public** (RevenueCat PUBLIC SDK key, Sentry DSN, API base) — **allow-listed by name** (kills alert-fatigue); (3) lint = a non-allow-listed secret-pattern/high-entropy string in the eas/expo-export bundle fails, AND an `EXPO_PUBLIC_*_SECRET/_KEY/PASSWORD/TOKEN` fails. Verify on a **real built bundle**; extend G-B's planted-secret demo to also plant a mis-prefixed `EXPO_PUBLIC_` secret. | 3 lines + allow-list; makes the existing "no secret in bundle" intent actually enforceable | major | 1→2 ★ |
| F05 | Windows-11 host vs Testcontainers parity + G-B's "loop on iPhone" demo | ADD | M1 pre-flight: verify Docker/WSL2 Testcontainers parity before declaring local↔CI parity; make G-B's iPhone clause **explicitly depend on M1-P** (EAS cloud build needs Apple enrollment/signing — no local iOS on Windows). Note EAS free-tier build-quota as a soft constraint (no local fallback on Windows). | half-day pre-flight avoids burning the M1-exit gate discovering the demo can't be produced | major | 1 |
| F06 ★ | SYS-01 **read-path** privacy-leak — the ENTIRE mechanical authz net (rule 4 / SYS-07 / G-D / DoD) is mutation-only; the two-shape privacy-gated read FAMILY is uncovered by both lint AND the standing-test law | MODIFY | **#1 SHIP-BLOCKER.** Widen SYS-07 / rule 4 / DoD from "every mutating endpoint" → "every endpoint that returns **another principal's** data carries a standing **relationship-matrix** test"; drive off an **explicit read-route inventory** (+ a public-read allow-list: SYS-12, AUTH-11, /stats/public); assert the **field-set** (not just status) across non-friend / friend / blocked-suspended-deleted (MOD-09 collapse) **+ an anonymized-author shape** (an AUTH-07-deleted contributor's still-public card/catalog rows must not leak the original username/userId via `designer`/`createdBy`); make the privacy shape a **typed serializer** (`toPublicShape`/`toFriendShape`) so a leaked field fails a snapshot; fold a READ-break into G-D. Endpoints: `/users/:id` · `/users/:id/contributions` · `/catalog/games/:id` friendsWhoOwn · `/users/:id/collection` · `/me/compare/:friendId`. | near-zero velocity, **highest safety payoff** — the prototype's literal original sin on the side the net structurally can't see | **ship-blocker** | 1→2 ★ |
| F07 | AUTH-07 deletion-ripple gated **real at M8**, dry-run M5 (G-N) | MODIFY | Pull the **real** gate to **M7-exit** (first point the full graph — adopted cards, contributor credit, friendships, PII — exists); M8 only **re-verifies**. | gets a high-entanglement, PII-bearing, irreversible bug off the launch critical path, free | major | 1 |
| F08 ★ | zod-status doc drift (road-map §3 L101 "→400" vs **422** everywhere else) **+ error-body serialization gaps + no truth-precedence note** | MODIFY | Fix road-map to **422** before G-A (CONVENTIONS already correct). **Add to 0046 #5:** (a) `SERVER_ERROR` 500 → **generic message only**, internals to Sentry, never in the `{error}` body; (b) auth failures → neutral `AUTH_FAILED` (don't undo AUTH-11). **+ Truth-precedence line** (CONVENTIONS/00-INDEX win over the road-map *planning* doc — stops every future drift of this class re-triggering an owner question). **+ error codes append-only** after the first shipped build (M2); F09's enum snapshot enforces additive-only diffs + asserts the 500 body excludes internals. | trivial doc edits; the precedence note compounds over a months-long build where the planning doc inevitably lags | minor | 1→3 ★ |
| F09 | error model (AppError → middleware → api-contract code enum): the lone cross-cutting seam with **no [LINT]/snapshot/gate** | ADD | A CI **snapshot/contract test** (not an owner gate) asserting AppError subclasses map onto exactly the api-contract error-code enum + the `{error:{code,message}}` shape. Wire in M2. | a few hours; makes the error seam consistent with every other guarded seam | minor | 1 |
| F10 | CONVENTIONS **rule 3 over-claim** (field-for-field fidelity stapled to the body-presence lint) + header "verified once at G-B" | MODIFY | Wording only: split presence-lint from fidelity (owned by the shared-schema snapshot test); reword header — [LINT]s run **every PR for the life of the app**; G-B is the one-time **teeth demo**, not a retirement. | keeps the load-bearing lints from being mentally retired after M1; zero mechanism change | minor | 1 |
| F11 | package manager left **"npm OR pnpm"** in a doc meant to be locked; also under-specifies the M1 lockfile gate + G-M lockfile-diff | MODIFY | **Pin npm workspaces at G-A.** Contest resolved: R1 favored pnpm (disk/speed — irrelevant at 12hr/wk), but **R3 + R4 both favor npm for this stack** — pnpm's strict symlink store is a known Metro/RN module-resolution footgun (needs `node-linker=hoisted` shims) an agent burns hours on mid-scaffold; npm has first-class Expo/Metro support + one lockfile gitleaks/SCA already grok. If the owner insists on pnpm, add a **G-B Metro+EAS-resolution sub-check**. | 30-sec decision vs a silent pnpm default + the RN hoisting wall + a baked-in lockfile/CI/EAS migration | minor | 1→4 (→npm) |
| F12 | citation/anchor hygiene: findings + docs loosely cite 0040/0041 for the signing key; multi-anchor (SYS-02 / 0043 / 0046) for the 422 rule | MODIFY | In the owner-facing bundle, cite **0043** for the deep-link signing key + the 422 ruling; add a one-line "SYS-02 owns the validation policy; 0043 set 422" so the seam has one traceable owner. | trivial; stops agents chasing wrong decision numbers | minor | 1 |
| F13 ★ | IAP webhook + `/iap/validate` + signed deep-link/QR — trust boundaries the mutation-authz net can't see | ADD | (a) **Name the non-actor / unauthenticated endpoint class** in CONVENTIONS (`/iap/webhook`, `/stats/public`, `/auth/username-available`, `/auth/password-reset/*`, neutral `/auth/login`) so rule-4's diff doesn't misfire or get weakened to swallow an actorless route; (b) webhook **verifies provider signature before any wallet/ledger write**, **idempotent per provider event-id** (replayed grant/refund never double-applies), server-resolves entitlements — carries **forge-rejected + replay-rejected** standing tests; (c) **`/iap/validate` IS an actor endpoint** — the RevenueCat app-user-id MUST be the server-derived authenticated userId, never client-asserted (IDOR), carries the cross-user authz test; (d) the deep-link/QR HMAC **verifier carries a forged-signature-rejected standing test** (forged invite = authz-bypass). Handler code is M5; the **class clause + test obligations land at G-A/G-E**. | cheap class-naming now prevents the net swallowing actorless routes + closes real-money/auth-bypass holes | major | 2 |
| F14 | on-device auth-token storage unspecified (0046 names redux-persist = plaintext AsyncStorage) | ADD | Auth tokens persist via **expo-secure-store** (iOS Keychain / Android Keystore), **never** redux-persist/AsyncStorage; the persist allow-list **explicitly excludes the auth slice** (it's for CARD-14 drafts + SYS-10 cache). A G-B-adjacent test asserts the auth/token slice is absent from the persist allow-list. | one sentence + one assertion; stops a long-lived refresh token sitting in plaintext through the M2/M4 device builds | major | 2 |
| F15 | refresh-token revocation model undecided; MOD-09 "suspend invalidates sessions" needs a store a stateless JWT can't honor; testing-strategy "reuse detection" binds to nothing | ADD | Decide at **G-A**: refresh tokens **server-stored + rotating**, with **reuse-detection** (a presented-but-already-rotated token revokes the whole family — the theft signal) + access-TTL short enough that MOD-09 suspend / AUTH-05 logout is honored (or a userId+suspendedAt deny-list checked on refresh). One clause on AUTH-02/05 so G-G's "rotation rejects old token" + testing-strategy's "reuse detection" bind to real behavior, and a stateless-JWT default isn't chosen by accident. | one clause now vs a revocation store retrofitted after sessions exist + a green-but-hollow "reuse detection" | major | 2 |
| F16 ★ | no CONVENTIONS rule for SYS-08 privilege-check or MOD-10 audit-row EMISSION (spec mandates the audit row "from the start," but nothing STANDING watches it) | ADD | CONVENTIONS rule: every privileged (admin-tier) action checks the SYS-08 permission **group server-side** + writes one **append-only MOD-10 `admin_audit_log` row TRANSACTIONALLY with the mutation** (audit-before-commit; one fails → both fail) so a money-moving ECON-11 op can't exist unaudited. Standing test: a lower-tier actor is refused 403 **and** the audit row is written. Extend rule-5's wrapper-presence lint to also assert the audit row on privileged routes; fold atomicity into G-E. Extend **F03 fail-closed** to the out-of-band ECON-11 / role-grant ops. | mechanical + atomicity layer under an already-correct spec intent; today a privileged write that emits analytics but no audit row passes CI | major | 2 |
| F17 | no SYS-05 rate-limit **presence** convention — G-K signs the VALUES, but nothing requires a listed endpoint to CARRY a limiter when built | ADD | DoD line + CONVENTIONS clause: an endpoint in the SYS-05 set carries a limiter at build (values stay SYS-04/G-K-tunable). A **SYS-05 route allow-list** (mirrors rule-4) + a **standing 429-under-burst test** (wire G-G's demo as a standing test), especially **AUTH-11** (per 0043 the deliberate sole account-existence oracle — unbounded = unbounded enumeration). Do **not** over-build a generic "every route limited" lint (false-positive hell). | a couple standing tests vs abuse/enumeration levers shipping unthrottled until a late gate notices | major | 2 |
| F18 | domain-event payload = latent PII egress (feeds PostHog from M2; AUTH-07 deletion could emit the PII it just removed) — Sentry is a second un-inventoried sink | MODIFY | Add a payload-minimization rule to 0046 #6 / rule 5: events carry **IDs + changed field-set + actor, not whole rows**; PII opt-in per event type; the analytics consumer reads a **projection**. Add **Sentry data-scrubbing** (beforeSend redaction, token/PII denylist) to the G-O label reconciliation + the PII-egress allow-list (a 500 with a token in context must not ship it to Sentry). | one payload-shape rule + Sentry scrub now vs months of PII silently fed to 3rd-party sinks by G-O (M8) | major | 2 |
| F19 | SYS-02 sanitization is input-strong but doesn't normalize Unicode at the render/screening boundary | KEEP+1 | KEEP the input posture (zod + control-char strip + MOD-07 + Drizzle params + RN no-DOM — do NOT add an HTML sanitizer or SQLi rule, that's velocity-draining theater). **Add one line** to 0043's SYS-02 policy: **NFC-normalize + strip bidi-control / zero-width / excess-combining** on fields feeding the skia render + the MOD-07 screen (so screening sees the same string skia draws); store-raw / escape-on-render for fields later surfaced in the operator tool / email. | one line closes a screening-bypass / render-grief gap; more validation = velocity for no gain | minor | 2 |
| F20 | redux-persist rehydrates the last user's cached data across an account-switch / re-login on a shared device — a client-side cross-user read the server net can't see | ADD | CONVENTIONS clause: persisted state is **purged on logout / account-switch**, the persist key is **namespaced per userId**, **+ a persist version-key + purge-on-unknown-version** (a changed cached response-shape must not rehydrate stale into new selector code — forward-compat for installed beta clients). Pair with F14's auth-slice-excluded assertion. | small client convention vs user B seeing user A's data, or a beta client crashing on a stale cached shape | major | 2→3 |
| F21 ★ | card composition JSON has **NO `schemaVersion`** (device `sticker_composition` IS versioned — spec L306 vs L307); 0046 #10 names only the runtime path | MODIFY | **SHIP-BLOCKER (timing: before the first persisted draft, M2/M3 — you cannot retro-stamp v1).** Add a top-level **`schemaVersion`** discriminator (int, =1 from first save) into the stored composition + the shared zod schema (rule-3 enforces presence); flatten **dispatches on it + rejects unknown** versions; **`composition_hash` computed over a version-aware canonical form** (else CARD-19 dedup silently collides/misses); asset/effect IDs **append-only**. Defer ALL migration machinery to the M4 spike; file an OQ for format-evolution. | one int + one CONVENTIONS line now vs an un-versionable corpus of immutable, fanned-out, PNG-un-re-derivable user blobs | **ship-blocker** | 3 |
| F22 ★ | the 5 [LINT] spine rules + G-C are **verified ONCE** (G-B); lint/glob/config rot silently over a months-long build (the spine every later gate trusts) | MODIFY | **SHIP-BLOCKER (rot-class).** Make enforcement **self-guarding**: a CI **meta-test** with a known-bad fixture per [LINT] rule (raw-DB / unscoped-repo / un-zod'd / missing-authz-test / non-emitting) that MUST fail every PR — freezes the G-B demo as a standing CI check (no owner load); **forbid blanket `eslint-disable`** of the spine rules; visible **`// SYS-01-EXEMPT` / `// @no-emit <reason>`** hatches (counted, surfaced at milestone exit); targeted owner re-glance only at **M6/M7** (new route families). | half-day one-time meta-test vs a silently-defanged spine discovered at M7 = months of un-guarded endpoints | **ship-blocker** | 3 |
| F23 | CONVENTIONS rule 3 "shared zod IS the contract, field-for-field" couples **wire shape + validation shape** forever — fights the F06 privacy serializer at the first withheld field | MODIFY | Split `packages/shared` into **REQUEST/input** schemas (rule-3 field-for-field applies — stricter where FE/BE must agree) and **RESPONSE/view** schemas (owned by the F06 typed privacy serializer — the **sanctioned divergence** point). CI contract-snapshot asserts the **request** contract + flags a documented endpoint with no shared schema. Allow client transforms (camel/snake). | keeps input-contract strict while unblocking the privacy serializer; the literal rule forces a schema-fork or a weakened read-guard at first divergence | major | 3 |
| F24 | 0046 #6 / rule 5 / road-map still say "emit/**outbox**" (R1 cut the outbox); emitted events carry **no frozen envelope / `eventVersion`** (M7 replays them, analytics consumes them — append-only, un-migratable) | MODIFY | Reconcile wording → "**emit in-process; the emit call-site is locked; durable outbox/transport DEFERRED (YAGNI)**." ADD a frozen **envelope** `{eventName, eventVersion, occurredAt, actorId, entityRef}` + typed body, **`eventVersion` stamped day-one**, bodies evolve additively. Tighten "mutation path" → "a write through the repository layer." Greppable **`// @no-emit <reason>`** allowlist. | wording fix (free) stops an agent building a polling-worker subsystem; the 5-field envelope = the same append-only hedge as F21 | major | 1→3 |
| F25 | migration discipline (0046 #7) catches DROP COLUMN but **not re-keying** (`UPDATE … SET code=…` reusing a retired ID); blanket expand-contract is ceremony on zero-row disposable DBs during M2–M4 churn | MODIFY | (a) Extend the **destructive change-class** to include **renaming/re-keying any value an existing row/event/composition references by ID** — append-only on reference/enum keys (error-enum, achievement/tier ladders 0038, asset/effect IDs, ECON product mappings); (b) **gate expand-contract on the G-C/irreplaceable-data line** (REQUIRED once an env holds non-regenerable data; before that, direct migration on disposable DBs is fine). | re-keying = decide-now (silently orphans referencing rows, irreversible); the relaxation reuses the crisp G-C trigger | major | 3 |
| F26 | the authz lint catches a **MISSING** test, not a **PRESENT-but-toothless** one (asserts 200 where 403 is required); G-D re-fire stops at M5 while M6/M7 add the largest cross-user surface | MODIFY | Prefer an automated **CI meta-assertion that authz tests assert a 4xx denial** (not merely exist) — covers all surfaces, **no owner load, preserves the taper**; demote the M6/M7 owner break-it glance to a cheap backstop. | closes the toothless-test gap on the friend/moderation surface without reopening per-PR owner load | major | 3 |
| F27 | gate scheme restated in **3 places** (0045, road-map §11, per-decision refs) with no single editor-of-record + no drift tripwire; 0046 is DRAFT with no rule that amending #1–#10 re-fires G-A | ADD | (1) Name **0045 the single editor-of-record**; reduce road-map §11 + CONVENTIONS refs to pointers; add a **status column** (pending/fired-pass/fired-fail/N-A). (2) Add the gate docs to the **/health doc-graph**. (3) 0046 governance line: **amending #1–#10 re-fires G-A** (owner re-sign); no routine agent edits to a lock-in. | applies 00-INDEX §4's one-editor law (already violated by the 3-way restatement) to the gates; pure governance, no code | minor | 3 |
| F28 | Drizzle migration **journal** (`meta/_journal.json`) is order/hash-sensitive; a rebased/squashed PR or hand-edit desyncs it from DB state — 0046 #7 reviews the SQL, not the journal | ADD | A CI check that the committed journal matches `drizzle-kit generate` output (no drift, no hand-edits) — same self-guarding pattern as F22/F03. Wire when migrations land (M2). | cheap CI hedge against a desynced migration ledger | minor | 3 |
| F29 ★ | CONVENTIONS states ~8 rules in prose but ships **no canonical worked example**; 5 of 7 rules + 3 of 10 decisions have **no substrate on a hello-world M1 repo** (so G-B is hollow as written) | MODIFY | **SHIP-BLOCKER — the load-bearing fix the whole round converged on.** Make the sign-in→profile slice a **living, CI-executed (every PR) golden-path exemplar** and an **M1 DELIVERABLE** (not M2): route-registration helper (F30) + scoped read + privacy serializer (F06) + req/resp zod (F23) + `z.infer`'d RTK endpoint (F31) + `emitOnCommit` covering ACH-08 **and** MOD-10 + a 4xx-asserting SYS-07 test + spec-ID tag + the annotation vocab — the reference every endpoint clones; G-B/G-D demo against it; it doubles as the F22 fixture. **Scope guard: exactly one entity, no second feature** (else it's its own over-build). | ~one M1 day converts 8 ambiguous prose rules into a clonable pattern + gives G-B real substrate; without it agents infer N divergent dialects, CI rubber-stamps them | **ship-blocker** | 4 |
| F30 ★ | rule-4 [LINT] route-inventory substrate is **undefined** (≥4 incompatible derivations) AND the test→route binding is **gameable** (a name-match is satisfied by `expect(200)`) | MODIFY | **SHIP-BLOCKER.** Pin one typed **`defineRoute({ method, path, mutates, authzTest })`** helper so route-inventory is a **data structure**, not an AST/verb guess; **`mutates` is an explicit flag** (a state-changing GET still trips); coverage counts **only when the harness hits the real route with actor-B creds and asserts 4xx** — the mechanical form of F26 (test-BINDING, not test-EXISTS). The F29 slice instantiates it. State it's **architecture, not "tooling minutiae".** | the one mechanism whose failure re-ships the cross-user vuln; under-specified, an agent picks one of 4 ways + fakes green with a stub | **ship-blocker** | 4 (subsumes F26) |
| F31 | RTK Query client endpoints are **not bound** to the shared zod schemas — the "executable contract" covers only the SERVER half; FE/BE can agree with each other while both drift from the contract | ADD | Every RTK endpoint types req/resp via **`z.infer` from `packages/shared`** (never a local interface) — **lintable** (`no-restricted-imports`); runtime `transformResponse` validation **only on privacy-gated cross-principal reads** (the F06 shape), **not blanket** (hot-path cost). | cheap import-binding closes the FE/BE drift class the monorepo was bought to close; scoped runtime check avoids over-guarding | major | 4 |
| F32 | rule 2 lints that the scoped-helper was **called**, but nothing defines **user-owned vs legitimately-global** tables — an agent mistags a catalog/stats read to dodge the lint | MODIFY | Name the boundary in 0046: every **user-owned table carries a non-null owner/actor FK** the helper requires; **global tables are an enumerated exempt list** (catalog, store items, SYS-12 stats). Position rule-2's lint as a **smell-catcher**; the real guarantee is the SYS-07 4xx tests (F30/G-D). The **`// SYS-01-EXEMPT` annotation is cross-checked against the exempt list** (EXEMPT on a non-listed table fails CI — else it's a sanctioned escape hatch). | cheap list + FK convention; trusting a presence-grep on borderline tables re-opens the cross-user hole | major | 4 |
| F33 | rule 7 ("file it and stop") collides with CLAUDE.md unattended "proceed + record the assumption" — an agent shipping a slice **won't stop**; it ships a plausible guess behind green CI | MODIFY | Sanctioned **third path**: a greppable **`// ASSUMPTION(OQ-xxx): <guess>`** tag + an open-questions entry, surfaced at the G-M glance. **Bright line:** risk-domain behavior (auth, SYS-01, economy ECON-*) is **STOP-and-file, never assumed**; everything else may be assumed-with-a-tag. | turns invisible silent guesses into a greppable ledger the owner sees; the bright line stops it laundering auth/economy guesses | major | 4 |
| F34 | G-B / G-D demos are **agent-improvised** — "a test goes RED on purpose" is satisfiable by `expect(true).toBe(false)` (proves the runner fails, not that the real authz suite has teeth) | MODIFY | Make the demos **checked-in scripted fixtures** (`scripts/gate-gb/*`, `scripts/gate-gd/*`): RED is produced by **reverting ONE named real guard** (remove the scoped-helper actorId on a repo method) showing a **pre-existing SYS-07 4xx test red in CI logs**; planted-bad cases open as real draft PRs vs protected `main`. **Edit the GATE TEXT in 0045** (not just scripts) so the loophole isn't in the contract the owner signs. Coupled to F29 (the real guard to break lives in the slice). | the highest-leverage anti-hollow-green spend; else the owner sitting is the agent narrating a run it authored | major | 4 |
| F35 | rule 6 (spec-ID tag) only checks a `/[A-Z]+-\d+/` pattern — a typo'd/wrong-but-well-formed ID passes, so grep-traceability silently lies; rule 8 (no-new-dep) relies on agent diligence | MODIFY | Rule 6: validate tags against a **generated id-registry** from the spec (unknown ID fails CI) + **enumerate the risk-domain dirs** by path. Rule 8: a **lockfile-diff presence surfacer** (deps changed + no `DEPENDENCY:` justification block → PR fails); honesty stays the G-M glance. | cheap hardening of already-accepted lints; closes the gameable/forgettable gaps | minor | 4 |
| F36 ★ | the Testcontainers job is named the real-PG safety net for **authz-under-concurrency / economy race** (review's documented blind spot) but **nothing forces the tests to be concurrent** — an agent writes sequential happy-path tests against a real container | ADD | A CONVENTIONS line for the risk domains: economy/authz integration tests carry an **explicit concurrent-execution assertion** (fire N parallel requests; assert exactly one wins / all-but-one 4xx). + assert the **PG container actually started** (fails on zero containers, so mocked repos can't fake green). | "real Postgres, not mocks" is otherwise satisfied by tests that can't see the bug it was bought to expose — the teeth under G-I/G-D | major | 4 |
| F37 ★ | with ~36 findings + a solo owner reviewing at gates, nothing draws the line between "must exist in the scaffold commit" vs "lands during M2" → M1 silently absorbs M2/M5/M7 work + slips the abandonment-critical first-phone moment | ADD | Write a **"Minimum M1 = the spine, nothing else"** note into the M1 scaffold task (+ 0046 or a short decision record, filed as 0051). The **7-item spine** to the M2 on-device sign-in→styled-profile win: (1) monorepo + shared zod; (2) SYS-01 scoped helper + route↔authz-test inventory-diff + lint; (3) AppError→422 middleware; (4) emit() seam + typed event registry ONLY (no outbox/teeth); (5) fail-closed migration runner + key taxonomy + bundle-grep; (6) CI spine green + F22 self-test + F29 slice + F30 route-helper; (7) Expo Chrome+iPhone loop. **Stamp every other finding must-now / fast-follow(M2) / defer** (see Priority stamp below). | one paragraph converts a 36-item pile into a 7-item gate without dropping the one law the prototype violated; the cheapest accretion-guard | ship-blocker | 5 |
| F38 | G-F restore-drill sits in the morale-critical M2-exit batch, but §3 says "before REAL users" — and there are none until the ~M4 beta (M2 = owner's own phone, no real data) | MODIFY | **Re-time the G-F restore-drill EXECUTION to M3-exit / M1-P-complete** (whichever is later), NOT blocking the M2 on-device win; keep the migration roll-forward/back demo at M2 (migrations DO ship in M2). Leave G-F **named, with a hard deadline before the M4 beta** (don't delete — a "later" safety task that never gets done). | lightens the heaviest M2 sitting + lands the first win sooner, zero safety loss; residual forgetting-risk mitigated by the named deadline | major | 5 |
| F39 | product-spec §9 names **no transactional-email transport**, but AUTH-04 (single-use reset token) + AUTH-08 (resend verification) are M2 auth-seam behavior | ADD | One line to §9 (or an OQ) naming a transactional-email transport as the M2 auth dependency (free-tier provider; vendor = a G-M glance, not a G-A lock); tag the SMTP/API secret **server-only** in F04's taxonomy. **Fast-follow / decide-at-M2-entry**, not must-before-scaffold. | one line avoids an M2 stop-and-file stall on the P0 auth critical path | minor | 5 |
| F40 | `testing-strategy.md` (v0.1) — the **unaudited 4th foundation doc**, stale vs CONVENTIONS/0046: §7 pipeline omits gitleaks+SCA+build; §2 "single zod contract" collides with F23; §3 authz "mutating only" collides with F06 | MODIFY | Fix the **3 verified stale lines** before G-B (align §7 to the 6-check spine; widen §3 to F06's "any endpoint returning another principal's data"; reconcile §2 with the F23 split) + a pointer that CONVENTIONS+0046 own the CI-gate truth + the F36 concurrency line. Do NOT rewrite the doc wholesale. | the doc that *claims to own testing* describes a 3-check pipeline → the lint spine (what G-B must demo) is the thing most likely under-built | major (must-now) | 6 |
| F41 | no **runtime version pin** — Expo SDK / RN / Reanimated / react-native-skia / Node are a tightly-coupled matrix; an agent doing bare `npm i react-native-skia@latest` (the CARD-15 piece) hits the RN-incompat wall | ADD | CONVENTIONS one-liner: **native deps via `expo install`, never bare `npm i`** (version-agnostic); commit `.nvmrc` + `engines` **at scaffold** from whatever `create-expo-app` pins (don't hand-type versions into 0046); F05 pre-flight verifies the matrix builds the loop. | a version-agnostic rule + scaffold-time pins vs a day-plus burned on an RN/skia incompatibility wall | major (must-now) | 6 |
| F42 | 0046 #8 says "automated daily backups" but the **provider isn't named** and "backups ON" is **assumed not verified** (road-map: "free tiers exist; backups want paid") — the G-F restore drill can pass on a manual one-off snapshot while the automated schedule is silently off | MODIFY | G-C checklist: **name the managed-PG provider + verify the automated schedule is ON** (distinct from G-F's restore drill) + a one-line RPO ("≤24h pre-scale; PITR if the tier offers it"). | one provisioning hour; confirm-don't-assume on the one unrecoverable failure mode | major | 6 |
| F43 ★theme | (a) the **MOD-10 audit-row is a 2nd mandatory per-mutation side-effect no lint covers** (§8 "every privileged write emits an audit row, no retrofit"; rule-5 lints only the ACH-08 event); (b) **nothing defines what makes a service method a "mutation"** for the lints — rules 4/5/audit each guess, so a method like `claimDailyBonus`/`adoptCard` (no create/update/delete prefix) escapes **all three** | ADD | The F29 slice establishes an explicit **`@mutation` marker / wrapper** that the **authz-test, outbox-emit, AND MOD-10-audit** lints all key off (one recognized seam, not three divergent heuristics); design the outbox-helper + audit-helper **together** so one "mutation side-effects present" check covers both the event and (for privileged writes) the audit row. | closes the exact "enforced by convention not CI" weakness rule-4 was created to fix — for the audit mandate + the unprefixed-mutation escape | major (must-now) | 6 |
| F44 | doc-anchor housekeeping (near-free, in the G-A doc-fix bundle) | MODIFY | §8 phasing +1 line cross-referencing M0–M8 (an off-by-one vs road-map otherwise) · §9 **token/email membership** (tokens in secure-store NOT redux-persist — the literal §9 list implies persist; rides F14/F39) · rule-8 → **"dependency (runtime *or dev*)"** (a malicious dev dep runs in CI with FS+secret access; matches G-M's manifest scope) · file an OQ pinning **ECON-02 = UTC-day** (decide-by-M5 wallet schema) · a 3-line CONVENTIONS **"Start here"** entry-block for a cold agent. | minutes each; the §9 token line is load-bearing (kills the "tokens in persist" reading) | minor | 6 |

**Round-6 integration & refinements (applied to existing findings):**
- **F01 — the round-1↔round-6 tension RESOLVED:** ship a **transactional OUTBOX TABLE** (event row inserted in the mutation's *own* DB tx — *less* code than a reliable in-process bus, and the only version where rule-5's "no lost history" claim is actually TRUE); **defer the relay/consumer to M7**. ("Cut the outbox" (R1) meant cut the *durable delivery/polling-worker*, not the tx-local table.) Split rule-5: presence-lint (helper present) now · content-registry (event-name enum + payload + snapshot) when it lands.
- **F03 → allowlist, not denylist:** the guard must fail-closed on **absence of a `DISPOSABLE_DB` sentinel** (not "URL ≠ known-prod") — a CI secret injecting a real `DATABASE_URL` into the Testcontainers job could point truncate/reset at prod *from inside CI*.
- **F05 → split G-B:** clauses a/b/c (lint-teeth · red-on-purpose · refused-bad-PR) fire at M1-exit **unconditionally** (emulator/Expo Go); clause **d** (physical iPhone) detaches to an **M1-P-completion tripwire** → dissolves the Apple-enrollment deadlock.
- **F18 → NARROWED:** the MOD-10 audit log is **already specced** (§6/§8) — only Sentry/pino/request-IDs/funnel lack a product-spec **§7 anchor**; add the §7 bullet + a **G-E round-trip demo** (thrown error shows in Sentry; a funnel event shows in the dash).
- **F25 → the one OVER-CONSTRAINT:** scope expand-contract to **data-bearing DBs (G-C onward)**; plain migrations are fine on disposable DBs during M1–M4.
- **F29 → include exactly ONE user-owned mutating route** (`PATCH /me bio`, stubbed actor, not the full M2 auth stack) so rules 4/5 + F30/F32 + G-B(c) all bite on one artifact → dissolves the double-bind.
- **F22 → merge with G-B(c)** into one committed `fixtures/bad-pr-corpus` (one bad fixture per lint rule + meta-test) that IS F22, IS G-B's evidence, AND the new-agent example. **F26 → folded into F30.** **F32 → promoted to MUST-NOW** (fail-closed manifest decided now, even if empty).

## Priority stamp (round 5 — the accretion guard, F37)
**MUST-NOW (in the M1 scaffold commit / G-A):** the SYS-01 guard (rules 2&4 + helper + inventory-diff lint) · F03 fail-closed DB runner · F04 key taxonomy + bundle-grep · F22 lint-spine self-test · F29 golden-path slice · F30 route-registration helper · F32 user-owned-vs-global boundary + EXEMPT cross-check · F33 rule-7 third path · F11 pin npm · F37 the manifest itself · F05 Windows/Docker pre-flight + **start Apple enrollment day-one** · F08/F10/F12 doc fixes before sign-off.
**M2-FOUNDATION (land with the first real endpoints; gate at M2 exit, batched on G-D/E/G):** F06 read-path contract · F14 token-at-rest · F15 refresh-revocation model · F16 audit-log + role-enum only · F17 limiter-presence · F20 persist purge/namespace · F21+F24 version-stamp convention · F23 request/response split · F31 RTK z.infer · F36 concurrency tests · F09 snapshot test.
**FAST-FOLLOW (during M2, not gate-blocking):** rule-5 lint TEETH (the emit *seam* is must-now; teeth defer until a consumer — **pull to M3 if checklist discipline slips**) · F39 email transport · F28 Drizzle journal check · F35 lint hardening.
**DEFER to owning milestone:** F13 (M5 IAP IDOR) · F16 privileged-ops (M7) · F07 (M7-exit deletion-ripple) · F18 PII/Sentry (M2 wiring, M8 G-O review) · F38 restore-drill (M3/before-M4-beta) · G-N/G-O (M8) · CARD-15 schema design (M4/G-H).

**Round-5 new risks (fold into the changes):**
- **Gate-dependency deadlock:** G-B's iPhone clause depends on M1-P (Apple enrollment, multi-week) + no local iOS on Windows → start enrollment day-one **OR** split G-B (a/b/c clear at M1-exit, d trails to M1-P). (sharpens F05)
- **F29 double-bind:** the G-B scaffold-proving slice (M1, no real authz) vs the M2 authz'd slice (needs the users/profile data layer) may be **two artifacts** — partially undoes "collapse two sittings." Decide which F29 is at G-A.
- **Async money-gates:** G-K/G-L must be **async (logged yes + safe-default-until-approved)** or a release blocks for days on owner throughput.
- **DoD↔rule-5 consistency:** if rule-5 teeth downgrade to a checklist, the DoD "domain event emitted" line must ripple in the **same** edit (else they contradict — an agent guess-point).

## RUNLOG
- **Round 1** · 2026-06-30 · lens: general/over-engineering (note: angle arg didn't propagate — ran broad, good seed) · 5 area-critics → synth → steelman-vet · **12 findings survived** (4 major-security/correctness, 3 major-process, 5 minor), **8 KEEPs confirmed**, **5 vet-risks folded in** (MOD-10 coupling→F01, RED-on-purpose toy→F02, EAS quota→F05, citation drift→F12). Verdict: foundation sound; sharpen 6 majors + 6 minors. Top regret: **F03 fail-closed DB guard** (the one unrecoverable, un-gated failure mode).
- **Round 6** · 2026-06-30 · lens: completeness critic + integration/convergence · **CONVERGED — loop closed (its own verdict: "do NOT run round 7").** 5 genuinely-new (F40 stale testing-strategy · F41 version-matrix pin · F42 backup-verify-provider · F43 `@mutation` marker + un-linted MOD-10 audit-row · F44 doc-housekeeping) + integration/merges that RESOLVED the open conflicts: **F29** = one-mutation slice (dissolves the double-bind) · **F05** split G-B (a/b/c unconditional, d→M1-P tripwire) · **F01** outbox-TABLE-in-tx + defer relay (reconciles R1↔R6) · **F22**+G-B(c) merge to one bad-PR corpus · **F26**→F30 · **F32**→must-now · **F03**→allowlist-not-denylist · **F18** narrowed (audit already specced; only Sentry/funnel needs a §7 anchor) · **F25** = the one over-constraint (scope to data-bearing DBs). Set certified **internally coherent + implementable together.** Top: **F40/F43** (the last unaudited doc + the un-linted 2nd mutation side-effect).
- **Round 5** · 2026-06-30 · lens: ship-speed + over-correction counter-lens · **CONVERGING** — only **3 net-new** (F37 manifest, F38 restore-drill re-time, F39 email transport); the rest re-confirmed/prioritized. **Verdict: NOT broadly over-corrected** — SYS-01 guard, boring stack, staged-release plan, scoped rulebook all confirmed right-sized; explicitly **REJECTED** adding Turborepo/Nx, blanket runtime validation, broader spec-ID tagging, per-PR dep gates, or a new pre-scaffold gate. Two over-correction pockets, both **timing not deletion**: (1) event teeth/outbox/G-E → keep seam, defer enforcement to M7; (2) designing M4/M5/M7 detail into M1. Key output: the **Priority stamp** (7-item M1 spine + must-now/fast-follow/defer for all 39). New risks: G-B↔M1-P deadlock · F29 double-bind · async money-gates · DoD↔rule-5 consistency. Top: **F37** — without the manifest the pile silently inflates the morale-critical M2 sitting.
- **Round 4** · 2026-06-30 · lens: AI-agent buildability · (1st run hit the session limit; **re-run completed full-rigor after reset**) · **convergent, not divergent.** The danger is concentrated where **prose pretends to be enforcement** — rule-4 "diff" + rule-5 "emit" have no defined substrate, so an agent invents a derivation + fakes green. The ship-blockers collapse onto ONE fix: **F29** (living golden-path slice → ship-blocker + M1 deliverable). Elevated **F03→ship-blocker** (runtime fail-closed marker) + **F30→ship-blocker** (route-registration helper, subsumes F26). New F30–F36 (route-helper · RTK z.infer binding · user-owned-vs-global boundary + EXEMPT cross-check · rule-7 `// ASSUMPTION(OQ)` third path · scripted G-B/G-D fixtures + edit the gate TEXT in 0045 · rule-6 id-registry + rule-8 dep-surfacer · **F36 Testcontainers must force CONCURRENCY** — else the economy/authz race tests are sequential = false confidence). Refined: **F01** (typed DomainEventType registry + negative meta-test; reframe G-E to "single emitOnCommit seam, un-bypassable" — NOT a re-opened outbox) · **F25** (destructive-DDL grep auto-trips the change-class + worked example + split G-F) · **F09** (the rule-3 snapshot test 0045 PROMISED is a **phantom guard** — make it a real M1 deliverable; doc-gen = fast-follow). **Churn (steelman-rebutted):** "add a transactional outbox" (F01 already cut it), blanket RTK runtime validation, up-front doc-gen. **KEEP-with-a-guard:** assert a real PG container started; G-H "surface the blocker, don't stub the skia renderer". Top regret: **F29/F30** — make the lints sound + give them substrate, or every later gate is theater.
- **Round 3** · 2026-06-30 · lens: long-term / reversibility / lock-in · **NOT converged.** +2 ship-blockers (**F21** composition-JSON `schemaVersion` — un-retrofittable; **F22** lint-spine self-guarding meta-test — rot-class) + 6 (F23 request/response schema split · F24 event envelope+version / outbox-wording · F25 migration re-keying + expand-contract trigger · F26 authz-test TEETH meta-assertion · F27 gate-scheme single-editor governance · F28 Drizzle journal CI check). Reinforced **F03**→★ (G-C rots → fail-closed standing assertion), **F08**→+truth-precedence + append-only enum, **F20**→+persist version-key, **F18**→funnel telemetry through the one ACH-08 emission seam. **Contested:** **F11** pnpm (R1) vs npm-for-RN/Metro (R3) — surface both. **Recurring theme:** an "**append-only / version-stamp**" discipline unifies F21+F24+F08-enum+F25 (one rule, four shapes); the matching CUT (outbox table) stays cut. **KEEP held:** stack/vendors (add no abstraction layers), rules 1&2, dep gate, G-A scope, the R1–R2 privacy/PII guards — all confirmed future-proof, not debt. Top regret: **F21/F22** (un-retrofittable data shape + a spine that can silently defang).
- **Round 2** · 2026-06-30 · lens: security deep-dive (challenge R1 + hunt missed) · **NOT converged — rich vein.** Sharpened 4 (F04→3-bucket taxonomy · **F06→#1 SHIP-BLOCKER** read-path-shape contract · F08→+error-serialization · F10→+client-vs-server-validation) and added **8 new majors F13–F20**: F13 IAP-webhook/`/iap/validate`/signed-payload trust boundaries · F14 token-at-rest (expo-secure-store) · F15 refresh-token revocation model · F16 SYS-08 privilege-check + transactional MOD-10 audit emission · F17 SYS-05 limiter-presence · F18 event-payload PII minimization + Sentry scrubbing · F19 Unicode-NFC on render/screen fields · F20 redux-persist cross-user rehydration. **KEEP re-confirmed:** gate sequencing, public-read surface, input boundary (no HTML/SQLi theater). All 5 vet new-risks folded in (anonymized-author shape→F06, rcUserId IDOR→F13, deep-link forge test→F13, Sentry sink→F18, persist rehydration→F20). Top regret: **F06 read-path ship-blocker** — the authz net watches the wrong half.
